# 目录整理诊断清单（投诉处理系统）

> 生成时间：2026-09-02
> 性质：**只读诊断报告，未移动 / 删除任何文件**。
> 后续若要执行归位或清理，须按本清单逐项目录确认 + 先备份 + 走回收站，严禁一次性大改。
> 配套规范见 [`file-conventions.md`](./file-conventions.md)。

## 一句话结论
根目录把"源码、启动脚本、本该在 `data/` 的数据库、本该在 `docs/` 的文档、空业务占位夹、180+ 项的 `tmp/` 临时堆"全部平铺同层，又混入了违规空 `.db` 与重复文档，所以显得乱。核心源码与数据区本身健康，问题集中在"散落文档 / 空壳 / 重复 / 临时堆"。

## 诊断明细

| 路径 | 分类 | 建议去向 | 依据 | 风险 |
|---|---|---|---|---|
| `app.py` `database.py` `config.py` `api/` `core/` `services/` `crawlers/` `utils/` `templates/` `static/` `tests/` `scripts/` `venv/` `requirements.txt` | 保留 | — | 核心源码 | 无 |
| `start.command` | 保留 | 建议 `scripts/` | 完整三级启动脚本（3390B），AGENTS 约定脚本放 `scripts/` | 低 |
| `启动.command` | 重复/简陋 | 删除留 `start.command` | 48B 仅 `python3 app.py`，未用 venv，是简陋重复版 | 低（需确认） |
| `clear_cache.py` | 保留/归位 | 建议 `scripts/` | 清 Python 缓存（`__pycache__`/`*.pyc`）的维护脚本，AGENTS 约定脚本放 `scripts/` | 低（需确认） |
| `data/`（`complaints.db`+shm/wal+3bak、`config.json`、`contract_templates.yaml`、`logs/`、`cache/`） | 保留 | — | 权威数据区（AGENTS 硬约束：数据库只在 `data/complaints.db`） | 无 |
| `data/cheshang.db` `complaint.db` `operations.db` | 可清理 | 删除 | 0 字节空壳，疑似废弃 | 低（需确认） |
| `uploads/` | 保留 | — | 业务收件，`.gitignore` 已排除 | 无 |
| `1合同种类/` | 保留 | — | 合同种类库，真实 PDF/DOC（2019–2023） | 无 |
| `合同文件/` | 易混/空 | 明确用途或合并 | 与 `1合同种类/` 易混；按 `.gitignore` 本意为"学员合同原件"，当前空 | 中（需确认） |
| `回复函/` | 保留(空占位) | — | `.gitignore` 排除，业务产出 | 无 |
| `回复模板/`（`default_template.docx`） | 保留 | — | 与回复函区分的模板 | 无 |
| `案件归档/` | 保留(空占位) | — | `.gitignore` 排除，业务产出 | 无 |
| `页面源码/`（1 rtf） | 抓取残留 | 归 `docs/` 或清理 | 单文件抓取残留 | 中（需确认） |
| `demo/`（22 html） | 保留 | 留 `demo/`（AGENTS 约定） | 设计产物原型 | 无 |
| `tmp/`（180+ 项） | 临时堆 | 归档清空 | `.gitignore` 排除，主杂乱源 | 中（需确认，深度清理） |
| 根 `database.db` | 违规空壳 | 删除 | **违反 AGENTS 硬约束**（DB 只在 `data/`） | 低（需确认） |
| 根 `complaints.db` | 违规空壳 | 删除 | 同上 | 低（需确认） |
| 根 `三系统API接口细节.md` | 重复 | 合并进 `docs/` | 与 `docs/` 同名重复 | 低（需确认） |
| 根 `…DRD (2).md` | 归位 | 移 `docs/` | 需求计划书（DRD），重要但散落 | 低（需确认） |
| 根 `车辆信息表.xlsx` | 归位 | 移 `data/` | 重要数据源，散落根目录 | 低（需确认） |
| `.gitignore` `AGENTS.md` `PLAN.md` `CONTEXT.md` `DESIGN.md` `MEMORY.md` `README.md` `docs/`(36) | 保留 | — | 规范文档区 | 无 |
| `.aionrs/` `.gemini/` `.omo/` `.opencode/` `.pytest_cache/` `.scratch/` `.sisyphus/` `.superpowers/` `.workbuddy/` | 工具状态 | 随清 | 各工具状态目录 | 中（需确认） |

## 风险分级说明
- **无**：核心资产，勿动。
- **低**：空壳 / 重复 / 明确归位，确认后即可执行（仍需先备份 + 走回收站）。
- **中**：涉及占位夹用途澄清、临时堆清空、工具状态目录，执行前需你确认范围。

## 推荐的后续执行顺序（仍需你逐项确认）
1. **归位散落文档**（最安全、收益最大）：根 `DRD (2).md` → `docs/`；根 `车辆信息表.xlsx` → `data/`；根 `三系统API接口细节.md` 并入 `docs/`。
2. **删除违规空壳**：根 `database.db` / `complaints.db`；`data/` 下 3 个空壳 `.db`。
3. **合并重复脚本**：删 `启动.command`，保留 `start.command`；`clear_cache.py` / `start.command` 归 `scripts/`。
4. **澄清占位夹**：`合同文件/` 与 `1合同种类/` 是否合并；`页面源码/` 处置。
5. **临时堆清理**：`tmp/`、`页面源码/`、`隐藏工具目录`——放在最后，逐批确认。
