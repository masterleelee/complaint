# 未跟踪文件清单 · 扫描报告（只读，未移动/删除任何文件）

**扫描时间**：2026-09-11 00:15
**扫描范围**：`git status` 中 39 项未跟踪条目（`??`），工作区跟踪文件**无改动**
**处置状态**：**本报告只列清单，不执行任何操作**。等你圈定后我再动。

---

## 一、结论速览：只有 1 项是"隐患"，其余都是"要不要存进版本库"的选择

| 优先级 | 类别 | 项数 | 建议 |
|---|---|---|---|
| **P0** | 密钥类 | 1 | **保持不入库**，并补 `.gitignore` 加固 |
| **P1** | 业务资产（丢了难找回） | 6 | **建议入库** |
| **P2** | 计划与诊断记录 | 6 | 建议入库 |
| **P3** | 日志与 DB 备份 | 7 | **不入库**（但也**不要删**） |
| **P4** | UI demo | 11 | **分类处理**：1 个含真实个人信息**不入库**、5 个待你确认、5 个可入库 |
| **P5** | 意外产生的重复文件 | 1 | **建议删除**（内容 100% 可弃） |
| — | 其余（自动生成/临时） | 7 | 不入库 |

---

## 二、P0 · 密钥类（唯一真正需要马上处理的）

### `data/.flask_secret_key`（4 KB）

| 项 | 说明 |
|---|---|
| **是什么** | Flask 用它给登录凭证（session cookie）签名。 |
| **风险** | **它现在没有被 `.gitignore` 忽略**。今天没入库只是因为我们没有执行 `git add -A`；任何人（或任何工具）哪天执行一次全量暂存，**这个密钥就会永久进入 git 历史**。拿到它的人可以伪造任意用户的登录状态，且删除文件也删不掉历史记录。 |
| **好处** | 保持不跟踪：无。它本来就不该进版本库。 |
| **建议** | ①把它写进 `.gitignore`；②顺带把 `data/*.bak-*`、`.scratch/*.log`、`app.pyn` 一并忽略。**这是一行配置的事，零风险**。 |

> ⚠️ 顺带发现的同类疏漏：`.gitignore` 里写的是 `*.db.backup`，但实际备份文件命名是
> `complaints.db.bak-20260829-preimport` —— **规则没生效**，现有 4 个 DB 备份同样处于"一按就进库"状态。

---

## 三、P1 · 业务资产：现在只存在这台电脑上，没有第二份

**风险**：这些文件**不在 git 里**，意味着它们**没有任何版本备份**。磁盘损坏 / 误删 / 换电脑，就真的没了，也没法回退到上一版。

| 文件 / 目录 | 内容 | 为什么建议入库 |
|---|---|---|
| `车辆信息表.xlsx` | 网点与车辆台账**权威源**（58 网点、252 台车） | 全系统的网点字典与"每百车投诉率"分母都从它来。丢了要人工重录。 |
| `纸质合同样板/` | 10 张 jpg，按学员/年份分组的纸质合同扫描 | 合同比对与档位识别的原始依据 |
| `1合同种类/` | 8 个文件（2019/2021/2022/2023 版合同 + 东城自制合同） | 合同条款与档位规则的业务底稿 |
| `scripts/com.complaint.system.plist` | LaunchAgent 守护配置（服务开机自启、崩溃自拉） | **部署关键文件**。丢了服务就起不来，且配置是手写的，重写要靠回忆。 |
| `scripts/setup_firewall.sh` | 防火墙放行脚本 | 局域网其他电脑能访问的前提 |
| `scripts/seed_mock_complaints_100.py` | 造 100 条测试工单的脚本 | 回归测试与演示用 |

**注意事项**：`1合同种类/` 里有 `.doc` 文件，会被 `.gitignore` 的 `*.doc` 规则挡住
（那条规则本意是挡「案件材料」）。入库时这几个需要单独 `-f` 强制添加——
**这是设计冲突，需要你确认**：`.doc` 挡案件材料是隐私保护，不能简单删掉规则。

---

## 四、P2 · 计划与诊断记录（建议入库）

`.scratch/` 是本项目既定的"本地工单台账"，此前已提交过一批。这批尚未入库：

- `.scratch/refactor-audit-20260910.md`（重构审计 10 项）
- `.scratch/open-archive-diagnosis-20260901.md`（"打开归档"故障诊断，是本次方案 A 的上游依据）
- `.scratch/contract-upload-parity/diagnosis-20260903.md`、`golden-samples.md`
- `.scratch/timeline-display-standardize/`（1 文件）、`.scratch/layout-test/`（3 文件）
- `docs/file-conventions.md`、`docs/目录整理清单.md`

**好处**：以后回看"当初为什么这么改"有据可查。**风险**：几乎无（纯 markdown）。

---

## 五、P3 · 日志与 DB 备份：不入库，但也不要删

| 项 | 体积 | 说明 |
|---|---|---|
| `.scratch/backup/` | 4.0 MB / 13 文件 | **历史 DB 备份 + 补丁**，是数据安全垫 |
| `.scratch/server*.log`（5 个）、`api-r2*.log`（2 个） | ~250 KB | 服务运行/重启日志 |
| `data/complaints.db.bak-*`（4 个） | ~1.2 MB | 数据库改动前的备份 |

**为什么不入库**：日志会被反复追加、DB 备份是二进制大文件，进 git 会让仓库体积持续膨胀，且历史价值低。
**为什么不删**：DB 备份是**唯一的数据回滚手段**（正式库 `data/complaints.db` 本身按设计不入库）。删除前必须确认。
**建议**：加进 `.gitignore` 让它"安静地待着"，不进版本库但留在磁盘上。

---

## 六、P4 · UI demo（11 个 html，共约 1 MB）—— ⚠️ **含真实个人信息，不能整目录入库**

对 `demo/*.html` 做了隐私扫描（身份证号 / 手机号），结果分为三类：

### 6.1 明确含**真实**个人信息 → **不要入库**（除非先脱敏）

| 文件 | 命中 |
|---|---|
| `demo/2026_complaints_summary.html` | **3 个真实身份证号**（110101199003070011 / 110101199003070011 / 110101199003070011）<br>**73 个手机号** + **真实学员姓名**（张锦辉、欧阳康、段火兰、熊运泉、王罗祥、蒋利民、陈淑芹、陈淑蓉…） |

这是一个**真实工单汇总报表**。它与 `案件归档/`、`uploads/`、`*.doc` 一样属于"含个人信息"的类别，
而 `.gitignore` 已经明确要挡这类文件。**建议：保持不入库**；若确实想留存，需先脱敏（姓名打码、证件号/手机号替换）。

### 6.2 含身份证号，**真伪无法从文件判断** → 需你确认

| 文件 | 出现的证件号 |
|---|---|
| `demo/ai_intake_demo.html` | 110101199003070011、110101199003070011、110101199003070011 |
| `demo/fuzzy_intake_v2.html`、`demo/fuzzy_search_prototype.html` | 110101199003070011、110101199003070011 |
| `demo/new_complaint_demo.html` | 110101199003070011 |
| `demo/new_complaint_unified_query_demo.html` | 110101199003070011、110101199003070011、110101199003070011、110101199003070011、110101199003070011 |

**无法从文件本身可靠区分"真实学员"与"随手编的测试号"**（例如 `110101199003070011` 尾部连续数字更像编的，
但 `110101199003070011` 无法判断）。**建议按最坏情况处理：不确定就不入库**，
或者你花两分钟核对一下其中 1–2 个是否真实存在。

### 6.3 无个人信息 → 可安全入库

交互原型类（`archive-panel-redesign-demo.html`、`archive-open-options-demo.html`、
`batch_bar_position_demo.html`、`rail_cards_demo.html`、`settings_layout_demo.html`、
`registration_preview_demo.html`、`cl_fix_verify.html`、`contract-upload-parity-v1.html`、
`intake_progress_optimization_demo.html`、`complaint_list_compact_demo.html`、
`complaint_list_final_demo.html`）。

> 本次新出的 `archive-panel-redesign-demo.html` 原本在"技术细节"示例里带了一个测试号
> `110101199003070011`（与 `tests/` 里用的假号一致），已改为脱敏写法 `441900********0011`，
> 使该文件**结构上不含任何证件号**，可直接入库。

---

## 七、P5 · 需要处理的意外文件（1 个）

### `app.pyn`（240 KB）

| 项 | 说明 |
|---|---|
| **是什么** | `app.py` 的一份**快照副本**，比当前版本少 5 行，是重构到一半时的中间状态。它在本次会话中被一个自动审查子进程意外写出（本应只读）。 |
| **风险（留着）** | 它不会被程序加载（Python 只认 `.py`），但**放在项目根会让下次改代码的人（或 AI）改错文件**——改了 `app.pyn` 却不生效，且很难察觉。 |
| **好处（删掉）** | 它的内容**没有任何独有信息**：= 当前 `app.py` 去掉本次新增的那部分，且那部分正是本次提交的内容，`git` 里都有。**删了 100% 不会有任何损失。** |
| **建议** | **删除**（先移到废纸篓，不做不可逆删除）。 |

---

## 八、建议动作清单（按顺序，每一项都可单独批准）

| # | 动作 | 风险 | 影响 |
|---|---|---|---|
| 1 | `.gitignore` 增补：`data/.flask_secret_key`、`data/*.bak-*`、`.scratch/*.log`、`.scratch/backup/`、`app.pyn`、`demo/2026_complaints_*.html` | 极低 | 堵住密钥与个人报表误入库的通道 |
| 2 | 删除 `app.pyn`（走废纸篓） | 极低 | 消除"改错文件"隐患 |
| 3 | 入库 P1 业务资产（6 项） | 低 | 业务资产首次获得版本保护 |
| 4 | 入库 P2 计划记录（6 项） | 极低 | 决策可追溯 |
| 5 | 入库 6.3 的 5 个无隐私 demo + 新 demo | 低 | 设计决策留存 |
| 6 | 6.1 不入库、6.2 待你确认 | 无 | 避免个人信息进入版本库 |
| 7 | P3 保持不入库、不删除 | 无 | 维持现状 |

**未做任何操作前，以上 39 项全部原样保留在磁盘上。**

### 关于 `demo/2026_complaints_summary.html` 的补充说明

它的隐私风险与本清单里的"密钥"是**两类不同的问题**：密钥是"别人能伪造登录"，
个人报表是"真实学员的姓名/证件号/手机号被复制到另一个文件"。后者哪怕不入库，
也建议评估一下这份报表**是否还需要留在项目目录里**——如果只是当时一次性看的统计，
可以考虑移出项目（或改名归档到 `案件归档/` 这类已被忽略的位置）。
是否需要我处理，等你指示，**我不会擅自动它**。
