diff --git a/app.py b/app.py
index 58863af..896f1cf 100644
--- a/app.py
+++ b/app.py
@@ -3,11 +3,13 @@ import asyncio
 import os
 import sys
 import json
+import re
 import traceback
 import mimetypes
 import certifi
 import uuid
 import hashlib
+import shutil
 import atexit
 from datetime import datetime
 import time
@@ -15,6 +17,7 @@ from concurrent.futures import ThreadPoolExecutor, TimeoutError
 from threading import Event, Lock, Thread
 from flask import Flask, render_template, request, jsonify, send_file
 from werkzeug.utils import secure_filename
+from werkzeug.exceptions import RequestEntityTooLarge, NotFound
 
 # 修复 macOS Python 3.14 SSL 证书问题，确保 OCR 库能下载模型
 os.environ['SSL_CERT_FILE'] = certifi.where()
@@ -29,20 +32,19 @@ from database import (
     list_tickets, update_ticket, get_ticket_statistics, get_processing_duration_stats,
     save_complaint, get_complaint_by_idcard,
     list_complaints, get_statistics,
-    add_log, get_recent_logs,
+    add_log,
     save_template, list_templates, get_default_template, delete_template,
     get_distinct_school_short,
-    save_communication_record, get_communication_records, delete_communication_record,
+    migrate_communications_to_notes,
     get_org_vehicle_count_items, get_org_vehicle_counts, save_org_vehicle_counts,
     create_contract_analysis_job, get_contract_analysis_job,
     find_active_contract_analysis_job, update_contract_analysis_job,
 )
 from core.query_engine import query_engine, query_all_systems_sync
-from core.case_workflow import calculate_saved_fee_plan
 from core.auth_manager import auth_manager, refresh_session_if_due, SystemType
-from services.contract_service import analyze_contract_from_file, contract_set_from_reviewed_fields, apply_authoritative_total_fee
-from services.reply_service import generate_reply
-from services.feishu_service import FeishuService
+from services.contract_service import analyze_contract_from_file, apply_authoritative_total_fee
+from services.reply_docx import generate_reply_docx
+from services.archive_service import archive_gate_errors, build_archive_dir, archive_case
 from services.intake_service import parse_complaint_file, ensure_upload_dir
 from services.org_unit_service import ORGANIZATION_UNITS, resolve_org_unit
 from services.file_service import is_path_within, create_derived_copy, unique_path
@@ -52,6 +54,19 @@ app = Flask(__name__, template_folder="templates", static_folder="static")
 app.config["MAX_CONTENT_LENGTH"] = 50 * 1024 * 1024  # 50MB
 app.config["TEMPLATES_AUTO_RELOAD"] = True  # 禁用模板缓存
 
+
+@app.errorhandler(RequestEntityTooLarge)
+def _handle_request_entity_too_large(_e):
+    return _err("上传文件超过大小限制（50MB）", 413)
+
+
+@app.errorhandler(NotFound)
+def _handle_not_found(e):
+    """API 未知路由返回 JSON 404，页面路由保持原 HTML 404"""
+    if request.path.startswith("/api/"):
+        return _err("接口不存在", 404)
+    return e
+
 UPLOAD_DIR = os.path.join(PROJECT_DIR, "uploads")
 ARCHIVE_DIR = os.path.join(PROJECT_DIR, "案件归档")
 REPLY_DIR = os.path.join(PROJECT_DIR, "回复函")
@@ -76,14 +91,6 @@ THIRD_SESSION_REFRESH_AFTER_SECONDS = 12 * 60
 DRIVING_SESSION_REFRESH_AFTER_SECONDS = 12 * 60
 INTERNAL_SESSION_CHECK_INTERVAL_SECONDS = 60
 INTERNAL_SESSION_MAINTENANCE_STOP = Event()
-FINAL_OUTCOMES = {
-    "投诉撤销",
-    "同意合同扣费",
-    "不同意合同扣费但协商一致",
-    "不同意合同扣费且协商失败",
-    "无法联系",
-    "继续培训/转校",
-}
 FEE_PLAN_FIELDS = {
     "total_fee",
     "actual_paid",
@@ -185,43 +192,40 @@ def _err(msg, code=400):
     return jsonify({"success": False, "error": msg}), code
 
 
-def _fee_plan_ticket_or_error(ticket_id: str, allowed_statuses: set[str], material_name: str):
-    """对外材料必须基于人工确认过的阶段或正式费用方案。"""
+def parse_deductions(raw) -> list:
+    """兼容解析扣费明细：list 直返；str 反序列化；dict（save_analysis 整包存储）提取其明细数组。"""
+    if isinstance(raw, str):
+        try:
+            raw = json.loads(raw)
+        except json.JSONDecodeError:
+            return []
+    if isinstance(raw, dict):
+        inner = raw.get("deductions", raw.get("deduction_detail"))
+        return inner if isinstance(inner, list) else []
+    return raw if isinstance(raw, list) else []
+
+
+def _fee_plan_ticket_or_error(ticket_id: str, material_name: str):
+    """对外材料必须基于人工确认过的费用明细。返回 (ticket, err, http_status)。"""
     if not ticket_id:
-        return None, f"缺少案件ID，不能生成{material_name}"
+        return None, f"缺少案件ID，不能生成{material_name}", 400
     ticket = get_ticket(ticket_id)
     if not ticket:
-        return None, "工单不存在"
-    if ticket.get("fee_plan_status") not in allowed_statuses:
-        if allowed_statuses == {"confirmed"}:
-            return None, "请先人工正式确认费用方案，再生成正式材料"
-        return None, "请先人工确认阶段或正式费用方案，再生成进展回复"
-    return ticket, ""
+        return None, "工单不存在", 404
+    if ticket.get("fee_plan_status") != "confirmed":
+        return None, "请先人工确认费用方案，再生成材料", 400
+    return ticket, "", 200
 
 
 def _confirmed_ticket_or_error(ticket_id: str):
-    """正式文档/归档输出必须基于已人工正式确认的费用方案。"""
-    return _fee_plan_ticket_or_error(ticket_id, {"confirmed"}, "正式材料")
+    """正式文档/归档输出必须基于已人工确认的费用明细。"""
+    return _fee_plan_ticket_or_error(ticket_id, "正式材料")
 
 
 def _official_case_payload(ticket: dict, incoming: dict | None = None) -> dict:
-    """以数据库中已确认的费用方案为准，忽略前端传入的金额/扣费草稿。"""
+    """以数据库中已确认的费用明细为准，忽略前端传入的金额/扣费草稿。"""
     incoming = incoming or {}
-    snapshot = ticket.get("fee_plan_snapshot", {})
-    if isinstance(snapshot, str):
-        try:
-            snapshot = json.loads(snapshot) if snapshot else {}
-        except json.JSONDecodeError:
-            snapshot = {}
-    if not isinstance(snapshot, dict):
-        snapshot = {}
-    official = snapshot if ticket.get("fee_plan_status") == "confirmed" else {}
-    deductions = official.get("deductions", ticket.get("deduction_detail", []))
-    if isinstance(deductions, str):
-        try:
-            deductions = json.loads(deductions) if deductions else []
-        except json.JSONDecodeError:
-            deductions = []
+    deductions = parse_deductions(ticket.get("deduction_detail"))
 
     return {
         **incoming,
@@ -237,14 +241,14 @@ def _official_case_payload(ticket: dict, incoming: dict | None = None) -> dict:
         "complaint_date": ticket.get("complaint_date") or incoming.get("complaint_date", ""),
         "complaint_channel": ticket.get("source_channel") or incoming.get("complaint_channel", ""),
         "complaint_type": ticket.get("complaint_type") or incoming.get("complaint_type", ""),
-        "total_fee": float(official.get("total_fee", ticket.get("total_fee", 0)) or 0),
-        "registration_fee": float(official.get("total_fee", ticket.get("total_fee", 0)) or 0),
-        "actual_paid": float(official.get("actual_paid", ticket.get("actual_paid", 0)) or 0),
+        "total_fee": float(ticket.get("total_fee", 0) or 0),
+        "registration_fee": float(ticket.get("total_fee", 0) or 0),
+        "actual_paid": float(ticket.get("actual_paid", 0) or 0),
         "deductions": deductions,
-        "total_deduction": float(official.get("total_deduction", ticket.get("deduction_fee", 0)) or 0),
-        "deduction": float(official.get("total_deduction", ticket.get("deduction_fee", 0)) or 0),
-        "deduction_fee": float(official.get("total_deduction", ticket.get("deduction_fee", 0)) or 0),
-        "refund": float(official.get("refund", ticket.get("refund_fee", 0)) or 0),
+        "total_deduction": float(ticket.get("deduction_fee", 0) or 0),
+        "deduction": float(ticket.get("deduction_fee", 0) or 0),
+        "deduction_fee": float(ticket.get("deduction_fee", 0) or 0),
+        "refund": float(ticket.get("refund_fee", 0) or 0),
         "contract_code": ticket.get("contract_code") or incoming.get("contract_code", ""),
         "training_hours": ticket.get("training_hours") or incoming.get("training_hours", {}),
         "final_outcome": ticket.get("final_outcome") or incoming.get("final_outcome", ""),
@@ -253,42 +257,22 @@ def _official_case_payload(ticket: dict, incoming: dict | None = None) -> dict:
 
 
 def _completion_gate_error(ticket: dict, incoming: dict) -> str:
-    """案件完结/归档必须满足闭环条件，避免空结论归档。"""
+    """案件完结/归档三闸门：处理情况已填 + 配合度已评 + 费用明细已确认。"""
     wants_complete = incoming.get("handle_status") == "已完结" or incoming.get("archive_status") == "已归档"
     if not wants_complete:
         return ""
 
-    final_outcome = (incoming.get("final_outcome") or ticket.get("final_outcome") or "").strip()
-    if not final_outcome:
-        return "请选择最终投诉结果，再完结归档案件"
-    if final_outcome not in FINAL_OUTCOMES:
-        return "最终投诉结果必须选择系统规定的六类结果之一"
-    if final_outcome == "投诉撤销":
-        if (incoming.get("withdraw_status") or ticket.get("withdraw_status") or "") != "已撤诉":
-            return "投诉撤销必须对应学员已撤诉（withdraw_status=已撤诉），请先更新撤诉状态再归档"
-        return ""
+    handling_notes = str(incoming.get("handling_notes") or ticket.get("handling_notes") or "").strip()
+    if not handling_notes:
+        return "请先填写处理情况，再完结归档案件"
+
+    cooperation = str(incoming.get("branch_cooperation") or ticket.get("branch_cooperation") or "").strip()
+    if not cooperation:
+        return "请先评价网点配合度，再完结归档案件"
 
     fee_status = incoming.get("fee_plan_status") or ticket.get("fee_plan_status", "")
     if fee_status != "confirmed":
-        return "请先人工正式确认费用方案，再完结归档案件"
-
-    communications = get_communication_records(ticket.get("id", ""))
-    if not communications:
-        return "请至少登记一次学员沟通记录，再完结归档案件"
-
-    current_version = int(ticket.get("fee_plan_version") or 0)
-    communication_versions = {
-        int(record.get("fee_plan_version") or 0) for record in communications
-    }
-    # 旧库中的沟通迁移为 v0；仅对历史/v1 方案兼容，v2 起必须严格对应。
-    if current_version == 0:
-        has_current_version = 0 in communication_versions
-    elif current_version == 1:
-        has_current_version = bool(communication_versions.intersection({0, 1}))
-    else:
-        has_current_version = current_version in communication_versions
-    if not has_current_version:
-        return f"请先登记当前费用方案版本 v{current_version} 的学员沟通记录，再完结归档案件"
+        return "请先人工确认费用明细，再完结归档案件"
 
     return ""
 
@@ -344,9 +328,6 @@ def background_login():
     system_logger.info("[预登录] 已启动后台登录线程")
 
 
-background_login()
-
-
 def _maintain_sessions():
     """按系统后台刷新登录态，避免首个业务查询承担验证码登录（含第三/东莞驾培）。"""
     refresh_plan = [
@@ -367,13 +348,17 @@ def _maintain_sessions():
                 system_logger.warning("[会话维护] %s: %s", system_type.value, exc)
 
 
-INTERNAL_SESSION_MAINTENANCE_THREAD = Thread(
-    target=_maintain_sessions,
-    name="session-maintenance",
-    daemon=True,
-)
-INTERNAL_SESSION_MAINTENANCE_THREAD.start()
-atexit.register(INTERNAL_SESSION_MAINTENANCE_STOP.set)
+def _start_background_services():
+    """应用真正对外服务前才启动后台线程与一次性迁移（模块导入保持无副作用）。"""
+    try:
+        migrate_communications_to_notes()
+    except Exception as e:
+        system_logger.error("[初始化] 沟通记录迁移失败: %s", e)
+
+    background_login()
+    thread = Thread(target=_maintain_sessions, name="session-maintenance", daemon=True)
+    thread.start()
+    atexit.register(INTERNAL_SESSION_MAINTENANCE_STOP.set)
 
 
 # ═══════════════════════════════════════════════════════════════
@@ -424,9 +409,10 @@ def api_intake_parse():
         add_log("intake_parse", f"受理解析: {file.filename} -> {result.get('student_name', '?')}")
         return _ok(result)
 
+    except RequestEntityTooLarge:
+        return _err("上传文件超过大小限制（50MB）", 413)
     except Exception as e:
         traceback.print_exc()
-        add_log("intake_parse", f"受理失败: {e}", success=False)
         return _err(str(e), 500)
 
 
@@ -441,7 +427,8 @@ def api_intake_parse():
 def validate_id_card(id_card: str) -> tuple[bool, str]:
     """
     只验证18位大陆身份证（GB 11643-1999 校验码算法）
-    其他证件（居留证、港澳台等）不验证，直接放行
+    其他证件仅放行已知格式（居留证如 F1249468(8)、外国人永久居留证 3字母+12数字、纯数字证件号），
+    明显乱码直接拦截，避免无效请求直打生产三系统
     返回: (是否有效, 错误信息)
     """
     id_card = id_card.strip().upper()
@@ -452,10 +439,10 @@ def validate_id_card(id_card: str) -> tuple[bool, str]:
         if id_card[17] != check_codes[s % 11]:
             return False, "身份证号校验失败，请确认是否正确"
         return True, ""
-    # 其他证件不验证，直接放行
-    if len(id_card) >= 7:
+    # 非18位：格式白名单（前缀最多3位大写字母 + 6~17位数字 + 可选括号尾注，兼容居留证 F1249468(8)）
+    if re.fullmatch(r"[A-Z]{0,3}\d{6,17}(?:[（(]\d{1,4}[)）])?", id_card):
         return True, ""
-    return False, "证件号至少7位"
+    return False, "证件号格式不正确"
 
 
 def _persist_query_result(data: dict, result: dict, id_card: str, phone: str) -> dict:
@@ -484,7 +471,7 @@ def _persist_query_result(data: dict, result: dict, id_card: str, phone: str) ->
         "complaint_type": data.get("complaint_type", "") or "A",
         "complaint_date": data.get("complaint_date", datetime.now().strftime("%Y-%m-%d")),
         "handler_name": data.get("handler_name", ""),
-        "complaint_summary": data.get("complaint_summary", ""),
+        "complaint_content": str(data.get("complaint_desc", "") or ""),
         "attachments": data.get("attachments", []),
     }
     if data.get("ticket_id"):
@@ -503,7 +490,9 @@ def _persist_query_result(data: dict, result: dict, id_card: str, phone: str) ->
 def api_query():
     """查询三系统学员信息（支持身份证/居留证优先，手机号备选）"""
     try:
-        data = request.get_json(force=True)
+        data = request.get_json(force=True, silent=True)
+        if data is None:
+            return _err("请求体不是合法JSON", 400)
         id_card = data.get("id_card", "").strip()
         phone = data.get("phone", "").strip()
 
@@ -668,7 +657,9 @@ def _query_job_worker(job_id: str, data: dict):
 
 @app.route("/api/query/start", methods=["POST"])
 def api_query_start():
-    data = request.get_json(force=True)
+    data = request.get_json(force=True, silent=True)
+    if data is None:
+        return _err("请求体不是合法JSON", 400)
     id_card = data.get("id_card", "").strip()
     phone = data.get("phone", "").strip()
 
@@ -749,15 +740,20 @@ def api_tickets_list():
         search = request.args.get("search", "")
         start_date = request.args.get("start_date", "")
         end_date = request.args.get("end_date", "")
-        limit = int(request.args.get("limit", 10))
-        offset = int(request.args.get("offset", 0))
+        repeat_only = request.args.get("repeat_only", "") in ("1", "true", "True")
+        try:
+            limit = int(request.args.get("limit", 10))
+            offset = int(request.args.get("offset", 0))
+        except ValueError:
+            limit, offset = 10, 0
         # 深分页保护：limit 上限 200，offset 上限 100000，防恶意深翻页拖垮查询
         limit = min(max(limit, 1), 200)
         offset = min(max(offset, 0), 100000)
 
         records, total = list_tickets(
             status=status, source=source, school=school, search=search,
-            start_date=start_date, end_date=end_date, limit=limit, offset=offset
+            start_date=start_date, end_date=end_date, repeat_only=repeat_only,
+            limit=limit, offset=offset
         )
         return _ok({"records": records, "total": total})
     except Exception as e:
@@ -780,18 +776,23 @@ def api_tickets_get(ticket_id):
 def api_tickets_update(ticket_id):
     """更新工单（支持文件归档）"""
     try:
-        data = request.get_json(force=True)
+        data = request.get_json(force=True, silent=True)
+        if data is None:
+            return _err("请求体不是合法JSON", 400)
         ticket = get_ticket(ticket_id)
         if not ticket:
             return _err("工单不存在", 404)
         if ticket.get("fee_plan_status") == "confirmed" and FEE_PLAN_FIELDS.intersection(data):
-            return _err("正式费用方案已锁定；请通过重新确认费用方案并填写修改原因来调整")
+            return _err("费用明细已确认锁定；请先调用费用解锁（fee-unlock）再调整")
+
+        if data.get("handle_status") is not None and data["handle_status"] not in {"待处理", "处理中", "已完结"}:
+            return _err("处理状态非法", 400)
 
-        if ticket.get("archive_status") == "已归档" and (
-            data.get("handle_status") in {"待处理", "处理中"}
-            or data.get("archive_status") in {"未归档", "归档失败"}
-        ):
-            return _err("已归档案件只能通过重新打开费用方案并填写修改原因恢复处理")
+        if ticket.get("archive_status") == "已归档":
+            if data.get("handle_status") is not None:
+                return _err("已归档案件请先通过费用解锁（fee-unlock）恢复处理", 400)
+            if data.get("archive_status") == "":
+                return _err("已归档案件不允许清空归档状态", 400)
 
         gate_error = _completion_gate_error(ticket, data)
         if gate_error:
@@ -848,124 +849,41 @@ def _merge_graduation_warning(existing, student_status) -> list:
 
 @app.route("/api/tickets/<ticket_id>/fee-confirm", methods=["POST"])
 def api_ticket_fee_confirm(ticket_id):
-    """人工确认费用方案：AI 结果只有确认后才作为正式沟通/文档依据。"""
+    """确认费用方案（v2 简化）：直接保存扣费行集与实缴金额并标记已确认。"""
     try:
         ticket = get_ticket(ticket_id)
         if not ticket:
             return _err("工单不存在", 404)
 
-        data = request.get_json(force=True)
-        plan_status = str(data.get("plan_status") or "confirmed").strip()
-        if plan_status not in {"provisional", "confirmed"}:
-            return _err("费用方案状态只能是 provisional 或 confirmed")
-        candidate_contract_set = data.get("contract_set")
-        reviewed_fields = data.get("contract_fields") or {}
-        if reviewed_fields and (
-            not ticket.get("contract_set")
-            or ticket.get("fee_plan_status") == "needs_review"
-        ):
-            manifest = ticket.get("contract_manifest") or {}
-            if isinstance(manifest, str):
-                try:
-                    manifest = json.loads(manifest)
-                except json.JSONDecodeError:
-                    manifest = {}
-            page_evidence = [
-                item.get("filepath")
-                for item in (manifest.get("source_files") or [])
-                if isinstance(item, dict) and item.get("filepath")
-            ]
-            reviewed_contract_set = contract_set_from_reviewed_fields(
-                reviewed_fields,
-                ticket.get("contract_path", ""),
-                contract_code=str(data.get("contract_code") or "").strip(),
-                evidence={"source": "human_review", "pages": page_evidence},
-            )
-            ticket = {**ticket, "contract_set": reviewed_contract_set}
-        elif not ticket.get("contract_set") and candidate_contract_set:
-            ticket = {**ticket, "contract_set": candidate_contract_set}
-
-        if ticket.get("fee_plan_status") == "needs_review":
-            fields = data.get("contract_fields", {})
-
-            def field_value(name):
-                value = fields.get(name, "")
-                return value.get("value", "") if isinstance(value, dict) else value
-
-            missing = []
-            if float(field_value("total_fee") or 0) <= 0:
-                missing.append("合同总培训费")
-            if not str(field_value("refund_clause") or "").strip():
-                missing.append("退费条款")
-            if missing:
-                return _err(f"合同关键字段人工补录仍缺少：{'、'.join(missing)}")
-
-        if ticket.get("contract_set"):
-            overrides = {}
-            if "actual_paid" in data:
-                overrides["actual_paid"] = data["actual_paid"]
-            fee_plan = calculate_saved_fee_plan(ticket, overrides)
-            if not fee_plan["can_confirm"]:
-                return _err("费用方案缺少确认条件：" + "、".join(
-                    item["message"] for item in fee_plan["blockers"]
-                ))
-            deductions = fee_plan["deductions"]
-            total_fee = float(fee_plan["total_fee"] or 0)
-            actual_paid = float(fee_plan["actual_paid"] or 0)
-            total_deduction = float(fee_plan["total_deduction"])
-            refund = float(fee_plan["refund"] or 0)
-        else:
-            deductions = data.get("deductions", [])
-            if not isinstance(deductions, list) or not deductions:
-                return _err("请提供扣费明细")
-            total_fee = float(data.get("total_fee", 0) or 0)
-            actual_paid = float(data.get("actual_paid", 0) or total_fee or 0)
-            if actual_paid <= 0:
-                return _err("请填写学员实际已交金额")
-            raw_deduction = round(sum(float(d.get("amount", 0) or 0) for d in deductions), 2)
-            total_deduction = min(raw_deduction, total_fee) if total_fee > 0 else raw_deduction
-            refund = max(0, round(actual_paid - total_deduction, 2))
-        now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
-        current_status = ticket.get("fee_plan_status") or ""
-        replacing_confirmed_plan = current_status in {"provisional", "confirmed"}
-        reopen_reason = str(data.get("reopen_reason") or "").strip()
-        if replacing_confirmed_plan and not reopen_reason:
-            return _err("已确认过费用方案，重新确认必须填写修改原因")
-
-        history = ticket.get("fee_plan_history") or []
-        if isinstance(history, str):
+        data = request.get_json(force=True, silent=True)
+        if data is None:
+            return _err("请求体不是合法JSON", 400)
+        data = data or {}
+        deductions = parse_deductions(data.get("deductions")) or parse_deductions(ticket.get("deduction_detail"))
+        if not deductions:
+            return _err("请提供扣费明细")
+
+        total_fee = float(data.get("total_fee", ticket.get("total_fee", 0)) or 0)
+        actual_paid = float(
+            data.get("actual_paid", data.get("paid_amount", ticket.get("actual_paid", 0))) or 0
+        )
+        if actual_paid <= 0:
+            return _err("请填写学员实际已交金额")
+
+        for _d in deductions:
+            if not isinstance(_d, dict):
+                return _err("扣费金额必须为不小于0的数值", 400)
             try:
-                history = json.loads(history) if history else []
-            except json.JSONDecodeError:
-                history = []
-        current_version = int(ticket.get("fee_plan_version") or 0)
-        if replacing_confirmed_plan:
-            history.append({
-                "version": current_version or 1,
-                "fee_plan_status": current_status,
-                "total_fee": float(ticket.get("total_fee", 0) or 0),
-                "actual_paid": float(ticket.get("actual_paid", 0) or 0),
-                "deduction_fee": float(ticket.get("deduction_fee", 0) or 0),
-                "refund_fee": float(ticket.get("refund_fee", 0) or 0),
-                "deduction_detail": ticket.get("deduction_detail", []),
-                "fee_confirmed_by": ticket.get("fee_confirmed_by", ""),
-                "fee_confirmed_at": ticket.get("fee_confirmed_at", ""),
-                "fee_confirm_note": ticket.get("fee_confirm_note", ""),
-                "reopen_reason": reopen_reason,
-                "replaced_at": now,
-            })
-        next_version = (current_version or 0) + 1
-        snapshot = {
-            "version": next_version,
-            "fee_plan_status": plan_status,
-            "total_fee": total_fee,
-            "actual_paid": actual_paid,
-            "deductions": deductions,
-            "total_deduction": total_deduction,
-            "refund": refund,
-            "confirmed_at": now,
-            "warnings": [w for w in (fee_plan.get("warnings") or [])] if ticket.get("contract_set") else [],
-        }
+                _amt = float(_d.get("amount", 0))
+            except (TypeError, ValueError):
+                return _err("扣费金额必须为不小于0的数值", 400)
+            if _amt < 0:
+                return _err("扣费金额必须为不小于0的数值", 400)
+        raw_deduction = round(sum(float(d.get("amount", 0) or 0) for d in deductions), 2)
+        total_deduction = min(raw_deduction, total_fee) if total_fee > 0 else raw_deduction
+        refund = max(0, round(actual_paid - total_deduction, 2))
+
+        now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
 
         case_fields = {}
         if ticket.get("archive_status") == "已归档" or ticket.get("handle_status") == "已完结":
@@ -987,29 +905,22 @@ def api_ticket_fee_confirm(ticket_id):
             "deduction_fee": total_deduction,
             "refund_fee": refund,
             "deduction_detail": deductions,
-            "fee_plan_status": plan_status,
-            "fee_plan_version": next_version,
-            "fee_plan_history": history,
-            "fee_plan_snapshot": snapshot if plan_status == "confirmed" else ticket.get("fee_plan_snapshot", {}),
-            "contract_set": ticket.get("contract_set", {}),
-            "reply_outdated": ticket.get("reply_outdated", False),
-            "contract_code": (data.get("contract_code") or ticket.get("contract_code") or "").strip(),
-            "fee_confirmed_by": data.get("confirmed_by", "").strip(),
+            "fee_plan_status": "confirmed",
+            "fee_confirmed_by": (str(ticket.get("fee_confirmed_by") or "")
+                                 if not data.get("confirmed_by") else str(data.get("confirmed_by")).strip()),
             "fee_confirmed_at": now,
-            "fee_confirm_note": data.get("confirm_note", "").strip(),
+            "fee_confirm_note": str(data.get("confirm_note") or "").strip(),
             "special_warnings": special_warnings,
             **case_fields,
         })
-        status_label = "阶段费用方案" if plan_status == "provisional" else "正式费用方案"
-        add_log("fee_confirm", f"确认{status_label}: 扣费{total_deduction}元，应退{refund}元", ticket_id=ticket_id)
+        add_log("fee_confirm", f"确认费用明细: 扣费{total_deduction}元，应退{refund}元", ticket_id=ticket_id)
         return _ok({
             "ticket_id": ticket_id,
             "total_fee": total_fee,
             "actual_paid": actual_paid,
             "total_deduction": total_deduction,
             "refund": refund,
-            "fee_plan_status": plan_status,
-            "fee_plan_version": next_version,
+            "fee_plan_status": "confirmed",
         })
     except ValueError as e:
         add_log("fee_confirm", f"校验失败: {str(e)}", success=False, ticket_id=ticket_id)
@@ -1020,85 +931,45 @@ def api_ticket_fee_confirm(ticket_id):
         return _err(str(e), 500)
 
 
-@app.route("/api/tickets/<ticket_id>/fee-reopen", methods=["POST"])
-def api_ticket_fee_reopen(ticket_id):
-    """Reopen a confirmed plan without destroying the last external-facing snapshot."""
+@app.route("/api/tickets/<ticket_id>/fee-unlock", methods=["POST"])
+def api_ticket_fee_unlock(ticket_id):
+    """解锁已确认的费用明细：仅把 confirmed 置回未确认状态，可再次编辑确认。"""
     ticket = get_ticket(ticket_id)
     if not ticket:
         return _err("工单不存在", 404)
     if ticket.get("fee_plan_status") != "confirmed":
-        return _err("只有正式费用方案可以重新打开")
-    data = request.get_json(force=True) or {}
-    reason = str(data.get("reason") or "").strip()
-    if not reason:
-        return _err("重新打开费用方案必须填写修改原因")
-    history = ticket.get("fee_plan_history") or []
-    if isinstance(history, str):
-        history = json.loads(history) if history else []
-    history.append({
-        **(ticket.get("fee_plan_snapshot") or {}),
-        "reopen_reason": reason,
-        "reopened_at": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
-    })
+        return _err("只有已确认的费用明细可以解锁")
     update_ticket(ticket_id, {
         "fee_plan_status": "draft",
-        "fee_plan_history": history,
-        "fee_confirm_note": reason,
         "reply_outdated": bool(ticket.get("reply_path")),
-        **_reopen_case_fields(),
     })
-    add_log("fee_reopen", f"重新打开费用方案: {reason}", ticket_id=ticket_id)
+    add_log("fee_unlock", "解锁费用明细，允许重新编辑确认", ticket_id=ticket_id)
     return _ok({"ticket_id": ticket_id, "fee_plan_status": "draft"})
 
 
-@app.route("/api/tickets/<ticket_id>/communications", methods=["GET"])
-def api_ticket_communications(ticket_id):
-    """列出投诉案件的多轮沟通记录。"""
-    try:
-        if not get_ticket(ticket_id):
-            return _err("工单不存在", 404)
-        return _ok(get_communication_records(ticket_id))
-    except Exception as e:
-        return _err(str(e), 500)
-
-
-@app.route("/api/tickets/<ticket_id>/communications", methods=["POST"])
-def api_ticket_communication_add(ticket_id):
-    """新增一次学员沟通记录。"""
+@app.route("/api/tickets/<ticket_id>/withdraw", methods=["PUT"])
+def api_ticket_withdraw(ticket_id):
+    """撤诉标记（v2）：任意状态可用（含已归档），写入撤诉时间与原因。"""
     try:
         ticket = get_ticket(ticket_id)
         if not ticket:
             return _err("工单不存在", 404)
-        data = request.get_json(force=True)
-        summary = data.get("summary", "").strip()
-        if not summary:
-            return _err("请填写沟通摘要")
-
-        record_id = save_communication_record(
-            ticket_id=ticket_id,
-            fee_plan_version=int(ticket.get("fee_plan_version") or 0),
-            contact_time=data.get("contact_time", "").strip(),
-            contact_method=data.get("contact_method", "").strip(),
-            summary=summary,
-            student_intention=data.get("student_intention", "").strip(),
-            next_follow_up=data.get("next_follow_up", "").strip(),
-        )
-        add_log("communication_add", f"新增沟通记录: {summary[:40]}", ticket_id=ticket_id)
-        return _ok({"id": record_id})
-    except Exception as e:
-        traceback.print_exc()
-        add_log("communication_add", f"异常: {str(e)}", success=False, ticket_id=ticket_id)
-        return _err(str(e), 500)
-
-
-@app.route("/api/tickets/<ticket_id>/communications/<int:record_id>", methods=["DELETE"])
-def api_ticket_communication_delete(ticket_id, record_id):
-    """删除沟通记录。"""
-    try:
-        if delete_communication_record(record_id, ticket_id=ticket_id):
-            add_log("communication_delete", f"删除沟通记录: {record_id}", ticket_id=ticket_id)
-            return _ok()
-        return _err("记录不存在", 404)
+        data = request.get_json(silent=True) or {}
+        reason = str(data.get("reason") or "").strip()
+        now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
+        update_ticket(ticket_id, {
+            "withdraw_status": "已撤诉",
+            "withdraw_updated_at": now,
+            "withdrawn_at": now,
+            "withdraw_reason": reason,
+        })
+        add_log("ticket_withdraw", f"学员撤诉: {reason[:40]}" if reason else "学员撤诉", ticket_id=ticket_id)
+        return _ok({
+            "ticket_id": ticket_id,
+            "withdraw_status": "已撤诉",
+            "withdrawn_at": now,
+            "withdraw_reason": reason,
+        })
     except Exception as e:
         return _err(str(e), 500)
 
@@ -1108,12 +979,13 @@ def api_ticket_withdraw_status(ticket_id):
     """更新撤诉状态（归档后仍可单独修改，不影响费用方案与协商结论）。
 
     撤诉是动态过程：学员今天没撤、明天撤了，不应强制重新打开整个案件。
+    withdrawn_at 与 withdraw_status 同步维护，保证统计口径一致。
     """
     try:
         ticket = get_ticket(ticket_id)
         if not ticket:
             return _err("工单不存在", 404)
-        data = request.get_json(force=True) or {}
+        data = request.get_json(force=True, silent=True) or {}
         status = str(data.get("withdraw_status") or "").strip()
         if status not in {"已撤诉", "未撤诉"}:
             return _err("撤诉状态只能是 已撤诉 或 未撤诉")
@@ -1121,6 +993,7 @@ def api_ticket_withdraw_status(ticket_id):
         update_ticket(ticket_id, {
             "withdraw_status": status,
             "withdraw_updated_at": now,
+            "withdrawn_at": now if status == "已撤诉" else "",
         })
         add_log("withdraw_status_update", f"撤诉状态更新为: {status}", ticket_id=ticket_id)
         return _ok({"withdraw_status": status, "withdraw_updated_at": now})
@@ -1141,7 +1014,10 @@ def api_tickets_export():
         school = request.args.get("school", "")
         date_start = request.args.get("date_start", "")
         date_end = request.args.get("date_end", "")
-        
+        # 批量导出：可选 ids 参数（逗号分隔工单 id），指定时仅导出这些工单
+        ids_raw = request.args.get("ids", "").strip()
+        selected_ids = {s.strip() for s in ids_raw.split(",") if s.strip()} if ids_raw else None
+
         # 查询工单列表（不分页，获取全部）
         tickets, _ = list_tickets(
             limit=10000,  # 最多导出1万条
@@ -1151,6 +1027,8 @@ def api_tickets_export():
             start_date=date_start,
             end_date=date_end,
         )
+        if selected_ids is not None:
+            tickets = [t for t in tickets if str(t.get("id")) in selected_ids]
         
         # 创建工作簿
         wb = Workbook()
@@ -1176,14 +1054,9 @@ def api_tickets_export():
         # 写入数据
         for row_idx, ticket in enumerate(tickets, 2):
             # 解析扣费明细
-            deductions = ticket.get("deduction_detail", "[]")
-            try:
-                if isinstance(deductions, str):
-                    deductions = json.loads(deductions)
-                deductions_str = "; ".join([f"{d.get('item', '')}:{d.get('amount', 0)}元" for d in deductions]) if deductions else ""
-            except:
-                deductions_str = str(deductions)[:100]
-            
+            deductions = parse_deductions(ticket.get("deduction_detail", "[]"))
+            deductions_str = "; ".join([f"{d.get('item', '')}:{d.get('amount', 0)}元" for d in deductions]) if deductions else ""
+
             penalty_rate = 0.2
             for deduction in deductions if isinstance(deductions, list) else []:
                 if deduction.get("item") == "违约金" and deduction.get("penalty_rate") is not None:
@@ -1254,7 +1127,9 @@ def api_auth_status():
 @app.route("/api/auth/login", methods=["POST"])
 def api_auth_login():
     try:
-        data = request.get_json(force=True)
+        data = request.get_json(force=True, silent=True)
+        if data is None:
+            return _err("请求体不是合法JSON", 400)
         background = data.get("background", True)
 
         results = query_engine.login_all(background=background)
@@ -1288,7 +1163,9 @@ def api_auth_clear():
 @app.route("/api/contract/download", methods=["POST"])
 def api_contract_download():
     try:
-        data = request.get_json(force=True)
+        data = request.get_json(force=True, silent=True)
+        if data is None:
+            return _err("请求体不是合法JSON", 400)
         id_card = data.get("id_card", "").strip()
         student_name = data.get("name", "")
         school_short = data.get("school_short", "未知")
@@ -1542,6 +1419,8 @@ def api_contract_upload():
             "manifest": manifest,
         })
 
+    except RequestEntityTooLarge:
+        return _err("上传文件超过大小限制（50MB）", 413)
     except Exception as e:
         traceback.print_exc()
         return _err(str(e), 500)
@@ -1550,7 +1429,9 @@ def api_contract_upload():
 @app.route("/api/contract/analyze", methods=["POST"])
 def api_contract_analyze():
     try:
-        data = request.get_json(force=True)
+        data = request.get_json(force=True, silent=True)
+        if data is None:
+            return _err("请求体不是合法JSON", 400)
         _filepath, validation_error = _validate_contract_analysis_request(data)
         if validation_error:
             return _err(validation_error, _contract_validation_status(validation_error))
@@ -1849,7 +1730,9 @@ def _contract_analysis_fingerprint(data: dict, filepath: str) -> str:
 
 @app.route("/api/contract/analyze/start", methods=["POST"])
 def api_contract_analyze_start():
-    data = request.get_json(force=True)
+    data = request.get_json(force=True, silent=True)
+    if data is None:
+        return _err("请求体不是合法JSON", 400)
     filepath, err = _validate_contract_analysis_request(data)
     if err:
         return _err(err, _contract_validation_status(err))
@@ -1921,19 +1804,17 @@ def api_contract_analyze_status(job_id):
 @app.route("/api/reply/generate", methods=["POST"])
 def api_reply_generate():
     try:
-        data = request.get_json(force=True)
+        data = request.get_json(force=True, silent=True)
+        if data is None:
+            return _err("请求体不是合法JSON", 400)
         ticket_id = data.get("ticket_id", "")
-        document_type = str(data.get("document_type") or "formal").strip()
-        if document_type == "progress":
-            ticket, err = _fee_plan_ticket_or_error(ticket_id, {"provisional", "confirmed"}, "进展回复")
-        else:
-            ticket, err = _confirmed_ticket_or_error(ticket_id)
+        ticket, err, status = _confirmed_ticket_or_error(ticket_id)
         if err:
             add_log("reply_generate", err, success=False, ticket_id=ticket_id)
-            return _err(err)
+            return _err(err, status)
         official = _official_case_payload(ticket, data)
 
-        # 确定回复函保存路径
+        # 确定回复函保存路径（文件命名规则保持旧版不变）
         reply_name = official.get("name", "")
         reply_id_card = official.get("id_card", "")
         reply_school = official.get("school_short", "")
@@ -1942,34 +1823,20 @@ def api_reply_generate():
         else:
             reply_dir = REPLY_DIR
 
-        result = generate_reply(
-            name=reply_name,
-            id_card=reply_id_card,
-            school_short=official.get("school_short", ""),
-            registration_date=official.get("registration_date", ""),
-            license_type=official.get("license_type", ""),
-            school_name=official.get("school_name", ""),
-            exam_stage=official.get("exam_stage", ""),
-            total_fee=float(official.get("total_fee", 0) or 0),
-            actual_paid=float(official.get("actual_paid", 0) or 0),
-            deductions=official.get("deductions", []),
-            total_deduction=float(official.get("total_deduction", 0) or 0),
-            refund=float(official.get("refund", 0) or 0),
-            contract_code=official.get("contract_code", ""),
-            training_hours=official.get("training_hours", {}),
-            complaint_summary=ticket.get("complaint_summary", ""),
-            final_outcome=ticket.get("final_outcome", ""),
-            communications=get_communication_records(ticket_id),
-            special_warnings=ticket.get("special_warnings", []),
-            output_dir=reply_dir,
-            template_id=data.get("template_id", ""),
-        )
+        # v2 回复函：扣费行直接取自请求体（amt<=0 由 reply_docx 内部跳过）
+        deductions = data.get("deductions", []) or []
+
+        today_str = datetime.now().strftime("%Y%m%d")
+        base_filename = f"{today_str}{reply_name}{reply_id_card}投诉回复函{reply_school}"
+        output_path = os.path.join(reply_dir, base_filename + ".docx")
 
+        result = generate_reply_docx(ticket=ticket, deductions=deductions, output_path=output_path)
         if result.get("success"):
-            add_log("reply_generate", f"生成回复函: {result['filename']}", ticket_id=ticket_id)
+            filepath = result["path"]
+            add_log("reply_generate", f"生成回复函(v2): {filepath}", ticket_id=ticket_id)
             if ticket_id:
                 update_ticket(ticket_id, {
-                    "reply_path": result["filepath"],
+                    "reply_path": filepath,
                     "reply_outdated": False,
                 })
             elif data.get("id_card"):
@@ -1977,12 +1844,16 @@ def api_reply_generate():
                 if existing:
                     save_complaint({
                         "id": existing["id"],
-                        "reply_path": result["filepath"],
+                        "reply_path": filepath,
                     })
+            return jsonify({
+                "success": True,
+                "filename": os.path.basename(filepath),
+                "filepath": filepath,
+            })
         else:
             add_log("reply_generate", f"生成失败: {result.get('error')}", success=False, ticket_id=ticket_id)
-
-        return jsonify(result)
+            return jsonify(result)
 
     except Exception as e:
         traceback.print_exc()
@@ -2010,6 +1881,63 @@ def api_reply_download():
         return _err(str(e))
 
 
+@app.route("/api/tickets/<ticket_id>/archive", methods=["POST"])
+def api_tickets_archive(ticket_id):
+    """D10 最终归档（权威实现）：三闸门校验 → 建夹 → 复制两件套 → 更新状态。
+
+    与 get_archive_folder（受理建夹，沿用 {日期}_{姓名}_{身份证}_{校区} 规则）分工不同：
+    本路由按 archive_service.build_archive_dir 的
+    {root}/{单位类型}/{代号-单位名}/{日期}_{姓名}_{身份证}_{代号}/ 规则拼路径，
+    仅作为结案归档入口，不负责受理阶段建夹。两者路径规则差异保留，不顺手统一。
+    """
+    try:
+        ticket = get_ticket(ticket_id)
+        if not ticket:
+            return _err("工单不存在", 404)
+
+        # 归档三闸门：处理情况 + 配合度 + 费用明细确认
+        errors = archive_gate_errors(ticket)
+        if errors:
+            return jsonify({"success": False, "errors": errors}), 400
+
+        root = load_config().get("archive_root", "案件归档")
+        case_dir, reg_target, reply_target = build_archive_dir(ticket, root=root)
+
+        # 收集已生成的源文件：登记表（受理流程写盘 registration_form_path）、
+        # 回复函（/api/reply/generate 写盘 reply_path），存在才复制
+        files = {}
+        reg_src = ticket.get("registration_form_path") or ""
+        if reg_src and os.path.isfile(reg_src):
+            files["register_form"] = reg_src
+        reply_src = ticket.get("reply_path") or ""
+        if reply_src and os.path.isfile(reply_src):
+            files["reply"] = reply_src
+
+        if not files:
+            return jsonify({"success": False, "errors": ["无可归档文件：请先生成登记表或回复函"]}), 400
+
+        result = archive_case(ticket, files, open_folder=False)
+        if not result.get("success"):
+            return jsonify(result), 400
+
+        update_ticket(ticket_id, {
+            "archive_status": "已归档",
+            "handle_status": "已完结",
+            "completed_at": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
+        })
+
+        add_log("archive", f"归档案件: {case_dir}", ticket_id=ticket_id)
+        return jsonify({
+            "success": True,
+            "dir": result["dir"],
+            "files": result["files"],
+            "opened": bool(result.get("opened")),
+        })
+    except Exception as e:
+        traceback.print_exc()
+        return _err(str(e), 500)
+
+
 @app.route("/api/templates", methods=["GET"])
 def api_templates_list():
     """列出回复模板"""
@@ -2040,11 +1968,17 @@ def api_templates_upload():
 
         template_dir = os.path.join(PROJECT_DIR, "回复模板")
         os.makedirs(template_dir, exist_ok=True)
-        safe_name = secure_filename(file.filename)
-        filepath = os.path.join(template_dir, safe_name)
-        file.save(filepath)
-
-        result = upload_template(filepath, name, description)
+        # 使用稳定的 uuid 文件名保存上传流，避免 secure_filename 剥离中文名，且不留孤儿文件
+        tmp_path = os.path.join(template_dir, f"{uuid.uuid4().hex}{ext}")
+        file.save(tmp_path)
+        try:
+            result = upload_template(tmp_path, name, description)
+        finally:
+            if os.path.exists(tmp_path):
+                try:
+                    os.remove(tmp_path)
+                except OSError:
+                    pass
         add_log("template_upload", f"上传模板: {name}")
         return _ok(result)
 
@@ -2059,60 +1993,37 @@ def api_templates_set_default(template_id):
     if set_default_template(template_id):
         add_log("template_default", f"设置默认模板: {template_id}")
         return _ok(None, "已设置为默认模板")
-    return _err("设置失败")
+    return _err("模板不存在", 404)
 
 
 @app.route("/api/templates/<template_id>", methods=["DELETE"])
 def api_templates_delete(template_id):
-    """删除模板"""
+    """删除模板（同时清理磁盘文件；删除最后一个默认模板时给出警告）"""
+    from database import get_db, get_default_template
+    tmpl_path = None
+    was_default = False
+    with get_db() as conn:
+        row = conn.execute(
+            "SELECT template_path, is_default FROM reply_templates WHERE id=?",
+            (template_id,),
+        ).fetchone()
+        if row:
+            tmpl_path = row["template_path"]
+            was_default = bool(row["is_default"])
     if delete_template(template_id):
+        if tmpl_path and os.path.exists(tmpl_path):
+            try:
+                os.remove(tmpl_path)
+            except OSError:
+                pass
         add_log("template_delete", f"删除模板: {template_id}")
+        if was_default and not get_default_template():
+            return _ok(
+                {"warning": "已删除最后一个默认模板，当前无默认模板，生成回复函时请先指定默认模板"},
+                "已删除",
+            )
         return _ok(None, "已删除")
-    return _err("删除失败")
-
-
-# ═══════════════════════════════════════════════════════════════
-#  API: 飞书登记
-# ═══════════════════════════════════════════════════════════════
-
-@app.route("/api/feishu/submit", methods=["POST"])
-def api_feishu_submit():
-    try:
-        data = request.get_json(force=True)
-        ticket_id = data.get("ticket_id", "")
-        ticket, err = _confirmed_ticket_or_error(ticket_id)
-        if err:
-            add_log("feishu_submit", err, success=False, ticket_id=ticket_id)
-            return _err(err)
-        official = _official_case_payload(ticket, data)
-        svc = FeishuService()
-        result = svc.add_record(official)
-
-        if result.get("success"):
-            add_log("feishu_submit", f"提交飞书: {result.get('handle_no', '')}", ticket_id=ticket_id)
-            if ticket_id:
-                update_ticket(ticket_id, {
-                    "feishu_record_id": result.get("record_id", ""),
-                    "feishu_handle_no": result.get("handle_no", ""),
-                })
-        else:
-            add_log("feishu_submit", f"提交失败: {result.get('error')}", success=False, ticket_id=ticket_id)
-
-        return jsonify(result)
-
-    except Exception as e:
-        traceback.print_exc()
-        add_log("feishu_submit", f"异常: {str(e)}", success=False)
-        return _err(str(e), 500)
-
-
-@app.route("/api/feishu/test")
-def api_feishu_test():
-    try:
-        svc = FeishuService()
-        return jsonify(svc.test_connection())
-    except Exception as e:
-        return _err(str(e))
+    return _err("模板不存在", 404)
 
 
 # ═══════════════════════════════════════════════════════════════
@@ -2128,18 +2039,16 @@ def api_tickets_register_form(ticket_id):
             return _err("工单不存在", 404)
 
         from services.visit_service import generate_registration_form
-        from database import get_communication_records
-        communications = get_communication_records(ticket_id)
         result = generate_registration_form(
             ticket,
-            communications=communications,
+            communications=[],
             final_outcome=ticket.get("final_outcome", ""),
             negotiation_outcome=ticket.get("negotiation_outcome", ""),
             withdraw_status=ticket.get("withdraw_status", ""),
             branch_cooperation=ticket.get("branch_cooperation", ""),
             total_fee=float(ticket.get("total_fee", 0) or 0),
             refund=float(ticket.get("refund_fee", 0) or 0),
-            deductions=ticket.get("deduction_detail", []) if isinstance(ticket.get("deduction_detail"), list) else json.loads(ticket.get("deduction_detail", "[]")),
+            deductions=parse_deductions(ticket.get("deduction_detail")),
             special_warnings=json.loads(ticket.get("special_warnings", "[]")) if isinstance(ticket.get("special_warnings"), str) else ticket.get("special_warnings", []),
             exam_stage=ticket.get("exam_stage", ""),
         )
@@ -2160,20 +2069,15 @@ def api_tickets_register_form(ticket_id):
 
 @app.route("/api/tickets/<ticket_id>/detail")
 def api_ticket_detail(ticket_id):
-    from database import get_ticket, get_communication_records
+    from database import get_ticket
     ticket = get_ticket(ticket_id)
     if not ticket:
-        return jsonify({"success": False, "error": "工单不存在"})
+        return _err("工单不存在", 404)
 
     # Parse deductions from JSON string
-    deductions = []
-    if ticket.get("deduction_detail"):
-        try:
-            deductions = json.loads(ticket["deduction_detail"]) if isinstance(ticket["deduction_detail"], str) else ticket["deduction_detail"]
-        except:
-            pass
+    deductions = parse_deductions(ticket.get("deduction_detail"))
 
-    communication_records = get_communication_records(ticket_id)
+    communication_records = []
 
     # Build document list
     documents = []
@@ -2198,7 +2102,7 @@ def api_get_analysis(ticket_id):
     from database import get_ticket
     ticket = get_ticket(ticket_id)
     if not ticket:
-        return jsonify({"success": True, "cached": False})
+        return _err("工单不存在", 404)
 
     # Check current ticket
     if ticket.get("deduction_detail") and ticket["deduction_detail"] != "[]":
@@ -2241,17 +2145,55 @@ def api_get_analysis(ticket_id):
 
 @app.route("/api/contract/save_analysis", methods=["POST"])
 def api_save_analysis():
-    data = request.get_json(force=True)
+    data = request.get_json(force=True, silent=True)
+    if data is None:
+        return _err("请求体不是合法JSON", 400)
     ticket_id = data.get("ticket_id")
     analysis_data = data.get("analysis_data", {})
     if not ticket_id:
-        return jsonify({"success": False, "error": "缺少工单ID"})
+        return _err("缺少工单ID", 400)
+
+    if not isinstance(analysis_data, dict) or not analysis_data:
+        return _err("缺少有效的分析结果数据", 400)
 
     ticket = get_ticket(ticket_id)
     if not ticket:
         return _err("工单不存在", 404)
     if ticket.get("fee_plan_status") == "confirmed" or ticket.get("archive_status") == "已归档":
-        return _err("正式费用方案已锁定；请通过重新打开费用方案并填写修改原因来调整")
+        return _err("费用明细已确认锁定；请先调用费用解锁（fee-unlock）再调整")
+
+    def _positive_number(val, name):
+        try:
+            num = float(val)
+        except (TypeError, ValueError):
+            return None, f"{name}必须是数值"
+        if num < 0:
+            return None, f"{name}不能为负数"
+        return num, None
+
+    for field, label in (("total_fee", "total_fee"), ("actual_paid", "actual_paid"),
+                         ("total_deduction", "total_deduction"), ("refund", "refund")):
+        val, err = _positive_number(analysis_data.get(field, 0), label)
+        if err:
+            return _err(err, 400)
+
+    deduction_detail = analysis_data.get("deduction_detail")
+    if deduction_detail is not None:
+        details = deduction_detail if isinstance(deduction_detail, list) else []
+        if isinstance(deduction_detail, str):
+            try:
+                details = json.loads(deduction_detail)
+            except json.JSONDecodeError:
+                details = []
+        if isinstance(details, list):
+            for _d in details:
+                if isinstance(_d, dict):
+                    try:
+                        _amt = float(_d.get("amount", 0))
+                    except (TypeError, ValueError):
+                        return _err("扣费明细金额必须为数值", 400)
+                    if _amt < 0:
+                        return _err("扣费明细金额不能为负数", 400)
 
     from database import update_ticket
     update_ticket(ticket_id, {
@@ -2284,9 +2226,6 @@ def api_config_get():
         if safe.get(section, {}).get("api_key"):
             key = safe[section]["api_key"]
             safe[section]["api_key"] = key[:8] + "••••" + key[-4:]
-    if safe.get("feishu", {}).get("app_secret"):
-        s = safe["feishu"]["app_secret"]
-        safe["feishu"]["app_secret"] = s[:8] + "••••"
 
     return jsonify(safe)
 
@@ -2294,7 +2233,9 @@ def api_config_get():
 @app.route("/api/config", methods=["PUT", "POST"])
 def api_config_put():
     try:
-        new_config = request.get_json(force=True)
+        new_config = request.get_json(force=True, silent=True)
+        if new_config is None:
+            return _err("请求体不是合法JSON", 400)
         current = load_config()
 
         for section in ("internal_system", "third_system", "driving_system"):
@@ -2313,11 +2254,6 @@ def api_config_put():
                 llm_cfg["api_key"] = current_key
             new_config[section] = llm_cfg
 
-        if new_config.get("feishu", {}).get("app_secret", ""):
-            secret = new_config["feishu"]["app_secret"]
-            if "•" in secret:
-                new_config["feishu"]["app_secret"] = current.get("feishu", {}).get("app_secret", "")
-
         save_config(new_config)
 
         # 重新初始化爬虫实例（不改动 query_engine 全局引用）
@@ -2343,8 +2279,14 @@ def api_complaints_list():
     """获取投诉记录列表（兼容旧接口）"""
     status = request.args.get("status", "")
     search = request.args.get("search", "")
-    limit = int(request.args.get("limit", 50))
-    offset = int(request.args.get("offset", 0))
+    try:
+        limit = int(request.args.get("limit", 50))
+    except (TypeError, ValueError):
+        limit = 50
+    try:
+        offset = int(request.args.get("offset", 0))
+    except (TypeError, ValueError):
+        offset = 0
     # 深分页保护：与 /api/tickets 一致
     limit = min(max(limit, 1), 200)
     offset = min(max(offset, 0), 100000)
@@ -2384,11 +2326,34 @@ def api_org_vehicle_counts():
 @app.route("/api/org-vehicle-counts", methods=["PUT"])
 def api_org_vehicle_counts_save():
     """批量保存网点车辆数配置"""
-    data = request.get_json(force=True) or {}
+    data = request.get_json(force=True, silent=True)
+    if data is None:
+        return _err("请求体不是合法JSON", 400)
     items = data.get("items", [])
     if not isinstance(items, list):
-        return _err("items 必须是数组")
-    saved = save_org_vehicle_counts(items)
+        return _err("items 必须是数组", 400)
+    if not items:
+        return _err("items 不能为空：本接口为全量替换，空提交会清空全部网点车辆数配置", 400)
+    validated = []
+    for idx, item in enumerate(items):
+        if not isinstance(item, dict):
+            return _err(f"第 {idx + 1} 条车辆数配置格式错误", 400)
+        code = str(item.get("unit_code") or "").strip()
+        if not code:
+            return _err(f"第 {idx + 1} 条车辆数配置缺少 unit_code", 400)
+        raw_count = item.get("vehicle_count")
+        if isinstance(raw_count, bool) or not isinstance(raw_count, int):
+            return _err(f"第 {idx + 1} 条车辆数（{raw_count}）必须是非负整数", 400)
+        count = raw_count
+        if count < 0:
+            return _err(f"第 {idx + 1} 条车辆数不能为负数", 400)
+        validated.append({
+            "unit_code": code,
+            "unit_name": item.get("unit_name", ""),
+            "unit_type": item.get("unit_type", ""),
+            "vehicle_count": count,
+        })
+    saved = save_org_vehicle_counts(validated)
     return _ok({"saved": saved})
 
 
@@ -2405,13 +2370,6 @@ def api_duration_stats():
         return jsonify({"success": False, "error": str(e)})
 
 
-@app.route("/api/logs/recent")
-def api_recent_logs():
-    limit = int(request.args.get("limit", 20))
-    logs = get_recent_logs(limit=limit)
-    return jsonify({"success": True, "logs": logs})
-
-
 # ═══════════════════════════════════════════════════════════════
 #  API: 文件下载
 # ═══════════════════════════════════════════════════════════════
@@ -2433,7 +2391,7 @@ def api_contract_download_file():
 def api_contract_preview():
     filepath = request.args.get("path", "")
     if not filepath:
-        return jsonify({"success": False, "error": "缺少文件路径"})
+        return _err("缺少文件路径", 400)
 
     from config import load_config
     cfg = load_config()
@@ -2475,6 +2433,7 @@ except Exception as e:
 # ═══════════════════════════════════════════════════════════════
 
 if __name__ == "__main__":
+    _start_background_services()
     print("=" * 50)
     print("  驾校投诉处理系统")
     print(f"  访问地址: http://127.0.0.1:5003")
diff --git a/database.py b/database.py
index 5a76c06..b4166ef 100644
--- a/database.py
+++ b/database.py
@@ -95,8 +95,6 @@ def init_db():
                 -- 产出物
                 reply_path TEXT DEFAULT '',
                 reply_outdated INTEGER DEFAULT 0,
-                feishu_record_id TEXT DEFAULT '',
-                feishu_handle_no TEXT DEFAULT '',
 
                 -- 学员信息缓存（三系统查询结果中常用的字段）
                 license_type TEXT DEFAULT '',
@@ -125,6 +123,9 @@ def init_db():
                 branch_cooperation TEXT DEFAULT '',
                 branch_cooperation_note TEXT DEFAULT '',
                 archive_status TEXT DEFAULT '',
+                handling_notes TEXT DEFAULT '',
+                withdrawn_at TEXT DEFAULT '',
+                withdraw_reason TEXT DEFAULT '',
                 cancellation_date TEXT DEFAULT '',
                 cancellation_source TEXT DEFAULT '',
                 cancellation_note TEXT DEFAULT '',
@@ -156,20 +157,7 @@ def init_db():
             CREATE INDEX IF NOT EXISTS idx_contract_analysis_jobs_active
                 ON contract_analysis_jobs(fingerprint, status);
 
-            -- 沟通记录表（多轮联系学员，取代旧版回访记录表）
-            CREATE TABLE IF NOT EXISTS communication_records (
-                id INTEGER PRIMARY KEY AUTOINCREMENT,
-                ticket_id TEXT NOT NULL,
-                fee_plan_version INTEGER NOT NULL DEFAULT 0,
-                contact_time TEXT NOT NULL DEFAULT '',
-                contact_method TEXT NOT NULL DEFAULT '',
-                summary TEXT NOT NULL DEFAULT '',
-                student_intention TEXT NOT NULL DEFAULT '',
-                next_follow_up TEXT DEFAULT '',
-                created_at TEXT NOT NULL DEFAULT '',
-                FOREIGN KEY (ticket_id) REFERENCES complaint_tickets(id)
-            );
-            CREATE INDEX IF NOT EXISTS idx_communication_records_ticket ON communication_records(ticket_id);
+            -- 沟通记录表已废弃（Wave2 迁移至 handling_notes 后由迁移函数 DROP）
 
             -- 网点车辆数配置表（投诉率分母，人工维护）
             CREATE TABLE IF NOT EXISTS org_vehicle_counts (
@@ -202,6 +190,12 @@ def init_db():
                 created_at TEXT DEFAULT ''
             );
 
+            -- 数据迁移标记表（避免一次性迁移重复执行）
+            CREATE TABLE IF NOT EXISTS migration_flags (
+                key TEXT PRIMARY KEY,
+                value TEXT NOT NULL DEFAULT ''
+            );
+
             -- 兼容旧版 complaints_v1 表（用于 get_complaint_by_idcard）
             CREATE TABLE IF NOT EXISTS complaints_v1 (
                 id TEXT PRIMARY KEY,
@@ -264,6 +258,9 @@ def init_db():
             ("branch_cooperation", "TEXT DEFAULT ''"),
             ("branch_cooperation_note", "TEXT DEFAULT ''"),
             ("archive_status", "TEXT DEFAULT ''"),
+            ("handling_notes", "TEXT DEFAULT ''"),
+            ("withdrawn_at", "TEXT DEFAULT ''"),
+            ("withdraw_reason", "TEXT DEFAULT ''"),
             ("organization_unit_id", "TEXT DEFAULT ''"),
             ("organization_unit_type", "TEXT DEFAULT ''"),
             ("organization_unit_name", "TEXT DEFAULT ''"),
@@ -285,14 +282,6 @@ def init_db():
                 except Exception as e:
                     system_logger.error("[数据库] 迁移 %s 失败: %s", col_name, e)
 
-        communication_columns = [
-            row[1] for row in conn.execute("PRAGMA table_info(communication_records)").fetchall()
-        ]
-        if "fee_plan_version" not in communication_columns:
-            conn.execute(
-                "ALTER TABLE communication_records ADD COLUMN fee_plan_version INTEGER NOT NULL DEFAULT 0"
-            )
-
         # ── 网点车辆数表：增加 unit_type 列（空表时用经营单位字典初始化）──
         vc_columns = [row[1] for row in conn.execute("PRAGMA table_info(org_vehicle_counts)").fetchall()]
         if "unit_type" not in vc_columns:
@@ -358,7 +347,7 @@ def save_ticket(data: dict) -> str:
         "contract_path", "contract_code", "total_fee", "actual_paid",
         "deduction_fee", "refund_fee", "deduction_detail", "contract_set", "contract_manifest",
         "handle_status", "handle_steps", "attachments",
-        "reply_path", "reply_outdated", "feishu_record_id", "feishu_handle_no",
+        "reply_path", "reply_outdated",
         "license_type", "school_name", "school_short",
         "organization_unit_id", "organization_unit_type",
         "organization_unit_name", "organization_unit_code",
@@ -372,9 +361,11 @@ def save_ticket(data: dict) -> str:
         "fee_confirmed_by", "fee_confirmed_at", "fee_confirm_note",
         "branch_cooperation",
         "branch_cooperation_note", "archive_status",
+        "handling_notes",
         "cancellation_date", "cancellation_source",
         "cancellation_note",
         "withdraw_status", "withdraw_updated_at",
+        "withdrawn_at", "withdraw_reason",
         "negotiation_outcome",
     }
 
@@ -535,10 +526,14 @@ def list_tickets(
     search: str = "",
     start_date: str = "",
     end_date: str = "",
+    repeat_only: bool = False,
     limit: int = 10,
     offset: int = 0,
 ) -> tuple[list[dict], int]:
-    """查询投诉工单列表，返回 (records, total)"""
+    """查询投诉工单列表，返回 (records, total)
+
+    repeat_only: 仅保留同一身份证出现多次的重复投诉工单
+    """
     conditions = []
     params = []
 
@@ -549,18 +544,28 @@ def list_tickets(
         conditions.append("source_channel=?")
         params.append(source)
     if school:
-        conditions.append("school_short LIKE ?")
-        params.append(f"%{school}%")
+        # 下钻网点时同时匹配分店自身代号与所属分校代号
+        like = f"%{school}%"
+        conditions.append("(school_short LIKE ? OR organization_unit_code LIKE ?)")
+        params.extend([like, like])
     if search:
-        conditions.append("(student_name LIKE ? OR id_card LIKE ? OR ticket_no LIKE ?)")
+        # 合一模糊搜索：学员姓名 / 身份证 / 工单号 / 网点代号
         like = f"%{search}%"
-        params.extend([like, like, like])
+        conditions.append("(student_name LIKE ? OR id_card LIKE ? OR ticket_no LIKE ? OR school_short LIKE ?)")
+        params.extend([like, like, like, like])
     if start_date:
         conditions.append("complaint_date>=?")
         params.append(start_date)
     if end_date:
         conditions.append("complaint_date<=?")
         params.append(end_date)
+    if repeat_only:
+        inner = " AND ".join(conditions) if conditions else "1=1"
+        conditions.append(
+            "id_card IN (SELECT id_card FROM complaint_tickets "
+            f"WHERE {inner} AND id_card != '' GROUP BY id_card HAVING COUNT(*)>1)"
+        )
+        params = params + list(params)
 
     where = f"WHERE {' AND '.join(conditions)}" if conditions else ""
 
@@ -607,6 +612,8 @@ def save_template(data: dict) -> str:
         data["variables"] = json.dumps(data["variables"], ensure_ascii=False)
 
     with get_db() as conn:
+        if data.get("is_default"):
+            conn.execute("UPDATE reply_templates SET is_default=0")
         existing = conn.execute("SELECT id FROM reply_templates WHERE id=?", (template_id,)).fetchone()
         if existing:
             fields = []
@@ -621,8 +628,6 @@ def save_template(data: dict) -> str:
             values.append(template_id)
             conn.execute(f"UPDATE reply_templates SET {','.join(fields)} WHERE id=?", values)
         else:
-            if data.get("is_default"):
-                conn.execute("UPDATE reply_templates SET is_default=0")
             data["id"] = template_id
             data["created_at"] = now
             data["updated_at"] = now
@@ -659,8 +664,8 @@ def get_default_template() -> dict | None:
 def delete_template(template_id: str) -> bool:
     """删除回复模板"""
     with get_db() as conn:
-        conn.execute("DELETE FROM reply_templates WHERE id=?", (template_id,))
-        return conn.total_changes > 0
+        cur = conn.execute("DELETE FROM reply_templates WHERE id=?", (template_id,))
+        return cur.rowcount > 0
 
 
 # ═══════════════════════════════════════════════════
@@ -787,30 +792,33 @@ def get_ticket_statistics(
 
         where_clause = ("WHERE " + " AND ".join(conditions)) if conditions else "WHERE 1=1"
 
-        # 一条 SQL 聚合：总数 + 各状态 + 有效投诉量 + 撤销量
+        # 一条 SQL 聚合：总数 + 各状态 + 有效投诉量（未撤诉）+ 撤诉量
         agg_row = conn.execute(f"""
             SELECT
                 COUNT(*) AS total,
                 SUM(CASE WHEN handle_status='待处理' THEN 1 ELSE 0 END) AS pending,
                 SUM(CASE WHEN handle_status='处理中' THEN 1 ELSE 0 END) AS processing,
                 SUM(CASE WHEN handle_status='已完结' OR archive_status='已归档' THEN 1 ELSE 0 END) AS completed,
-                SUM(CASE WHEN final_outcome='投诉撤销' THEN 1 ELSE 0 END) AS cancelled_total,
-                SUM(CASE WHEN final_outcome!='投诉撤销' OR final_outcome='' THEN 1 ELSE 0 END) AS effective_total
+                SUM(CASE WHEN TRIM(COALESCE(withdrawn_at,''))!='' OR withdraw_status='已撤诉'
+                         THEN 1 ELSE 0 END) AS withdrawn_total,
+                SUM(CASE WHEN TRIM(COALESCE(withdrawn_at,''))='' AND COALESCE(withdraw_status,'')!='已撤诉'
+                         THEN 1 ELSE 0 END) AS effective_total
             FROM complaint_tickets {where_clause}
         """, params).fetchone()
 
         integrity_row = conn.execute(f"""
             SELECT
-                SUM(CASE WHEN archive_status='已归档' AND handle_status!='已完结' THEN 1 ELSE 0 END) AS archived_not_completed,
-                SUM(CASE WHEN handle_status='已完结' AND TRIM(final_outcome)='' THEN 1 ELSE 0 END) AS completed_without_final_outcome
+                SUM(CASE WHEN archive_status='已归档' AND handle_status!='已完结' THEN 1 ELSE 0 END) AS archived_not_completed
             FROM complaint_tickets {where_clause}
         """, params).fetchone()
 
-        # 一条 SQL 聚合：经营单位 / 渠道 / 类型 / 结果 / 配合度分组
+        # 一条 SQL 聚合：经营单位 / 渠道 / 类型 / 结果 / 配合度分组（撤诉量单独累计）
         group_rows = conn.execute(f"""
             SELECT school_short, organization_unit_name, organization_unit_code, organization_unit_type,
                    source_channel, complaint_type, final_outcome, branch_cooperation,
-                   COUNT(*) as cnt
+                   COUNT(*) as cnt,
+                   SUM(CASE WHEN TRIM(COALESCE(withdrawn_at,''))!='' OR withdraw_status='已撤诉'
+                            THEN 1 ELSE 0 END) as withdrawn_cnt
             FROM complaint_tickets {where_clause}
             GROUP BY school_short, organization_unit_name, organization_unit_code, organization_unit_type,
                      source_channel, complaint_type, final_outcome, branch_cooperation
@@ -862,9 +870,15 @@ def get_ticket_statistics(
         processing = agg_row["processing"] or 0
         completed = agg_row["completed"] or 0
         effective_total = agg_row["effective_total"] or 0
-        cancelled_total = agg_row["cancelled_total"] or 0
+        withdrawn_total = agg_row["withdrawn_total"] or 0
         archived_not_completed = integrity_row["archived_not_completed"] or 0
-        completed_without_final_outcome = integrity_row["completed_without_final_outcome"] or 0
+
+        # 按 complaint_type 分组计数（撤诉案件不计入有效，但单独列出撤诉量）
+        type_group_rows = conn.execute(f"""
+            SELECT complaint_type, COUNT(*) as cnt
+            FROM complaint_tickets {where_clause}
+            GROUP BY complaint_type
+        """, params).fetchall()
 
         # 解析分组结果
         type_map = {"A": "退费纠纷", "B": "教学服务", "C": "考试安排", "D": "合同争议", "E": "其他"}
@@ -888,10 +902,9 @@ def get_ticket_statistics(
                 "cancelled_count": 0,
             })
             school_counts[key]["total_count"] += row["cnt"]
-            if row["final_outcome"] == "投诉撤销":
-                school_counts[key]["cancelled_count"] += row["cnt"]
-            else:
-                school_counts[key]["count"] += row["cnt"]
+            withdrawn_cnt = row["withdrawn_cnt"] or 0
+            school_counts[key]["cancelled_count"] += withdrawn_cnt
+            school_counts[key]["count"] += row["cnt"] - withdrawn_cnt
             src = row["source_channel"] or "未知"
             source_counts[src] = source_counts.get(src, 0) + row["cnt"]
             t = type_map.get(row["complaint_type"], row["complaint_type"] or "未知")
@@ -917,6 +930,14 @@ def get_ticket_statistics(
 
         by_source = sorted([{"source": k, "count": v} for k, v in source_counts.items()], key=lambda x: x["count"], reverse=True)
         by_type = sorted([{"type": k, "count": v} for k, v in type_counts.items()], key=lambda x: x["count"], reverse=True)
+        by_complaint_type = sorted(
+            [
+                {"complaint_type": row["complaint_type"] or "未知", "count": row["cnt"] or 0}
+                for row in type_group_rows
+            ],
+            key=lambda x: x["count"],
+            reverse=True,
+        )
         by_outcome = sorted([{"outcome": k, "count": v} for k, v in outcome_counts.items()], key=lambda x: x["count"], reverse=True)
         by_branch_cooperation = sorted(
             [{"cooperation": k, "count": v} for k, v in cooperation_counts.items()],
@@ -931,17 +952,18 @@ def get_ticket_statistics(
             "processing": processing,
             "completed": completed,
             "effective_total": effective_total,
-            "cancelled_total": cancelled_total,
+            "withdrawn_total": withdrawn_total,
+            "cancelled_total": withdrawn_total,
             "integrity_issues": {
                 "archived_not_completed": archived_not_completed,
-                "completed_without_final_outcome": completed_without_final_outcome,
-                "total": archived_not_completed + completed_without_final_outcome,
+                "total": archived_not_completed,
             },
             "refund_sum": 0,
             "by_school": by_school,
             "by_rate": by_rate,
             "by_source": by_source,
             "by_type": by_type,
+            "by_complaint_type": by_complaint_type,
             "by_outcome": by_outcome,
             "by_branch_cooperation": by_branch_cooperation,
             "monthly_trend": [
@@ -1116,7 +1138,6 @@ def save_complaint(data: dict) -> str:
         "contract_code": data.get("contract_code", ""),
         "contract_path": data.get("contract_path", ""),
         "reply_path": data.get("reply_path", ""),
-        "feishu_record_id": data.get("feishu_record_id", ""),
         "handle_status": data.get("handle_status", "待处理"),
         "license_type": data.get("license_type", ""),
         "school_name": data.get("school_name", ""),
@@ -1301,5 +1322,64 @@ def delete_communication_record(record_id: int, ticket_id: str = "") -> bool:
         return cursor.rowcount > 0
 
 
+# ═══════════════════════════════════════════════════
+#  v2 迁移：多轮沟通记录 → handling_notes
+# ═══════════════════════════════════════════════════
+
+def migrate_communications_to_notes() -> int:
+    """把每个工单的存量沟通记录摘要拼接为 handling_notes 初始文本（幂等）。
+
+    格式：每条一行「[日期] 摘要；」。迁完即删除源记录并打标记，
+    再次执行时无源记录可迁，直接返回 0，不会重复追加。
+    """
+    with get_db() as conn:
+        marked = conn.execute(
+            "SELECT value FROM migration_flags WHERE key='communications_to_notes'"
+        ).fetchone()
+        if marked and marked["value"] == "done":
+            conn.execute("DROP TABLE IF EXISTS communication_records")
+            return 0
+        has_table = conn.execute(
+            "SELECT name FROM sqlite_master WHERE type='table' AND name='communication_records'"
+        ).fetchone()
+        rows = (
+            conn.execute(
+                "SELECT ticket_id, contact_time, created_at, summary"
+                " FROM communication_records ORDER BY ticket_id, contact_time ASC, id ASC"
+            ).fetchall()
+            if has_table
+            else []
+        )
+        grouped: dict[str, list[str]] = {}
+        for r in rows:
+            stamp = (r["contact_time"] or r["created_at"] or "")[:10]
+            summary = (r["summary"] or "").strip()
+            if not summary:
+                continue
+            grouped.setdefault(r["ticket_id"], []).append(f"[{stamp}] {summary}；")
+        now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
+        migrated = 0
+        for ticket_id, lines in grouped.items():
+            row = conn.execute(
+                "SELECT handling_notes FROM complaint_tickets WHERE id=?", (ticket_id,)
+            ).fetchone()
+            if not row:
+                continue
+            existing = (row["handling_notes"] or "").strip()
+            text = "\n".join(lines)
+            new_notes = text if not existing else existing + "\n" + text
+            conn.execute(
+                "UPDATE complaint_tickets SET handling_notes=?, updated_at=? WHERE id=?",
+                (new_notes, now, ticket_id),
+            )
+            migrated += 1
+        conn.execute("DROP TABLE IF EXISTS communication_records")
+        conn.execute(
+            "INSERT OR REPLACE INTO migration_flags (key, value) VALUES ('communications_to_notes', 'done')"
+        )
+    system_logger.info("[数据库] 沟通记录已迁移至 handling_notes，源表已删除：%d 个工单", migrated)
+    return migrated
+
+
 # 模块加载时初始化
 init_db()
diff --git a/services/template_service.py b/services/template_service.py
index 7129671..716ab94 100644
--- a/services/template_service.py
+++ b/services/template_service.py
@@ -198,7 +198,12 @@ def get_available_templates() -> list[dict]:
 
 
 def set_default_template(template_id: str) -> bool:
-    """设置默认模板"""
+    """设置默认模板（目标不存在时拒绝，避免产生幽灵默认行）"""
+    from database import get_db
+    with get_db() as conn:
+        exists = conn.execute("SELECT id FROM reply_templates WHERE id=?", (template_id,)).fetchone()
+    if not exists:
+        return False
     return bool(save_template({"id": template_id, "is_default": 1}))
 
 
@@ -236,15 +241,38 @@ def create_default_template() -> str:
     """
     创建默认 .docx 模板并存入数据库。
     返回模板文件路径。
+    判重：已存在任一默认模板或同名「默认模板」记录时不再插入新行，
+    仅在文件缺失时按既有记录的路径重建文件并复用。
     """
     template_dir = _get_template_dir()
     filepath = os.path.join(template_dir, "default_template.docx")
 
-    # 检查数据库中是否已有默认模板
     default_tmpl = get_default_template()
-    if default_tmpl and os.path.exists(default_tmpl.get("template_path", "")):
-        return default_tmpl["template_path"]
+    existing = default_tmpl or next(
+        (t for t in list_templates() if t.get("name") == "默认模板"), None
+    )
+    if existing:
+        existing_path = existing.get("template_path", "") or filepath
+        if not os.path.exists(existing_path):
+            _write_default_reply_doc(existing_path)
+        return existing_path
 
+    _write_default_reply_doc(filepath)
+
+    # 注册到数据库
+    save_template({
+        "name": "默认模板",
+        "description": "默认回复函模板，含 {{name}}, {{id_card}} 等变量",
+        "template_path": filepath,
+        "variables": json.dumps(list(SUPPORTED_VARIABLES.keys()), ensure_ascii=False),
+        "is_default": 1,
+    })
+
+    return filepath
+
+
+def _write_default_reply_doc(filepath: str):
+    """生成默认回复函 .docx 文件并写入 filepath。"""
     doc = Document()
 
     # 设置默认字体
@@ -309,17 +337,6 @@ def create_default_template() -> str:
 
     doc.save(filepath)
 
-    # 注册到数据库
-    save_template({
-        "name": "默认模板",
-        "description": "默认回复函模板，含 {{name}}, {{id_card}} 等变量",
-        "template_path": filepath,
-        "variables": json.dumps(list(SUPPORTED_VARIABLES.keys()), ensure_ascii=False),
-        "is_default": 1,
-    })
-
-    return filepath
-
 
 def _add_t_para(doc, text: str):
     """添加左对齐段落（首行缩进2字符）"""
diff --git a/static/css/style.css b/static/css/style.css
index c2ef8b5..8273e18 100644
--- a/static/css/style.css
+++ b/static/css/style.css
@@ -233,12 +233,13 @@ body {
   background: #047857;
 }
 
-.btn-danger {
+/* 双类名提升优先级：覆盖 index.html 内联<style>中后加载的 .btn 基础规则 */
+.btn.btn-danger {
   background: var(--danger);
   color: #fff;
 }
 
-.btn-danger:hover:not(:disabled) {
+.btn.btn-danger:hover:not(:disabled) {
   background: #b91c1c;
 }
 
@@ -405,7 +406,7 @@ textarea.form-control { resize: vertical; min-height: 60px; }
   height: 100%;
   background: linear-gradient(90deg, var(--primary), #3b82f6, #60a5fa);
   border-radius: 999px;
-  transition: width .4s ease;
+  transition: width .3s ease;
 }
 
 .progress-status { display: flex; gap: 10px; flex-wrap: wrap; }
diff --git a/static/js/composables/useComplaint.js b/static/js/composables/useComplaint.js
index fc0f932..b033a88 100644
--- a/static/js/composables/useComplaint.js
+++ b/static/js/composables/useComplaint.js
@@ -19,7 +19,6 @@ export function useComplaint(onAutoQueryDone = null) {
     complaint_date: todayStr(),
     complaint_type: "A",
     handler_name: "",
-    complaint_summary: "",
   });
 
   // 受理文件上传
@@ -28,6 +27,10 @@ export function useComplaint(onAutoQueryDone = null) {
   const intakeResult = Vue.ref(null);
   const intakeErr = Vue.ref("");
 
+  // 自动回填后的输入框高亮（脉冲动画）
+  const pulseIdCard = Vue.ref(false);
+  const pulsePhone = Vue.ref(false);
+
   // 三系统查询
   const querying = Vue.ref(false);
   const qErr = Vue.ref("");
@@ -52,6 +55,10 @@ export function useComplaint(onAutoQueryDone = null) {
   // 当前工单ID
   const currentTicketId = Vue.ref("");
 
+  // 绑定工单的原始证件号/手机号（用于提交前判断是否换人，防止覆盖他人工单）
+  const boundIdCard = Vue.ref("");
+  const boundPhone = Vue.ref("");
+
   function resetQueryProgress() {
     queryProgress.show = true;
     queryProgress.percent = 0;
@@ -239,8 +246,9 @@ export function useComplaint(onAutoQueryDone = null) {
       if (d.success && d.data) {
         intakeResult.value = d.data;
         // 自动填充表单
-        if (d.data.id_card) form.id_card = d.data.id_card;
-        if (d.data.phone) form.phone = d.data.phone;
+        if (d.data.id_card) { form.id_card = d.data.id_card; pulseIdCard.value = true; }
+        if (d.data.phone) { form.phone = d.data.phone; pulsePhone.value = true; }
+        setTimeout(() => { pulseIdCard.value = false; pulsePhone.value = false; }, 2200);
 
         // 提取到身份证号或手机号后，自动触发三系统查询（手机号场景不再需手动点）
         const phoneDigits = (d.data.phone || "").replace(/\D/g, "");
@@ -318,7 +326,7 @@ export function useComplaint(onAutoQueryDone = null) {
   function validateIdCard(id) {
     /**
      * 只验证18位大陆身份证（GB 11643-1999 校验码算法）
-     * 其他证件（居留证、港澳台等）不验证，直接放行交给系统判断
+     * 其他证件仅放行已知格式白名单（居留证、外国人永久居留证等），与服务端口径一致
      */
     id = (id || "").trim().toUpperCase();
     if (!id) return { valid: false, msg: "证件号不能为空" };
@@ -338,12 +346,13 @@ export function useComplaint(onAutoQueryDone = null) {
       return { valid: true };
     }
     
-    // 其他所有证件 → 不验格式，直接放行
-    if (id.length >= 7) {
+    // 非18位：与服务端白名单口径一致（前缀≤3大写字母 + 6~17位数字 + 可选括号尾注，
+    // 兼容居留证 F1249468(8)、外国人永久居留证），拦截明显乱码
+    if (/^[A-Z]{0,3}\d{6,17}(?:[（(]\d{1,4}[)）])?$/.test(id)) {
       return { valid: true };
     }
-    
-    return { valid: false, msg: "证件号至少7位" };
+
+    return { valid: false, msg: "证件号格式不正确" };
   }
 
   async function queryAll() {
@@ -388,6 +397,20 @@ export function useComplaint(onAutoQueryDone = null) {
     qr.value = null;
     resetQueryProgress();
 
+    // P1-1：若当前绑定了旧工单，但本次查询的学员与绑定工单原证件号/手机号不一致，
+    // 说明用户在「新增投诉」页改成了另一名学员，必须新建工单，避免把他人数据写回旧工单。
+    if (currentTicketId.value && (boundIdCard.value || boundPhone.value)) {
+      const fId = (form.id_card || "").trim().toUpperCase();
+      const fPhone = (form.phone || "").trim();
+      const bId = (boundIdCard.value || "").trim().toUpperCase();
+      const bPhone = (boundPhone.value || "").trim();
+      const idMatch = fId && bId && fId === bId;
+      const phoneMatch = fPhone && bPhone && fPhone === bPhone;
+      if (!idMatch && !phoneMatch) {
+        currentTicketId.value = "";
+      }
+    }
+
     try {
       const payload = {
         ticket_id: currentTicketId.value,
@@ -395,13 +418,13 @@ export function useComplaint(onAutoQueryDone = null) {
         complaint_type: form.complaint_type,
         source_channel: concreteChannel,
         handler_name: form.handler_name.trim(),
-        complaint_summary: form.complaint_summary.trim(),
+        complaint_desc: (intakeText.value || "").trim(),
         attachments: intakeResult.value?._filepath ? [{
           _filepath: intakeResult.value._filepath,
           _filename: intakeResult.value._filename || "",
         }] : [],
       };
-      if (methodLabel === "身份证号") payload.id_card = idToQuery;
+      if (methodLabel === "身份证号") { payload.id_card = idToQuery; payload.phone = rawPhone; }
       else payload.phone = idToQuery;
 
       const started = await postJ("/api/query/start", payload);
@@ -472,11 +495,12 @@ export function useComplaint(onAutoQueryDone = null) {
     form.complaint_date = (ticket.complaint_date || todayStr()).slice(0, 10);
     form.complaint_type = ticket.complaint_type || "A";
     form.handler_name = ticket.handler_name || "";
-    form.complaint_summary = ticket.complaint_summary || "";
     intakeResult.value = Array.isArray(ticket.attachments) && ticket.attachments.length
       ? ticket.attachments[0]
       : null;
     currentTicketId.value = ticket.id || "";
+    boundIdCard.value = ticket.id_card || "";
+    boundPhone.value = ticket.phone || "";
     qr.value = {
       ...(ticket.query_result || {}),
       ticket_id: ticket.id || "",
@@ -502,7 +526,6 @@ export function useComplaint(onAutoQueryDone = null) {
     form.complaint_date = todayStr();
     form.complaint_type = "A";
     form.handler_name = "";
-    form.complaint_summary = "";
     intakeFile.value = null;
     intakeResult.value = null;
     intakeErr.value = "";
@@ -511,6 +534,8 @@ export function useComplaint(onAutoQueryDone = null) {
     qr.value = null;
     qErr.value = "";
     currentTicketId.value = "";
+    boundIdCard.value = "";
+    boundPhone.value = "";
   }
 
   return {
@@ -521,6 +546,8 @@ export function useComplaint(onAutoQueryDone = null) {
     intakeLoading,
     intakeResult,
     intakeErr,
+    pulseIdCard,
+    pulsePhone,
     intakeText,
     handleIntakeText,
     intakeTextareaRef,
diff --git a/static/js/composables/useHistory.js b/static/js/composables/useHistory.js
index d44302b..2ac3ec7 100644
--- a/static/js/composables/useHistory.js
+++ b/static/js/composables/useHistory.js
@@ -6,7 +6,7 @@ export function useHistory() {
   const hList = Vue.ref([]);
   const hTotal = Vue.ref(0);
   const hSearch = Vue.ref("");
-  const hLimit = Vue.ref(10);
+  const hLimit = Vue.ref(20);
   const hPage = Vue.ref(1);
   const hTotalPages = Vue.computed(() => Math.max(1, Math.ceil(hTotal.value / hLimit.value)));
   const hLoading = Vue.ref(false);
@@ -50,6 +50,14 @@ export function useHistory() {
   const vehicleSaving = Vue.ref(false);
   const chartDateStart = Vue.ref("");
   const chartDateEnd = Vue.ref("");
+  // 看板时间周期 tab：week / month / year / all / custom
+  const chartPeriod = Vue.ref("all");
+  // 身份证脱敏显示（明细表默认打码，可一键切换）
+  const masked = Vue.ref(true);
+  // 只看重复投诉（同身份证多条记录）
+  const repeatOnly = Vue.ref(false);
+  // 单位排行维度：count=按数量 / rate=按每百车率
+  const rankMode = Vue.ref("count");
   const periodStat = Vue.computed(() => stats.value);
   const durationStats = Vue.ref({ overdue_count: 0, avg_processing_hours: 0, completed_count: 0 });
 
@@ -87,14 +95,23 @@ export function useHistory() {
     return { start: _fmt(first), end: todayStr() };
   }
   function setChartPeriod(period) {
+    chartPeriod.value = period;
     if (period === 'week') { const r = getWeekRange(); chartDateStart.value = r.start; chartDateEnd.value = r.end; }
     else if (period === 'month') { const r = getMonthRange(); chartDateStart.value = r.start; chartDateEnd.value = r.end; }
     else if (period === 'year') { const r = getYearRange(); chartDateStart.value = r.start; chartDateEnd.value = r.end; }
+    else if (period === 'custom') { /* 使用手动选择的区间，不改日期 */ }
     else { chartDateStart.value = ''; chartDateEnd.value = ''; }
     loadStats();
     loadHist();
   }
 
+  /** 身份证显示：默认脱敏（前6后4），masked=false 时显示完整 */
+  function maskId(id) {
+    const s = String(id || "");
+    if (!masked.value) return s;
+    return s.length >= 15 ? s.slice(0, 6) + "********" + s.slice(-4) : s;
+  }
+
   let _st = null;
   let _idCardCounts = {};
 
@@ -119,6 +136,7 @@ export function useHistory() {
       const params = new URLSearchParams();
       if (hSearch.value) params.set("search", hSearch.value);
       if (statusFilter.value) params.set("status", statusFilter.value);
+      if (repeatOnly.value) params.set("repeat_only", "1");
       if (drill.value) params.set("school", drill.value.code);
       if (chartDateStart.value) params.set("start_date", chartDateStart.value);
       if (chartDateEnd.value) params.set("end_date", chartDateEnd.value);
@@ -148,6 +166,7 @@ export function useHistory() {
   }
 
   async function loadStats() {
+    loadPrevStats();
     try {
       const params = new URLSearchParams();
       if (chartDateStart.value) params.set("start_date", chartDateStart.value);
@@ -162,6 +181,106 @@ export function useHistory() {
     await loadDurationStats();
   }
 
+  // ── 环比对比卡：上期窗口 = 本期窗口等长向前平移 ──
+  const prevStats = Vue.ref(null);      // 上期 ticket-statistics
+  const prevDuration = Vue.ref(null);   // 上期处理时长统计
+
+  function shiftedRange(startStr, endStr) {
+    const s = new Date(startStr + "T00:00:00");
+    const e = new Date(endStr + "T00:00:00");
+    const len = Math.round((e - s) / 86400000) + 1;
+    const p1 = new Date(s); p1.setDate(p1.getDate() - len);
+    const p2 = new Date(s); p2.setDate(p2.getDate() - 1);
+    return { start: _fmt(p1), end: _fmt(p2) };
+  }
+
+  async function loadPrevStats() {
+    prevStats.value = null;
+    prevDuration.value = null;
+    if (!chartDateStart.value || !chartDateEnd.value) return; // "全部"无上期可比
+    try {
+      const w = shiftedRange(chartDateStart.value, chartDateEnd.value);
+      const params = new URLSearchParams();
+      params.set("start_date", w.start);
+      params.set("end_date", w.end);
+      if (drill.value) params.set("unit_code", drill.value.code);
+      else params.set("scope", statScope.value);
+      const [a, b] = await Promise.all([
+        getJ("/api/ticket-statistics?" + params.toString()),
+        getJ("/api/statistics/duration?" + params.toString()),
+      ]);
+      prevStats.value = a.success ? a.data : null;
+      prevDuration.value = b.success ? b.data : null;
+    } catch (e) {
+      console.error(e);
+    }
+  }
+
+  /** 投诉类指标语义：涨=坏(红)、降=好(绿)；率类用百分点差 */
+  function cmpDelta(curV, preV, pp) {
+    if (!preV && !curV) return { deltaText: '持平', cls: 'delta-flat', icon: 'bi-dash-lg' };
+    if (!preV) return { deltaText: '新增', cls: 'delta-bad', icon: 'bi-arrow-up' };
+    if (!curV) return { deltaText: '清零', cls: 'delta-good', icon: 'bi-arrow-down' };
+    if (pp) {
+      const d = Number((curV - preV).toFixed(1));
+      if (d === 0) return { deltaText: '持平', cls: 'delta-flat', icon: 'bi-dash-lg' };
+      return d > 0
+        ? { deltaText: '+' + d + 'pp', cls: 'delta-bad', icon: 'bi-arrow-up' }
+        : { deltaText: d + 'pp', cls: 'delta-good', icon: 'bi-arrow-down' };
+    }
+    const pct = Math.round((curV - preV) / preV * 100);
+    if (pct === 0) return { deltaText: '持平', cls: 'delta-flat', icon: 'bi-dash-lg' };
+    return pct > 0
+      ? { deltaText: '+' + pct + '%', cls: 'delta-bad', icon: 'bi-arrow-up' }
+      : { deltaText: '-' + Math.abs(pct) + '%', cls: 'delta-good', icon: 'bi-arrow-down' };
+  }
+
+  function cmpRow(label, unit, curD, preD, fmtFn, pp) {
+    const max = Math.max(curD, preD);
+    return Object.assign(
+      { label, unit, cur: fmtFn(curD), prev: fmtFn(preD), wCur: max > 0 ? curD / max * 100 : 0, wPrev: max > 0 ? preD / max * 100 : 0 },
+      cmpDelta(curD, preD, pp),
+    );
+  }
+
+  const compare = Vue.computed(() => {
+    const cur = stats.value || {};
+    const pre = prevStats.value;
+    if (!pre || !chartDateStart.value || !chartDateEnd.value) return { rows: [], windowText: '' };
+    const curDur = durationStats.value || {};
+    const preDur = prevDuration.value || {};
+    const rows = [
+      cmpRow('投诉总量', '件', cur.total || 0, pre.total || 0, v => String(v)),
+      cmpRow('有效投诉', '件', cur.effective_total || 0, pre.effective_total || 0, v => String(v)),
+      cmpRow('撤诉率', '%',
+        cur.total ? (cur.withdrawn_total || 0) / cur.total * 100 : 0,
+        pre.total ? (pre.withdrawn_total || 0) / pre.total * 100 : 0,
+        v => Number(v).toFixed(1), true),
+      cmpRow('平均处理时长', '天',
+        Number(curDur.avg_processing_hours || 0) / 24,
+        Number(preDur.avg_processing_hours || 0) / 24,
+        v => Number(v).toFixed(1)),
+    ];
+    const w = shiftedRange(chartDateStart.value, chartDateEnd.value);
+    return { rows, windowText: `本期 ${chartDateStart.value}~${chartDateEnd.value} · 上期 ${w.start}~${w.end}` };
+  });
+
+  // 来源渠道 TOP5（迷你条形，占比基于 TOP5 内合计）
+  const topChannels = Vue.computed(() => {
+    const list = (stats.value.by_source || []).slice(0, 5);
+    const total = list.reduce((a, b) => a + (Number(b.count) || 0), 0) || 1;
+    return list.map(x => ({ source: x.source, count: x.count, pct: Number((x.count / total * 100).toFixed(1)) }));
+  });
+
+  // 明细表分页按钮（当前页 ±2 窗口）
+  const hPageButtons = Vue.computed(() => {
+    const tp = hTotalPages.value, cur = hPage.value;
+    const from = Math.max(1, cur - 2), to = Math.min(tp, cur + 2);
+    const arr = [];
+    for (let i = from; i <= to; i++) arr.push({ n: i, cur: i === cur });
+    return arr;
+  });
+
   /** 切换看板范围（全部/分校/分店） */
   function setStatScope(scope) {
     statScope.value = scope;
@@ -337,6 +456,7 @@ export function useHistory() {
       const params = new URLSearchParams();
       if (statusFilter.value) params.set("status", statusFilter.value);
       if (drill.value) params.set("school", drill.value.code);
+      if (statScope.value) params.set("scope", statScope.value);
       if (chartDateStart.value) params.set("date_start", chartDateStart.value);
       if (chartDateEnd.value) params.set("date_end", chartDateEnd.value);
 
@@ -419,15 +539,16 @@ export function useHistory() {
   }
 
   function updateCharts() {
-    // 飞书风格配色
     const colors = ['#3370FF', '#34D399', '#FBBF24', '#F87171', '#A78BFA', '#60A5FA', '#2DD4BF'];
     
     // 1. 投诉趋势 - 只展示数据库中的真实案件数量
     if (chartInstances.trend) {
       const trend = stats.value.daily_trend || [];
       const days = trend.map(item => {
-        const parts = String(item.date || "").split("-");
-        return parts.length === 3 ? `${Number(parts[1])}/${Number(parts[2])}` : item.date;
+        const d = String(item.date || "").trim();
+        if (!d || d === "null" || d === "None") return "未登记日期";
+        const parts = d.split("-");
+        return parts.length === 3 ? `${Number(parts[1])}/${Number(parts[2])}` : d;
       });
       const data = trend.map(item => Number(item.count) || 0);
       chartInstances.trend.setOption({
@@ -440,17 +561,24 @@ export function useHistory() {
       });
     }
 
-    // 2. 驾校排行 - 横向柱状图（渐变色）
+    // 2. 单位排行 - 横向柱状图（按数量 / 按每百车投诉率，点击柱子下钻该网点）
     if (chartInstances.school && stats.value.by_school) {
-      const schoolData = stats.value.by_school.slice(0, 10).reverse();
-      const schoolVals = schoolData.map(i => Number(i.count) || 0);
-      const maxVal = Math.max(...schoolVals, 1);
+      let items = stats.value.by_school.slice(0, 10);
+      if (rankMode.value === 'rate') {
+        items = items.filter(i => i.complaint_rate != null)
+                     .sort((a, b) => b.complaint_rate - a.complaint_rate);
+      } else {
+        items.sort((a, b) => b.count - a.count);
+      }
+      items = items.reverse(); // 图表自下而上递增
+      const vals = items.map(i => rankMode.value === 'rate' ? Number(i.complaint_rate) : Number(i.count) || 0);
+      const unitLabel = rankMode.value === 'rate' ? '件/百车' : '件';
       chartInstances.school.setOption({
-        grid: { left: 60, right: 30, top: 10, bottom: 20 },
+        grid: { left: 60, right: 44, top: 10, bottom: 20 },
         xAxis: { type: 'value', axisLine: { show: false }, splitLine: { lineStyle: { color: '#F3F4F6' } }, axisLabel: { color: '#6B7280', fontSize: 10 } },
-        yAxis: { type: 'category', data: schoolData.map(i => i.name || i.school || i.code || '未查到分校'), axisLine: { lineStyle: { color: '#E5E7EB' } }, axisLabel: { color: '#374151', fontSize: 11 } },
+        yAxis: { type: 'category', data: items.map(i => i.name || i.school || i.code || '未知网点'), axisLine: { lineStyle: { color: '#E5E7EB' } }, axisLabel: { color: '#374151', fontSize: 11 } },
         series: [{
-          data: schoolVals, type: 'bar', barWidth: 16,
+          data: vals, type: 'bar', barWidth: 16,
           itemStyle: {
             borderRadius: [0, 4, 4, 0],
             color: new echarts.graphic.LinearGradient(0, 0, 1, 0, [
@@ -458,15 +586,31 @@ export function useHistory() {
               { offset: 1, color: '#2563EB' }
             ])
           },
-          label: { show: true, position: 'right', fontSize: 11, fontWeight: 'bold', color: '#374151' }
+          label: {
+            show: true, position: 'right', fontSize: 11, fontWeight: 'bold', color: '#374151',
+            formatter: p => p.value + (rankMode.value === 'rate' ? '%' : '')
+          }
         }],
-        tooltip: { trigger: 'axis' }
+        tooltip: { trigger: 'axis', formatter: ps => `${ps[0].name}<br>${unitLabel}：<b>${ps[0].value}</b>` }
+      }, true);
+      chartInstances.school.off('click');
+      chartInstances.school.on('click', p => {
+        const it = items[p.dataIndex];
+        if (it && it.code && it.code !== drillCode.value) drillInto(it.code, it.name || it.school || it.code);
       });
     }
 
     // 3. 投诉类型 - 饼图（带图例 + 百分比）
     if (chartInstances.type && stats.value.by_type) {
-      const typeData = stats.value.by_type.map(i => ({ name: i.type, value: i.count }));
+      // P2-14：前端双保险归一，避免 'A' 与 'A-退费纠纷' 并存、空值显示字面标签
+      const RAW_TYPE_LABELS = { "A": "退费纠纷", "B": "教学服务", "C": "考试安排", "D": "合同争议", "E": "其他", "A-退费纠纷": "退费纠纷" };
+      const normType = (t) => RAW_TYPE_LABELS[String(t || "").trim()] || (t ? String(t).trim() : "未知");
+      const typeAgg = {};
+      for (const i of stats.value.by_type) {
+        const n = normType(i.type);
+        typeAgg[n] = (typeAgg[n] || 0) + (Number(i.count) || 0);
+      }
+      const typeData = Object.entries(typeAgg).map(([name, value]) => ({ name, value }));
       const typeTotal = typeData.reduce((s, d) => s + d.value, 0);
       chartInstances.type.setOption({
         color: colors,
@@ -523,6 +667,8 @@ export function useHistory() {
     statusFilter, schoolOptions,
     stats, chartDateStart, chartDateEnd, setChartPeriod, periodStat,
     durationStats, loadDurationStats,
+    // 看板增强：周期tab / 脱敏 / 重复投诉过滤 / 排行维度 / 环比对比卡
+    chartPeriod, masked, maskId, repeatOnly, rankMode, compare, topChannels, hPageButtons,
     groupedHistory, toggleGroup,
     debSearch, loadHist, loadStats, loadFromHist, exportTickets, updateTicketStatus,
     loadSchoolCodes,
diff --git a/static/js/composables/useSettings.js b/static/js/composables/useSettings.js
index 50e0acb..b8f9510 100644
--- a/static/js/composables/useSettings.js
+++ b/static/js/composables/useSettings.js
@@ -11,7 +11,6 @@ function createDefaultConfig() {
     llm_intake: llm(),
     llm_contract_vision: llm(),
     llm_contract_text: llm(),
-    feishu: { app_id: "", app_secret: "", bitable_app_token: "", bitable_table_id: "" },
     paths: { reply_dir: "", contract_dir: "", upload_dir: "" },
   };
 }
diff --git a/static/js/composables/useWorkflow.js b/static/js/composables/useWorkflow.js
index 68d6927..1f5bf03 100644
--- a/static/js/composables/useWorkflow.js
+++ b/static/js/composables/useWorkflow.js
@@ -1,4 +1,4 @@
-// 工作流程组合式函数：合同 → 退费分析 → 沟通记录 → 结果归档/文档 → 飞书可选归档
+// 工作流程组合式函数：合同 → 退费分析 → 沟通记录 → 结果归档/文档
 import { getJ, postJ } from "api";
 
 /** 计算考试费
@@ -56,11 +56,12 @@ function calcExamFees(examCounts, includesExam, includesMakeup = true) {
   return { total, fees: feeItems };
 }
 
-export function useWorkflow(toast, getQr, getTicketId) {
-  // 工作流步骤：1=合同获取，2=退费分析，3=沟通记录，4=结果归档/文档，5=飞书可选归档
+export function useWorkflow(toast, getQr, getTicketId, hooks = {}) {
+  // hooks.afterFeeConfirm(confirmed, ticketId)：费用确认成功后通知外部同步工作台/列表状态
+  // 工作流步骤：1=合同获取，2=退费分析，3=沟通记录，4=结果归档/文档
   const workflowStep = Vue.ref(1);
   const workflowStatusText = Vue.computed(() => {
-    const m = { 1: "待合同获取", 2: "待退费分析", 3: "待沟通记录", 4: "待结果归档", 5: "待飞书归档" };
+    const m = { 1: "待合同获取", 2: "待退费分析", 3: "待沟通记录", 4: "待结果归档" };
     return m[workflowStep.value] || "已完成";
   });
 
@@ -178,10 +179,6 @@ export function useWorkflow(toast, getQr, getTicketId) {
   const rpResult = Vue.ref(null);
   const rpErr = Vue.ref("");
 
-  // 飞书
-  const fsLoading = Vue.ref(false);
-  const fsResult = Vue.ref(null);
-
   // Load saved analysis (check cache + history inheritance)
   async function loadSavedAnalysis(ticketId) {
     try {
@@ -337,7 +334,7 @@ export function useWorkflow(toast, getQr, getTicketId) {
 
     // 非手动模式必须等三系统数据就绪，否则退费计算会用陈旧/缺失数据
     if (!threeSystemReady.value) {
-      toast("三系统数据未就绪", "请先在受理页完成「查询三系统」（身份证/手机号），再进入退费分析", "warning");
+      toast("三系统数据未就绪", "请先在新增投诉页完成「查询三系统」（身份证/手机号），再进入退费分析", "warning");
       return;
     }
 
@@ -653,7 +650,7 @@ export function useWorkflow(toast, getQr, getTicketId) {
 
 	  async function handleSaveAndConfirm(ticketId, planStatus = "confirmed") {
     if (!ar.value) {
-      toast("请先进行AI分析", "", "warning");
+      toast("请先进行AI分析或手动录入费用", "", "warning");
       return;
     }
     if (ar.value.can_confirm_fee_plan === false) {
@@ -661,7 +658,7 @@ export function useWorkflow(toast, getQr, getTicketId) {
       return;
     }
 	    if (!ticketId) {
-	      toast("缺少案件ID", "请先完成工单受理和学员查询", "warning");
+	      toast("缺少案件ID", "请先完成工单登记和学员查询", "warning");
 	      return;
 	    }
 	    recalc();
@@ -676,6 +673,7 @@ export function useWorkflow(toast, getQr, getTicketId) {
 	        plan_status: planStatus,
 	        total_fee: Number(ar.value.total_fee) || 0,
 	        actual_paid: actualPaid,
+	        deductions: ar.value.deductions || [],
 	        contract_fields: ar.value.contract_fields || {},
 	        clauses: ar.value.clauses || [],
 	        contract_code: ar.value.contract_code || manualContract.contract_code || "",
@@ -693,6 +691,7 @@ export function useWorkflow(toast, getQr, getTicketId) {
 	      await loadCommunications(ticketId);
 	      workflowStep.value = 3;
 	      toast(planStatus === "provisional" ? "阶段费用方案已确认" : "正式费用方案已确认", "已进入沟通记录", "success");
+	      if (hooks.afterFeeConfirm) await hooks.afterFeeConfirm(confirmed, ticketId);
 	    } catch (e) {
 	      toast("保存失败", e.message, "danger");
 	    } finally {
@@ -702,6 +701,23 @@ export function useWorkflow(toast, getQr, getTicketId) {
 
   // ── 扣费明细编辑 ──
 
+  // ISS-UJ-02：AI 分析失败/无结果时，初始化空表骨架供手动录入，保证流程可走通
+  function startManualFeeEntry() {
+    if (ar.value) return;
+    ar.value = {
+      deductions: [],
+      total_fee: 0,
+      actual_paid: 0,
+      total_deduction: 0,
+      refund: 0,
+      contract_fields: {},
+      clauses: [],
+      contract_code: "",
+      source: "manual",
+    };
+    toast("已切换手动录入", "请填写合同总额、实缴金额与扣费行后确认明细", "info");
+  }
+
   function recalc() {
     if (!ar.value) return;
     
@@ -768,6 +784,38 @@ export function useWorkflow(toast, getQr, getTicketId) {
     recalc();
   }
 
+  // 导出扣费明细为 Excel（前端生成 .xls，Excel/WPS 可直接打开）
+  function xesc(s) {
+    return String(s ?? "").replace(/[&<>]/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;" }[c]));
+  }
+
+  function exportDeductions() {
+    if (!ar.value || !Array.isArray(ar.value.deductions)) {
+      toast("暂无可导出的明细", "请先完成费用分析", "warning");
+      return;
+    }
+    const rows = ar.value.deductions.map((d, i) =>
+      `<tr><td>${i + 1}</td><td>${xesc(d.item)}</td><td>${Number(d.amount) || 0}</td><td>${xesc(d.reason)}</td></tr>`).join("");
+    const actualPaid = Number(ar.value.actual_paid || ar.value.paid_amount || 0);
+    const totalDeduction = Number(ar.value.total_deduction || 0);
+    const refund = Math.max(actualPaid - totalDeduction, 0);
+    const html = `<html xmlns:x="urn:schemas-microsoft-com:office:excel"><head><meta charset="UTF-8"></head><body>`
+      + `<table border="1"><thead><tr><th>序号</th><th>扣费项目</th><th>金额（元）</th><th>扣费依据</th></tr></thead>`
+      + `<tbody>${rows}</tbody>`
+      + `<tfoot><tr><td colspan="2">实缴总额</td><td>${actualPaid}</td><td></td></tr>`
+      + `<tr><td colspan="2">扣费合计</td><td>${totalDeduction}</td><td></td></tr>`
+      + `<tr><td colspan="2">应退</td><td>${refund}</td><td></td></tr></tfoot></table></body></html>`;
+    const blob = new Blob(["\ufeff" + html], { type: "application/vnd.ms-excel;charset=utf-8" });
+    const url = URL.createObjectURL(blob);
+    const a = document.createElement("a");
+    a.download = `投诉扣费明细_${getQr?.()?.name || "学员"}_${new Date().toISOString().slice(0, 10)}.xls`;
+    document.body.appendChild(a);
+    a.click();
+    a.remove();
+    URL.revokeObjectURL(url);
+    toast("已导出扣费明细", a.download, "success");
+  }
+
   function upsertDeductionFromField(itemName, amount, reason) {
     if (!ar.value) return;
     if (!Array.isArray(ar.value.deductions)) ar.value.deductions = [];
@@ -1144,27 +1192,6 @@ export function useWorkflow(toast, getQr, getTicketId) {
     }
   }
 
-  // ── 飞书 ──
-
-  async function submitFeishu(data) {
-    fsLoading.value = true;
-    fsResult.value = null;
-
-    try {
-      const d = await postJ("/api/feishu/submit", data);
-      if (d.success) {
-        fsResult.value = d;
-        toast("飞书提交成功", "编号: " + (d.handle_no || ""), "success");
-      } else {
-        toast("提交失败", d.error, "danger");
-      }
-    } catch (e) {
-      toast("提交失败", e.message, "danger");
-    } finally {
-      fsLoading.value = false;
-    }
-  }
-
   // ── 投诉登记表 ──
 
   async function genRegistrationForm(ticketId) {
@@ -1199,7 +1226,6 @@ export function useWorkflow(toast, getQr, getTicketId) {
     if (aTimerId) { clearInterval(aTimerId); aTimerId = null; }
     rpResult.value = null;
     rpErr.value = "";
-	    fsResult.value = null;
 	    feeConfirmed.value = false;
 	    feeConfirmedAt.value = "";
 	    communications.value = [];
@@ -1237,7 +1263,6 @@ export function useWorkflow(toast, getQr, getTicketId) {
 	    feeReopenReason.value = "";
 	    rpLoading.value = false;
     rpErr.value = "";
-    fsLoading.value = false;
   }
 
   function restore(detail) {
@@ -1310,8 +1335,6 @@ export function useWorkflow(toast, getQr, getTicketId) {
     rpLoading,
     rpResult,
     rpErr,
-    fsLoading,
-    fsResult,
     contractInput,
     uploadedFiles,
     contractManifest,
@@ -1325,12 +1348,13 @@ export function useWorkflow(toast, getQr, getTicketId) {
     openPreview, closePreview,
     handleSaveAndConfirm,
     recalc,
+    startManualFeeEntry,
     applyContractFields,
     addDeduction,
     removeDeduction,
+    exportDeductions,
     updatePenaltyRate,  // 导出违约金比例修正方法
     genReply,
-    submitFeishu,
 	    // 沟通/归档
 	    fromHistoryLabel,
 	    OUTCOME_OPTIONS, COOPERATION_OPTIONS, NEGOTIATION_OPTIONS,
diff --git a/templates/index.html b/templates/index.html
index 48b37f2..e55d2ac 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -3,1666 +3,1178 @@
 <head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
-  <title>驾校投诉处理系统</title>
+  <title>投诉处理系统 · v2 工作台</title>
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
-  <link rel="stylesheet" href="/static/css/style.css?v=10">
+  <link rel="stylesheet" href="/static/css/style.css?v=13">
   <script src="https://cdn.jsdelivr.net/npm/echarts@5.4/dist/echarts.min.js"></script>
+  <style>
+  /* ===== Token（DESIGN.md：政务信赖蓝） ===== */
+  :root{
+    --color-primary:#2563EB; --color-primary-dark:#1D4ED8; --color-primary-50:#EFF6FF;
+    --brand-gradient:linear-gradient(180deg,#1E3A8A,#2563EB);
+    --color-brand-accent:#4F46E5;
+    --color-success:#16A34A; --color-success-50:#F0FDF4;
+    --color-warning:#D97706; --color-warning-50:#FFFBEB;
+    --color-danger:#DC2626;  --color-danger-50:#FEF2F2;
+    --color-text:#0F172A; --color-text-secondary:#64748B;
+    --color-border:#E2E8F0; --color-surface-50:#F8FAFC; --color-bg:#EEF3F8; --color-surface:#FFFFFF;
+    --font-sans:"PingFang SC","Microsoft YaHei","Hiragino Sans GB",-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;
+    --font-mono:"SF Mono","JetBrains Mono",Menlo,Consolas,monospace;
+    --shadow-sm:0 1px 2px rgba(15,23,42,.06),0 1px 3px rgba(15,23,42,.04);
+    --shadow-md:0 4px 12px rgba(15,23,42,.08);
+    --shadow-lg:0 12px 32px rgba(15,23,42,.12);
+    --radius:8px; --radius-lg:12px;
+    --sp-2:8px; --sp-3:12px; --sp-4:16px; --sp-6:24px; --sp-8:32px;
+  }
+  *{box-sizing:border-box;margin:0;padding:0}
+  body{min-height:100vh;background:var(--color-bg);color:var(--color-text);
+    font:400 13px/1.6 var(--font-sans);}
+  .mono{font-family:var(--font-mono)}
+
+  /* ===== 左侧导航栏（对齐原型 v2-demo 布局）===== */
+  .app-shell{display:flex;min-height:100vh}
+  .sidebar{width:240px;flex:none;background:var(--brand-gradient);color:#fff;display:flex;flex-direction:column;
+    padding:20px 12px;position:sticky;top:0;height:100vh}
+  .sidebar .brand{font:700 15px/1.3 var(--font-sans);padding:0 8px 20px;white-space:nowrap}
+  .sidebar .brand small{display:block;font-weight:400;font-size:11px;color:rgba(255,255,255,.62)}
+  .nav-link{display:flex;align-items:center;gap:10px;color:rgba(255,255,255,.78);padding:10px 16px;border-radius:8px;cursor:pointer;
+    font-weight:600;margin-bottom:2px;border:none;background:none;width:100%;text-align:left;font-size:13px;font-family:var(--font-sans)}
+  .nav-link:hover{background:rgba(255,255,255,.08);color:#fff}
+  .nav-link.active{background:rgba(255,255,255,.16);color:#fff;box-shadow:inset 3px 0 0 var(--color-brand-accent)}
+  .sidebar .footer{margin-top:auto;padding:0 8px;font-size:11px;color:rgba(255,255,255,.62)}
+
+  .main{flex:1;min-width:0;padding:var(--sp-6) var(--sp-8)}
+  .page-title{font-size:20px;font-weight:700;margin-bottom:var(--sp-4)}
+
+  /* ===== 按钮 ===== */
+  .btn{display:inline-flex;align-items:center;gap:6px;font:600 13px/1 var(--font-sans);padding:8px 16px;
+    border-radius:var(--radius);border:1px solid transparent;cursor:pointer;background:none;color:var(--color-text);transition:background .15s,box-shadow .15s}
+  .btn:disabled{opacity:.45;cursor:not-allowed}
+  .btn:focus-visible{outline:2px solid var(--color-primary);outline-offset:2px}
+  .btn-primary{background:var(--color-primary);color:#fff;border-color:var(--color-primary)}
+  .btn-primary:hover:not(:disabled){background:var(--color-primary-dark);box-shadow:var(--shadow-md)}
+  .btn-secondary{background:#fff;color:var(--color-text);border-color:var(--color-border)}
+  .btn-secondary:hover:not(:disabled){background:var(--color-surface-50);border-color:var(--color-text-secondary)}
+  .btn-ghost{color:var(--color-danger);border-color:transparent}
+  .btn-ghost:hover:not(:disabled){background:var(--color-danger-50)}
+  .btn-sm{padding:6px 12px;font-size:12px}
+
+  /* ===== 卡片 ===== */
+  .card{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);box-shadow:var(--shadow-sm)}
+  .card-header{padding:12px 16px;border-bottom:1px solid var(--color-border);font:600 14px/1.4 var(--font-sans);
+    display:flex;align-items:center;gap:8px;flex-wrap:wrap}
+  .card-body{padding:16px}
+  .panel{scroll-margin-top:16px}
+  .panel + .panel{margin-top:16px}
+
+  /* ===== 徽章 ===== */
+  .badge{display:inline-flex;align-items:center;gap:4px;padding:3px 10px;font:600 11px/1.3 var(--font-sans);border-radius:999px;border:1px solid transparent}
+  .badge-pending{background:var(--color-warning-50);color:var(--color-warning);border-color:#FDE68A}
+  .badge-processing{background:var(--color-primary-50);color:var(--color-primary);border-color:#BFDBFE}
+  .badge-completed{background:var(--color-success-50);color:var(--color-success);border-color:#A7F3D0}
+  .badge-muted{background:var(--color-surface-50);color:var(--color-text-secondary);border-color:var(--color-border)}
+  .badge-danger{background:var(--color-danger-50);color:var(--color-danger);border-color:#FECACA}
+
+  /* ===== 表格 ===== */
+  .table{width:100%;border-collapse:collapse;font-size:13px;line-height:1.45}
+  .table th{padding:10px 14px;text-align:left;font-weight:600;font-size:12px;color:var(--color-text-secondary);
+    background:var(--color-surface-50);border-bottom:2px solid var(--color-border);white-space:nowrap}
+  .table td{padding:10px 14px;border-bottom:1px solid var(--color-border);vertical-align:middle}
+  .table tbody tr:hover{background:var(--color-surface-50)}
+  .amt-td{text-align:center;font-weight:600;font-family:var(--font-mono);white-space:nowrap}
+
+  /* ===== 表单 ===== */
+  .form-label{display:block;font-weight:600;font-size:12px;color:var(--color-text-secondary);margin-bottom:6px}
+  .form-control{width:100%;padding:8px 12px;font-size:13px;color:var(--color-text);border:1.5px solid var(--color-border);
+    border-radius:var(--radius);background:#fff;font-family:inherit;transition:border-color .15s}
+  .form-control:focus{outline:none;border-color:var(--color-primary);box-shadow:0 0 0 3px var(--color-primary-50)}
+  textarea.form-control{resize:vertical}
+
+  /* ===== KPI ===== */
+  .kpi-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:16px;margin-bottom:20px}
+  .kpi{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:16px;box-shadow:var(--shadow-sm)}
+  .kpi .num{font-size:26px;font-weight:700;line-height:1.25}
+  .kpi .lbl{font-size:12px;color:var(--color-text-secondary)}
+
+  /* ===== 布局栅格 ===== */
+  .grid-2{display:grid;grid-template-columns:1fr 1fr;gap:20px}
+  .grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
+  .chips{display:flex;gap:8px;margin-bottom:16px;flex-wrap:wrap;align-items:center}
+  .chip{border:1px solid var(--color-border);background:#fff;border-radius:999px;padding:5px 14px;font-size:12px;cursor:pointer;font-weight:600;color:var(--color-text-secondary)}
+  .chip.on{background:var(--color-primary);border-color:var(--color-primary);color:#fff}
+  .hint{font-size:12px;color:var(--color-text-secondary)}
+
+  /* ===== 列表分组 ===== */
+  .grp{margin-bottom:24px}
+  .grp-head{display:flex;align-items:center;gap:10px;margin-bottom:10px}
+  .grp-head .g-num{font-weight:700;font-size:15px}
+  .case-card{display:flex;align-items:center;gap:14px;padding:12px 16px;border:1px solid var(--color-border);
+    border-radius:var(--radius-lg);background:#fff;margin-bottom:10px;cursor:pointer;transition:border-color .15s,box-shadow .15s}
+  .case-card:hover{border-color:var(--color-primary);box-shadow:var(--shadow-md)}
+  .case-card .who{font-weight:700;font-size:14px}
+  .case-card .meta{color:var(--color-text-secondary);font-size:12px;margin-top:2px}
+  .case-card .spacer{flex:1}
+
+  /* ===== 受理 ===== */
+  .intake-banner{display:flex;align-items:center;gap:12px;justify-content:space-between;flex-wrap:wrap;
+    background:var(--color-warning-50);border:1px solid #FDE68A;color:var(--color-warning);
+    border-radius:var(--radius-lg);padding:12px 16px;margin-bottom:16px;font-size:13px}
+  .intake-banner .ib-text{flex:1;min-width:240px;line-height:1.5}
+  .intake-banner b{color:var(--color-text)}
+  .intake-banner .bi{margin-right:4px}
+  .intake-row{display:flex;align-items:flex-end;gap:10px;flex-wrap:wrap}
+  .intake-icon{width:44px;height:44px;flex:none;border:1.5px dashed var(--color-border);border-radius:var(--radius);
+    display:flex;align-items:center;justify-content:center;font-size:20px;color:var(--color-text-secondary);cursor:pointer}
+  .intake-icon:hover{border-color:var(--color-primary);color:var(--color-primary)}
+  .intake-input{flex:1;min-width:240px}
+  .query-form{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:16px}
+  .progress-card{margin-top:14px}
+  .clues{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-top:12px;
+    background:var(--color-success-50);border:1px solid #BBF7D0;border-radius:var(--radius);padding:10px 14px}
+  .clues-head{display:flex;align-items:center;gap:6px;font-weight:700;color:var(--color-success);font-size:12px}
+  .clue-chip{display:inline-flex;align-items:center;gap:6px;background:#fff;border:1px solid #BBF7D0;border-radius:999px;padding:3px 12px;font-size:12px}
+  .clue-chip b{font-family:var(--font-mono);font-weight:600}
+  .fgroup+.fgroup{margin-top:16px;padding-top:14px;border-top:1px dashed var(--color-border)}
+  .fgroup-title{display:flex;align-items:center;gap:6px;font-size:12px;font-weight:700;color:var(--color-text-secondary);letter-spacing:.05em;margin-bottom:8px}
+  .fgroup-title .bi{color:var(--color-primary)}
+  .form-label .req{color:var(--color-danger);margin-left:2px}
+  .pulse{animation:pulseGlow 1s ease 2}
+  @keyframes pulseGlow{0%,100%{box-shadow:none}50%{box-shadow:0 0 0 4px #93C5FD}}
+  .progress-bar-bg{height:8px;background:var(--color-surface-50);border-radius:4px;overflow:hidden}
+  .progress-bar-fill{height:100%;background:var(--color-primary);transition:width .3s}
+  .progress-status{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}
+  .alert{padding:10px 14px;border-radius:var(--radius);font-size:12.5px;margin-top:10px}
+  .alert-success{background:var(--color-success-50);color:var(--color-success);border:1px solid #A7F3D0}
+  .alert-danger{background:var(--color-danger-50);color:var(--color-danger);border:1px solid #FECACA}
+  .alert-warning{background:var(--color-warning-50);color:var(--color-warning);border:1px solid #FDE68A}
+  .alert-info{background:var(--color-primary-50);color:var(--color-primary);border:1px solid #BFDBFE}
+
+  /* ===== 工作台两栏（主列 + 右侧档案栏，对齐原型布局C）===== */
+  .wb{display:grid;grid-template-columns:minmax(0,1fr) 280px;gap:20px;align-items:start}
+  .case-head{display:flex;align-items:flex-start;gap:16px;margin-bottom:16px;flex-wrap:wrap}
+  .case-head .who{font-size:17px;font-weight:700}
+  .case-head .who .code{color:var(--color-primary)}
+  .case-head .meta{color:var(--color-text-secondary);font-size:12px;margin-top:2px;display:flex;gap:12px;flex-wrap:wrap}
+  .case-head .actions{margin-left:auto;display:flex;gap:8px}
+  .info-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0 36px}
+  .info-grid .ig{display:flex;align-items:center;padding:9px 2px;border-bottom:1px solid var(--color-surface-50);font-size:13px}
+  .info-grid .ig .k{width:88px;flex:none;color:var(--color-text-secondary)}
+  .info-grid .ig .v{flex:1;min-width:0;font-weight:600;word-break:break-all}
+  .qtabbar{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:14px}
+  .sys-strip{display:flex;gap:8px;flex-wrap:wrap;margin-left:auto}
+  .sys-pill{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:700;padding:3px 10px;border-radius:999px;border:1px solid}
+  .sys-pill.ok{color:var(--color-success);background:var(--color-success-50);border-color:#A7F3D0}
+  .sys-pill.warn{color:var(--color-warning);background:var(--color-warning-50);border-color:#FDE68A}
+
+  /* 扣费明细 */
+  .fee-total{display:flex;gap:20px;align-items:baseline;flex-wrap:wrap;padding:12px 16px;background:var(--color-surface-50);border-radius:var(--radius);margin-top:12px}
+  .fee-total .big{font-size:24px;font-weight:800;color:var(--color-success)}
+
+  /* 合同预览 */
+  .modal-overlay{position:fixed;inset:0;background:rgba(15,23,42,.5);z-index:4500;display:flex;align-items:center;justify-content:center}
+  .modal-content{background:#fff;border-radius:var(--radius-lg);box-shadow:var(--shadow-lg);width:min(940px,94vw);max-height:90vh;display:flex;flex-direction:column}
+  .modal-content .m-head{padding:14px 18px;font-weight:700;border-bottom:1px solid var(--color-border);display:flex;align-items:center;gap:8px}
+  .modal-content .m-body{padding:18px;overflow:auto}
+  .modal-content .m-foot{padding:12px 18px;border-top:1px solid var(--color-border);display:flex;justify-content:flex-end;gap:8px}
+  .contract-wrap{display:grid;grid-template-columns:1fr 250px;gap:16px;min-height:420px}
+  .contract-doc{overflow:auto;border:1px solid var(--color-border);border-radius:var(--radius);padding:18px 22px;
+    font-family:"Songti SC","STSong","SimSun",serif;font-size:13px;line-height:2;background:#fff}
+  .contract-doc h4{font-size:14px;text-align:center;margin-bottom:12px}
+  .contract-doc p{margin-bottom:10px;text-indent:2em}
+  .contract-doc p.hl{background:#FEF9C3;border-radius:4px;transition:background .3s}
+  .contract-side{overflow:auto;display:flex;flex-direction:column;gap:8px}
+  .contract-side .fi{border:1.5px solid var(--color-border);border-radius:var(--radius);padding:8px 12px;cursor:pointer;font-size:12px;line-height:1.6;background:#fff}
+  .contract-side .fi:hover{border-color:var(--color-primary);box-shadow:var(--shadow-sm)}
+  .contract-side .fi b{display:block}
+  .contract-side .fi .b{color:var(--color-text-secondary)}
+
+  /* 回复函富文本 */
+  .rte-bar{display:flex;gap:4px;margin-bottom:8px;flex-wrap:wrap;align-items:center}
+  .rte-bar button{border:1px solid var(--color-border);background:#fff;border-radius:6px;height:28px;font-size:12px;cursor:pointer;padding:0 9px;font-weight:600;color:var(--color-text)}
+  .rte-bar button:hover{background:var(--color-surface-50);border-color:var(--color-primary)}
+  .rte-bar select{height:28px;border:1px solid var(--color-border);border-radius:6px;background:#fff;font-size:12px;padding:0 4px}
+  .rte-bar .sep{width:1px;height:18px;background:var(--color-border);margin:0 4px}
+  .letter{width:100%;min-height:240px;padding:16px 20px;border:1.5px solid var(--color-border);border-radius:var(--radius);
+    font-size:13.5px;line-height:2.1;font-family:"Songti SC","STSong","SimSun",serif;background:#fff;outline:none;overflow:auto}
+  .letter p{margin:0 0 10px;text-indent:2em}
+  .letter .ltr-title{text-align:center;font-weight:800;font-size:19px;margin-bottom:16px;letter-spacing:1px;text-indent:0}
+  .letter .noind{text-indent:0}
+  .letter .sig{text-align:right;text-indent:0}
+  .letter:focus{border-color:var(--color-primary);box-shadow:0 0 0 3px var(--color-primary-50)}
+  .letter.ro{background:var(--color-surface-50);color:#334155}
+  .reply-preview{border:1px dashed var(--color-border);border-radius:var(--radius);padding:16px;background:var(--color-surface-50);font-size:12.5px;line-height:1.9;max-height:260px;overflow:auto}
+
+  /* 配合度圆标 */
+  .coop-opt{display:inline-flex;align-items:center;gap:7px;border:1.5px solid var(--color-border);border-radius:999px;padding:6px 16px 6px 8px;cursor:pointer;font-weight:700;margin-right:8px;background:#fff;font-size:13px}
+  .cico{width:20px;height:20px;border-radius:50%;display:inline-flex;align-items:center;justify-content:center;color:#fff;font-size:12px;font-weight:800;flex:none}
+  .coop-opt.on-好{border-color:var(--color-success);color:var(--color-success);background:var(--color-success-50)}
+  .coop-opt.on-中{border-color:var(--color-warning);color:var(--color-warning);background:var(--color-warning-50)}
+  .coop-opt.on-差{border-color:var(--color-danger);color:var(--color-danger);background:var(--color-danger-50)}
+
+  /* 归档闸门 */
+  .gate-item{display:flex;align-items:center;gap:8px;padding:8px 0;font-size:13px}
+  .gate-ok{color:var(--color-success)} .gate-no{color:var(--color-danger)}
+  .path-tree{background:#0F172A;color:#E2E8F0;border-radius:var(--radius);padding:14px 16px;font-family:var(--font-mono);font-size:12px;line-height:1.7;overflow-x:auto}
+  .path-tree .ok{color:#4ADE80}.path-tree .dim{color:#64748B}.path-tree .warn{color:#FBBF24}
+
+  /* 右栏 */
+  .rail{display:flex;flex-direction:column;gap:16px;position:sticky;top:16px}
+  .student-card .row{display:flex;justify-content:space-between;gap:10px;padding:7px 0;border-bottom:1px dashed var(--color-border);font-size:12.5px}
+  .student-card .row:last-child{border:none}
+  .student-card .k{color:var(--color-text-secondary);flex:none}
+  .student-card .v{text-align:right;word-break:break-all}
+  .tl-item{position:relative;padding:0 0 10px 18px;font-size:12px;line-height:1.65}
+  .tl-item:before{content:'';position:absolute;left:4px;top:6px;width:7px;height:7px;border-radius:50%;background:var(--color-primary)}
+  .tl-item:last-child{padding-bottom:0}
+  .tl-item .d{color:var(--color-text-secondary);font-family:var(--font-mono);font-size:11px;margin-right:6px}
+  .mini-case{padding:10px 14px;border-bottom:1px solid var(--color-border);cursor:pointer}
+  .mini-case:last-child{border-bottom:none}
+  .mini-case:hover{background:var(--color-surface-50)}
+  .mini-case.on{background:var(--color-primary-50)}
+  .mini-case .l1{display:flex;align-items:center;gap:8px;font-weight:700;font-size:13.5px}
+  .mini-case .l1 .uc{font-size:11px;color:var(--color-text-secondary);font-weight:600}
+  .mini-case .l2{font-family:var(--font-mono);font-size:11px;color:var(--color-text-secondary);margin-top:2px}
+  .acc-h{display:flex;align-items:center;gap:8px;padding:12px 16px;font-weight:700;font-size:13px;cursor:pointer;user-select:none}
+  .acc-h .arr{margin-left:auto;transition:transform .15s;color:var(--color-text-secondary)}
+  .acc-h.open .arr{transform:rotate(90deg)}
+
+  /* Toast */
+  .toast-container{position:fixed;top:20px;right:20px;z-index:5000;display:flex;flex-direction:column;gap:8px}
+  .toast{padding:12px 16px;border-radius:10px;box-shadow:var(--shadow-lg);color:#fff;font-size:13px;max-width:360px}
+  .toast-success{background:var(--color-success)} .toast-danger{background:var(--color-danger)} .toast-info{background:var(--color-primary)}
+  .toast-title{font-weight:700;margin-bottom:2px}
+
+  @media(max-width:1100px){.wb{grid-template-columns:minmax(0,1fr)}.rail{position:static}}
+  @media(max-width:900px){.sidebar{width:64px;padding:12px 4px}.sidebar .brand small,.nav-link span.txt,.sidebar .footer{display:none}
+    .query-form{grid-template-columns:1fr 1fr}.grid-2,.grid-3{grid-template-columns:1fr}}
+  /* ≤640px：侧栏转为顶部横向导航；表格横向滚动避免整页溢出 */
+  @media(max-width:640px){
+    .app-shell{flex-direction:column}
+    .sidebar{width:100%;height:auto;position:static;flex-direction:row;align-items:center;gap:4px;padding:8px 10px;overflow-x:auto}
+    .sidebar .brand{display:none}
+    .nav-link{flex:none;width:auto;white-space:nowrap;margin-bottom:0;padding:8px 12px;font-size:13px}
+    .sidebar .nav-link span.txt{display:inline}
+    .sidebar .footer{display:none}
+    .main{padding:var(--sp-4) var(--sp-3)}
+    .query-form{grid-template-columns:1fr}
+    .grid-2,.grid-3{grid-template-columns:1fr}
+    .kpi-row{grid-template-columns:repeat(auto-fit,minmax(140px,1fr))}
+    .table,.case-table{display:block;overflow-x:auto;white-space:nowrap}
+    .intake-banner{flex-direction:column;align-items:flex-start}
+    .intake-banner .btn{width:100%;justify-content:center}
+  }
+
+  /* ── 看板增强：筛选条 / KPI徽标 / 图表网格 / 明细分页 ── */
+  .kb-filter{position:sticky;top:8px;z-index:40;display:flex;align-items:center;gap:16px;flex-wrap:wrap;background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:10px 14px;margin-bottom:16px;box-shadow:var(--shadow-sm)}
+  .kb-group{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
+  .kb-label{font-size:12px;color:var(--color-text-secondary);white-space:nowrap}
+  .kb-seg{display:flex;background:var(--color-surface-50,#F2F3F5);border-radius:8px;padding:2px}
+  .kb-seg button{border:none;background:none;padding:5px 13px;border-radius:6px;font-size:13px;color:var(--color-text-secondary);cursor:pointer}
+  .kb-seg button.active{background:#fff;color:var(--color-primary);font-weight:600;box-shadow:0 1px 4px rgba(31,35,41,.12)}
+  .drill-chip{display:inline-flex;align-items:center;gap:6px;background:var(--color-primary-50);color:var(--color-primary);border:1px solid #BCD0FF;border-radius:20px;padding:3px 12px;font-size:12px;font-weight:600;cursor:pointer}
+  .mom-tag{font-size:11px;font-weight:600;border-radius:4px;padding:1px 6px;margin-left:6px;vertical-align:middle}
+  .mom-up{background:#FEECEB;color:#D54941}.mom-down{background:var(--color-success-50);color:var(--color-success)}
+  .overdue-pill{display:inline-flex;align-items:center;gap:4px;font-size:11px;background:#FEECEB;color:#D54941;border-radius:10px;padding:1px 8px;margin-left:8px;vertical-align:middle}
+  .sub-num{font-size:12px;color:var(--color-text-secondary);font-weight:400;margin-left:4px}
+  .kb-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(380px,1fr));gap:16px;margin-top:16px}
+  .rank-tabs{display:inline-flex;background:var(--color-surface-50,#F2F3F5);border-radius:6px;padding:2px}
+  .rank-tabs button{border:none;background:none;font-size:12px;padding:3px 10px;border-radius:4px;color:var(--color-text-secondary);cursor:pointer}
+  .rank-tabs button.active{background:#fff;color:var(--color-primary);font-weight:600}
+  .mini-list{display:flex;flex-direction:column;gap:14px;padding-top:6px}
+  .mini-row{display:flex;justify-content:space-between;font-size:13px;margin-bottom:4px}
+  .mini-track{flex:1;height:8px;background:var(--color-surface-50,#F2F3F5);border-radius:4px;overflow:hidden}
+  .mini-fill{height:100%;background:linear-gradient(90deg,#93C5FD,var(--color-primary));border-radius:4px;transition:width .3s}
+  .prev-fill{background:#C9CFD6}
+  .cmp-row{display:flex;align-items:center;gap:12px;padding:8px 0;border-bottom:1px dashed var(--color-border)}
+  .cmp-row:last-child{border-bottom:none}
+  .cmp-label{width:84px;font-size:13px;font-weight:600;line-height:1.3;flex-shrink:0}
+  .cmp-bars{flex:1;display:flex;flex-direction:column;gap:4px;min-width:0}
+  .cmp-line{display:flex;align-items:center;gap:8px}
+  .cmp-tag{font-size:10px;width:30px;text-align:center;border-radius:4px;flex-shrink:0}
+  .cmp-tag.cur{background:var(--color-primary-50);color:var(--color-primary);font-weight:600}
+  .cmp-tag.prev{background:var(--color-surface-50,#F2F3F5);color:var(--color-text-secondary)}
+  .cmp-line .mini-track{flex:1}
+  .cmp-line b{width:52px;text-align:right;font-size:12.5px;font-variant-numeric:tabular-nums;flex-shrink:0}
+  .cmp-delta{width:86px;text-align:right;font-size:13px;font-weight:700;display:flex;align-items:center;justify-content:flex-end;gap:3px;flex-shrink:0}
+  .delta-bad{color:#D54941}.delta-good{color:var(--color-success)}.delta-flat{color:var(--color-text-secondary)}
+  .cmp-empty{text-align:center;padding:70px 0;color:var(--color-text-secondary);font-size:13px}
+  .cmp-empty i{font-size:22px;display:block;margin-bottom:8px}
+  .kb-toolbar{display:flex;align-items:center;gap:10px;padding:12px 14px;border-bottom:1px solid var(--color-border);flex-wrap:wrap}
+  .kb-search{max-width:300px}
+  .kb-seq{color:var(--color-text-secondary);font-size:12px;font-variant-numeric:tabular-nums}
+  .kb-idcard{font-family:ui-monospace,Menlo,monospace;font-size:12.5px;color:var(--color-text-secondary);letter-spacing:.3px}
+  .kb-muted{color:var(--color-text-secondary)}
+  .repeat-flag{color:#D54941;margin-left:4px;font-size:12px}
+  .kb-pager{display:flex;align-items:center;gap:8px;padding:12px 14px;flex-wrap:wrap}
+  .pg-btn{min-width:30px;height:30px;border:1px solid var(--color-border);background:#fff;border-radius:6px;font-size:13px;color:var(--color-text-secondary);cursor:pointer;padding:0 8px}
+  .pg-btn.on{background:var(--color-primary);color:#fff;border-color:var(--color-primary)}
+  .pg-btn:disabled{opacity:.4;cursor:not-allowed}
+  .veh-table td{vertical-align:middle;padding:6px 8px}
+
+  /* ===== 投诉列表重设计（对齐 demo/list_demo.html）===== */
+  .kpi.cl-kpi{cursor:pointer;border:1.5px solid transparent;transition:border-color .15s}
+  .kpi.cl-kpi:hover{border-color:var(--color-border)}
+  .kpi.cl-kpi.on{border-color:var(--color-primary)}
+  .overdue-sub{margin-left:8px;font-size:11px;font-weight:700;color:var(--color-danger);background:var(--color-danger-50);border-radius:999px;padding:2px 8px;vertical-align:middle}
+  .cl-toolbar{background:var(--color-surface);border-radius:var(--radius-lg);box-shadow:var(--shadow-sm);padding:12px 16px;margin-bottom:16px;display:flex;flex-direction:column;gap:10px}
+  .cl-row{display:flex;align-items:center;gap:10px;flex-wrap:wrap}
+  .search-wrap{position:relative;flex:1;min-width:220px;max-width:340px}
+  .search-wrap i{position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--color-text-secondary);font-size:13px}
+  .search-wrap input{width:100%;padding:8px 10px 8px 32px;font-size:13px;border:1.5px solid var(--color-border);border-radius:var(--radius);outline:none;background:var(--color-surface-50)}
+  .search-wrap input:focus{border-color:var(--color-primary);background:#fff;box-shadow:none}
+  select.cl-sel{max-width:170px;padding:7px 10px;font-size:12px;border:1.5px solid var(--color-border);border-radius:var(--radius);background:#fff;cursor:pointer;color:var(--color-text)}
+  .divider-v{width:1px;height:20px;background:var(--color-border)}
+  .chk-label{display:flex;align-items:center;gap:6px;font-size:12px;color:var(--color-text-secondary);cursor:pointer;user-select:none}
+  .grp-tight{margin-bottom:14px}
+  .grp-card{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);box-shadow:var(--shadow-sm);overflow:hidden}
+  .grp-head-bar{display:flex;align-items:center;gap:10px;padding:10px 16px;border-bottom:1px solid var(--color-border);cursor:pointer;user-select:none;background:var(--color-surface-50)}
+  .grp-head-bar:hover{filter:brightness(.985)}
+  .grp-head-bar .g-num{font-weight:700;font-size:13px}
+  .grp-head-bar .arr{transition:transform .15s;color:var(--color-text-secondary)}
+  .grp.collapsed .case-table,.grp.collapsed .empty-hint{display:none}
+  .grp.collapsed .grp-head-bar .arr{transform:rotate(-90deg)}
+  .badge-overdue{background:var(--color-danger)!important;color:#fff!important;border-color:var(--color-danger)!important}
+  .case-table{width:100%;border-collapse:collapse;font-size:12.5px}
+  .case-table thead th{text-align:left;font-size:11.5px;font-weight:600;color:var(--color-text-secondary);padding:8px 12px;border-bottom:1px solid var(--color-border);white-space:nowrap;background:#fff}
+  .case-table th.sortable{cursor:pointer}
+  .case-table th.sortable:hover{color:var(--color-primary)}
+  .case-table th.sorted,.case-table th.sorted .sort-icon{color:var(--color-primary);opacity:1}
+  .sort-icon{opacity:.4;font-size:10px;margin-left:2px}
+  .case-table tbody td{padding:9px 12px;border-top:1px solid var(--color-border);white-space:nowrap;vertical-align:middle}
+  .case-table tbody tr.rowlink{cursor:pointer}
+  .case-table tbody tr.rowlink:hover{background:var(--color-primary-50)}
+  .case-table tr.overdue{box-shadow:inset 3px 0 0 var(--color-danger);background:#FFF7F7}
+  .case-table tr.overdue:hover{background:#FFEEEE}
+  .case-table tr.selrow td{background:var(--color-primary-50)}
+  .case-table tr.overdue.selrow td{background:#FFE9E9}
+  .case-table .sm{font-size:11.5px;color:var(--color-text-secondary)}
+  .case-table td.r,.case-table th.r{text-align:right}
+  .days-warn{color:var(--color-danger);font-weight:700}
+  .fresh{color:var(--color-success);font-weight:600}
+  .money-cell{font-weight:700}
+  .tpill{display:inline-block;font-size:11px;font-weight:600;border-radius:6px;padding:1px 8px}
+  .tp-A{color:#B91C1C;background:#FEF2F2}.tp-B{color:#1D4ED8;background:#EFF6FF}
+  .tp-C{color:#047857;background:#ECFDF5}.tp-D{color:#B45309;background:#FFFBEB}.tp-E{color:#475569;background:#F1F5F9}
+  .outcome-txt{font-size:11.5px;color:var(--color-text-secondary)}
+  .go{float:right;color:var(--color-text-secondary)}
+  .chk{width:15px;height:15px;cursor:pointer;accent-color:var(--color-primary)}
+  .col-chk{width:34px}
+  .empty-hint{padding:24px;text-align:center;color:var(--color-text-secondary);font-size:12.5px}
+  .batch-bar{position:fixed;left:50%;bottom:28px;transform:translateX(-50%) translateY(90px);display:flex;gap:10px;align-items:center;background:rgba(15,23,42,.95);color:#fff;padding:10px 20px;border-radius:999px;box-shadow:var(--shadow-lg);transition:transform .25s;z-index:4000}
+  .batch-bar.show{transform:translateX(-50%) translateY(0)}
+  .batch-bar .btn-batch{background:transparent;color:#fff;border:1px solid rgba(255,255,255,.35)}
+  .batch-bar .btn-batch:hover{background:rgba(255,255,255,.12);border-color:#fff}
+  .batch-bar .vline{width:1px;height:18px;background:rgba(255,255,255,.25)}
+  @media(max-width:1100px){.hide-md{display:none}}
+  </style>
 </head>
 <body>
   <div id="app">
-    <!-- ═══ 导航栏 ═══ -->
-    <nav class="navbar">
-      <div class="navbar-brand"><i class="bi bi-shield-check"></i> </div>
-      <div class="nav-links">
-        <a class="nav-link" :class="{active: page==='query'}" @click="goPage('query')"><i class="bi bi-search"></i> 工单查询</a>
-        <a class="nav-link" :class="{active: page==='process'}" @click="goPage('process')"><i class="bi bi-gear"></i> 工单处理</a>
-        <a class="nav-link" :class="{active: page==='history'}" @click="goPage('history')"><i class="bi bi-clock-history"></i> 处理历史</a>
-        <a class="nav-link" :class="{active: page==='settings'}" @click="goPage('settings')"><i class="bi bi-gear"></i> 系统设置</a>
-      </div>
-      <div class="navbar-footer">v2.0 · 投诉处理系统</div>
-    </nav>
-
-    <div class="container">
+    <div class="app-shell">
+      <!-- ═══ 左侧导航栏 ═══ -->
+      <aside class="sidebar">
+        <div class="brand">投诉系统<small>v2 智能工作台</small></div>
+        <button class="nav-link" :class="{active: view==='intake'}" @click="goView('intake')"><i class="bi bi-plus-circle"></i> <span class="txt">新增投诉</span></button>
+        <button class="nav-link" :class="{active: view==='workbench'}" @click="goView('workbench')"><i class="bi bi-person-badge"></i> <span class="txt">案件工作台</span></button>
+        <button class="nav-link" :class="{active: view==='list'}" @click="goView('list')"><i class="bi bi-list-ul"></i> <span class="txt">投诉列表</span></button>
+        <button class="nav-link" :class="{active: view==='kanban'}" @click="goView('kanban')"><i class="bi bi-bar-chart"></i> <span class="txt">投诉统计</span></button>
+        <div class="footer">[[ currentDate ]]</div>
+      </aside>
+
+      <main class="main">
       <!-- ════════════════════════════════════════ -->
-      <!-- 工单查询页面（轻：只做查询入口） -->
+      <!-- 视图一：投诉列表（KPI 过滤 + 工具栏 + 分组表格 + 批量操作） -->
       <!-- ════════════════════════════════════════ -->
-      <div v-if="page==='query'" class="fade-in">
-        <div class="page-title"><i class="bi bi-search text-primary"></i> 工单查询</div>
-
-        <!-- 第一步：投诉查询 -->
-        <div class="card">
-          <div class="card-header"><i class="bi bi-search text-primary"></i> 投诉查询</div>
-          <div class="card-body">
-            <!-- 投诉查询：文件上传图标 + 文本输入 一体 -->
-            <div class="intake-row"
-              @dragover="onDragOver"
-              @dragenter="onDragEnter"
-              @dragleave="onDragLeave"
-              @drop="onDrop"
-              :class="{'drag-over': isDragOver}">
-              <!-- 上传图标 -->
-              <div class="intake-icon" @click="triggerFileInput" title="点击上传文件">
-                <i v-if="!intakeLoading && !intakeResult" class="bi bi-paperclip"></i>
-                <i v-if="intakeLoading" class="bi bi-arrow-repeat spin"></i>
-                <i v-if="intakeResult && !intakeLoading" class="bi bi-check-circle-fill" style="color:var(--green)"></i>
-              </div>
-              <!-- 文本输入（支持粘贴图片） -->
-              <textarea class="form-control intake-input"
-                v-model="intakeText"
-                placeholder="粘贴投诉内容、截图，或拖拽文件至此..."
-                rows="1"
-                @input="autoResizeTextarea"
-                @paste="onIntakePaste"
-                ref="intakeTextareaRef"></textarea>
-              <!-- 提取按钮 -->
-              <button class="btn btn-primary btn-sm" @click="handleIntakeText"
-                :disabled="intakeLoading || !intakeText.trim()">
-                [[ intakeLoading ? '提取中...' : '提取线索' ]]
-              </button>
-              <!-- 隐藏文件输入 -->
-              <input type="file" ref="fileInputRef"
-                accept=".pdf,.png,.jpg,.jpeg,.docx,.xlsx,.txt"
-                @change="onFileSelected" style="display:none">
-            </div>
-            <!-- 提示语 -->
-            <div class="intake-hint">
-              支持上传 PDF / 图片 / Word / Excel / 文本文件，或直接输入/粘贴投诉内容（Ctrl+V 粘贴图片也可识别）
-            </div>
-
-            <!-- 提取结果预览 -->
-            <div v-if="intakeResult && !intakeLoading" class="alert alert-success mt-2 fade-in" style="margin-bottom:0">
-              <i class="bi bi-info-circle"></i>
-              已识别查询线索
-              <span v-if="intakeResult.id_card"> | 身份证: [[ intakeResult.id_card ]]</span>
-              <span v-if="intakeResult.phone"> | 手机号: [[ intakeResult.phone ]]</span>
-              </div>
-            <div v-if="intakeErr" class="alert alert-danger mt-2">[[ intakeErr ]]</div>
+      <section v-if="view==='list'" class="fade-in">
+        <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
+          <div class="page-title" style="margin-bottom:0">投诉列表</div>
+          <span class="hint">口径：投诉即入账 · 撤诉不计有效投诉 · 在途超 [[ OVERDUE_DAYS ]] 天标红置顶</span>
+          <span style="flex:1"></span>
+          <button class="btn btn-secondary btn-sm" @click="loadTickets()"><i class="bi bi-arrow-clockwise"></i> 刷新</button>
+        </div>
 
-            <hr style="margin:20px 0; border:none; border-top:2px solid var(--gray-200)">
+        <div class="kpi-row">
+          <div class="kpi cl-kpi" :class="{on: clGroup==='all'}" @click="clGroup='all'">
+            <div class="num">[[ allTickets.length ]]</div><div class="lbl">累计投诉（投诉即入账）</div>
+          </div>
+          <div class="kpi cl-kpi" :class="{on: clGroup==='open'}" @click="clGroup='open'">
+            <div class="num" style="color:var(--color-primary)">[[ ticketsOpen.length ]]<span v-if="clOverdueTotal" class="overdue-sub">⚠ 超时 [[ clOverdueTotal ]]</span></div>
+            <div class="lbl">在途（待处理+处理中）</div>
+          </div>
+          <div class="kpi cl-kpi" :class="{on: clGroup==='archived'}" @click="clGroup='archived'">
+            <div class="num" style="color:var(--color-success)">[[ ticketsArchived.length ]]</div><div class="lbl">已归档</div>
+          </div>
+          <div class="kpi cl-kpi" :class="{on: clGroup==='withdrawn'}" @click="clGroup='withdrawn'; clCollapsed.withdrawn=false">
+            <div class="num">[[ ticketsWithdrawn.length ]]</div><div class="lbl">已撤诉<span class="hint"> 不计有效投诉</span></div>
+          </div>
+        </div>
 
-            <!-- 查询表单：紧凑布局 -->
-            <div class="query-form">
-              <div>
-                <label class="form-label">身份证号</label>
-                <input type="text" class="form-control" v-model="form.id_card" placeholder="大陆身份证/港澳/台湾/居留证（支持特殊格式）">
-              </div>
-              <div>
-                <label class="form-label">手机号</label>
-                <input type="text" class="form-control" v-model="form.phone" placeholder="11位" maxlength="11">
-              </div>
-              <div>
-                <label class="form-label">来源渠道</label>
-                <select class="form-control" v-model="form.source_channel" @change="onSourceChange">
-                  <option value="12345">12345</option>
-                  <option value="交通部门">交通部门</option>
-                  <option value="电话来访">电话来访</option>
-                  <option value="信访">信访</option>
-                  <option value="邮件投诉">邮件投诉</option>
-                  <option value="驾培协会">驾培协会</option>
-                  <option value="其他途径">其他途径</option>
-                </select>
-              </div>
-              <div v-if="form.source_channel === '其他途径'">
-                <label class="form-label">具体途径</label>
-                <input type="text" class="form-control" v-model="form.other_channel" placeholder="请填写具体渠道">
-              </div>
-              <div>
-                <label class="form-label">投诉日期</label>
-                <input type="date" class="form-control" v-model="form.complaint_date">
-              </div>
-              <div>
-                <label class="form-label">投诉类型</label>
-                <select class="form-control" v-model="form.complaint_type">
-                  <option value="A">A-退费纠纷</option>
-                  <option value="B">B-教学服务</option>
-                  <option value="C">C-考试安排</option>
-                  <option value="D">D-合同争议</option>
-                  <option value="E">E-其他</option>
-                </select>
-              </div>
-              <div>
-                <label class="form-label">处理人</label>
-                <input type="text" class="form-control" v-model="form.handler_name" placeholder="请输入处理人">
-              </div>
-              <div>
-                <label class="form-label">投诉事实简述</label>
-                <textarea class="form-control" v-model="form.complaint_summary" rows="2" placeholder="简要记录投诉事实"></textarea>
-              </div>
-              <div style="display:flex;align-items:flex-end;padding-bottom:2px">
-                <button class="btn btn-primary btn-lg" @click="queryAllAndGo" :disabled="querying">
-                  <span v-if="querying" class="spinner-custom spinner-custom-sm me-1"></span>
-                  <i v-else class="bi bi-search"></i>
-                  [[ querying ? '查询中...' : '查询三系统' ]]
-                </button>
-              </div>
+        <div class="cl-toolbar">
+          <div class="cl-row">
+            <div class="search-wrap">
+              <i class="bi bi-search"></i>
+              <input v-model.trim="clKw" placeholder="搜索 姓名 / 工单号 / 手机号 / 身份证…">
             </div>
+            <select class="cl-sel" v-model="clType">
+              <option value="">类型：全部</option>
+              <option value="A">A-退费纠纷</option><option value="B">B-教学服务</option>
+              <option value="C">C-考试安排</option><option value="D">D-合同争议</option><option value="E">E-其他</option>
+            </select>
+            <select class="cl-sel" v-model="clChannel">
+              <option value="">渠道：全部</option>
+              <option v-for="c in channelOptions" :key="c" :value="c">[[ c ]]</option>
+            </select>
+            <select class="cl-sel" v-model="clHandler">
+              <option value="">处理人：全部</option>
+              <option value="__unassigned">未分配</option>
+              <option v-for="h in handlerOptions" :key="h" :value="h">[[ h ]]</option>
+            </select>
+            <select class="cl-sel" v-model="clFee">
+              <option value="">费用状态：全部</option>
+              <option value="pending">费用待确认</option>
+              <option value="confirmed">费用已确认</option>
+              <option value="none">未核算</option>
+            </select>
+            <span style="flex:1"></span>
+            <button class="btn btn-secondary btn-sm" @click="clClearFilters()"><i class="bi bi-x-circle"></i> 清空筛选</button>
+          </div>
+          <div class="cl-row">
+            <button class="chip" :class="{on: clDays==='all'}" @click="clDays='all'">日期：全部</button>
+            <button class="chip" :class="{on: clDays==='7'}" @click="clDays='7'">近 7 天</button>
+            <button class="chip" :class="{on: clDays==='30'}" @click="clDays='30'">近 30 天</button>
+            <input type="date" class="form-control" style="width:auto;padding:5px 8px;font-size:12px" v-model="clDateFrom">
+            <span class="hint">至</span>
+            <input type="date" class="form-control" style="width:auto;padding:5px 8px;font-size:12px" v-model="clDateTo">
+            <span class="divider-v"></span>
+            <label class="chk-label"><input type="checkbox" v-model="clOnlyOverdue"> 只看超时</label>
+            <span style="flex:1"></span>
+            <span class="hint">共 [[ clResultCount ]] 条</span>
+          </div>
+        </div>
 
-            <!-- 查询进度 -->
-            <div v-if="queryProgress.show" class="progress-card fade-in">
-              <div class="progress-bar-bg">
-                <div class="progress-bar-fill" :style="{width: queryProgress.percent + '%'}"></div>
-              </div>
-              <div class="progress-status">
-                <span v-for="(item, key) in queryProgress.items" :key="key" class="badge" :class="item.class" :title="item.tip || item.label">
-                  <i class="bi" :class="item.icon"></i> [[ item.label ]]<span v-if="item.duration"> · [[ item.duration ]]</span>
-                </span>
-              </div>
-              <div v-if="queryProgress.message || !queryProgress.done" class="d-flex gap-2 align-center flex-wrap" style="font-size:12px">
-                <span :class="queryProgress.done ? (queryProgress.hasError ? 'text-warning' : 'text-success') : 'text-gray'">
-                  <i class="bi" :class="queryProgress.done ? (queryProgress.hasError ? 'bi-exclamation-triangle' : 'bi-check-circle') : 'bi-hourglass text-primary'"></i>
-                  [[ queryProgress.message ]]
-                  <span v-if="!queryProgress.done" style="font-family:mono">
-                    ([[ queryProgress.elapsed ]]s)
-                    <span v-if="queryProgress.elapsed > 15" class="text-warning">系统响应较慢，请耐心等待...</span>
-                    <span v-if="queryProgress.elapsed > 30" class="text-gray">仍在运行中，未卡住...</span>
-                  </span>
-                </span>
-              </div>
+        <template v-for="g in listGroups" :key="g.key">
+          <div v-if="g.items.length || clGroup===g.key" class="grp grp-tight" :class="{collapsed: !!clCollapsed[g.key]}">
+            <div class="grp-card">
+              <div class="grp-head-bar" @click="clCollapsed[g.key]=!clCollapsed[g.key]">
+                <input type="checkbox" class="chk" title="全选本组"
+                       :checked="g.items.length>0 && g.items.every(t=>clSelectedIds.includes(t.id))"
+                       @click.stop @change="clToggleGroupSelect(g)">
+                <span class="badge" :class="g.key==='open' ? 'badge-processing' : (g.key==='archived' ? 'badge-completed' : 'badge-muted')">[[ g.label ]]</span>
+                <span class="g-num">[[ g.items.length ]]</span>
+                <span v-if="g.overdueCount" class="badge badge-overdue"><i class="bi bi-exclamation-triangle-fill"></i> 超时 [[ g.overdueCount ]]</span>
+                <span style="flex:1"></span>
+                <i class="bi bi-chevron-down arr"></i>
+              </div>
+              <table v-if="g.items.length" class="case-table">
+                <thead><tr>
+                  <th class="col-chk"></th>
+                  <th>工单号</th><th>学员</th><th class="hide-md">手机号</th><th>类型</th><th class="hide-md">来源渠道</th>
+                  <th class="sortable" :class="{sorted: clSort.key==='date'}" @click="clSetSort('date')">投诉日期
+                    <i class="bi sort-icon" :class="clSort.key==='date' ? (clSort.dir===1?'bi-sort-up-alt':'bi-sort-down') : 'bi-arrow-down-up'"></i></th>
+                  <th class="sortable" :class="{sorted: clSort.key==='days'}" @click="clSetSort('days')">在途/结局
+                    <i class="bi sort-icon" :class="clSort.key==='days' ? (clSort.dir===1?'bi-sort-up-alt':'bi-sort-down') : 'bi-arrow-down-up'"></i></th>
+                  <th>处理人</th>
+                  <th class="sortable r" :class="{sorted: clSort.key==='refund'}" @click="clSetSort('refund')">应退金额
+                    <i class="bi sort-icon" :class="clSort.key==='refund' ? (clSort.dir===1?'bi-sort-up-alt':'bi-sort-down') : 'bi-arrow-down-up'"></i></th>
+                  <th>费用状态</th><th>状态</th><th></th>
+                </tr></thead>
+                <tbody>
+                  <tr v-for="t in g.items" :key="t.id" class="rowlink"
+                      :class="{overdue: g.key==='open' && isOverdue(t), selrow: clSelectedIds.includes(t.id)}"
+                      @click="openCase(t.id)">
+                    <td class="col-chk" @click.stop><input type="checkbox" class="chk" :checked="clSelectedIds.includes(t.id)" @change="clToggleRow(t.id)"></td>
+                    <td class="mono sm">[[ t.ticket_no || t.id ]]</td>
+                    <td><b>[[ t.student_name || '—' ]]</b> <span class="hint">（[[ t.school_short || '未归属' ]]）</span></td>
+                    <td class="mono sm hide-md">[[ maskPhone(t.phone) ]]</td>
+                    <td><span class="tpill" :class="'tp-'+(t.complaint_type||'E')">[[ t.complaint_type || 'E' ]] [[ TYPE_LABELS[t.complaint_type] || '其他' ]]</span></td>
+                    <td class="hide-md">[[ t.source_channel || '—' ]]</td>
+                    <td class="mono sm">[[ t.complaint_date ]]</td>
+                    <td>
+                      <template v-if="g.key==='open'">
+                        <span :class="isOverdue(t) ? 'days-warn' : (daysOpen(t)<=1 ? 'fresh':'')">[[ daysOpen(t) ]] 天</span>
+                        <span v-if="isOverdue(t)" class="badge badge-overdue" style="margin-left:6px"><i class="bi bi-exclamation-triangle-fill"></i>超 [[ daysOpen(t)-OVERDUE_DAYS ]] 天</span>
+                      </template>
+                      <span v-else-if="g.key==='archived'" class="outcome-txt">[[ t.final_outcome || '已归档' ]]</span>
+                      <span v-else class="outcome-txt">撤诉[[ t.withdraw_reason ? ' · '+t.withdraw_reason : '' ]]</span>
+                    </td>
+                    <td>[[ t.handler_name || '未分配' ]]</td>
+                    <td class="r money-cell">[[ t.refund_fee==null || t.refund_fee==='' ? '—' : '¥'+Number(t.refund_fee).toLocaleString('zh-CN') ]]</td>
+                    <td>
+                      <span v-if="feeState(t)==='confirmed'" class="badge badge-completed">✓ 费用已确认</span>
+                      <span v-else-if="feeState(t)==='pending'" class="badge badge-pending">⏳ 费用待确认</span>
+                      <span v-else class="badge badge-muted">未核算</span>
+                    </td>
+                    <td>
+                      <span v-if="g.key==='open'" class="badge" :class="t.handle_status==='处理中' ? 'badge-processing' : 'badge-muted'">[[ t.handle_status || '待处理' ]]</span>
+                      <i class="bi bi-chevron-right go"></i>
+                    </td>
+                  </tr>
+                </tbody>
+              </table>
+              <div v-else class="empty-hint">暂无案件</div>
             </div>
-
-            <div v-if="qErr" class="alert alert-danger mt-2">[[ qErr ]]</div>
           </div>
+        </template>
+
+        <div v-if="!clResultCount" class="card empty-hint" style="text-align:center;padding:40px">
+          <i class="bi bi-inbox" style="font-size:32px;display:block;margin-bottom:8px;color:var(--color-border)"></i>
+          没有符合条件的案件，试试<a href="#" @click.prevent="clClearFilters()" style="color:var(--color-primary);font-weight:600">清空筛选</a>
         </div>
+
+        <div class="batch-bar" :class="{show: clSelectedIds.length>0}">
+          <b>已选 [[ clSelectedIds.length ]] 条</b>
+          <button class="btn btn-sm btn-batch" @click="clSelectAllShown()">全选当前结果</button>
+          <button class="btn btn-sm btn-batch" @click="clClearSelection()">清除</button>
+          <span class="vline"></span>
+          <button class="btn btn-sm btn-primary" @click="batchExportSelected()"><i class="bi bi-download"></i> 批量导出</button>
+          <button class="btn btn-sm btn-primary" @click="askBatchTransfer()"><i class="bi bi-arrow-left-right"></i> 批量转办</button>
         </div>
+      </section>
 
-        <!-- ═══ 工单处理页面（重：查询结果 + 工作流） ═══ -->
-        <div v-if="page==='process'" class="fade-in">
-          <!-- 面包屑：查询 › 工单 #id › 学员 -->
-          <div class="d-flex align-center justify-between flex-wrap" style="gap:12px;margin-bottom:16px;padding-bottom:12px;border-bottom:1px solid var(--color-border)">
-            <div class="page-title" style="margin:0"><i class="bi bi-gear text-primary"></i> 工单处理</div>
-            <button class="btn btn-sm btn-secondary" @click="goQuery"><i class="bi bi-arrow-left"></i> 返回查询</button>
+      <!-- ════════════════════════════════════════ -->
+      <!-- 视图二：新增投诉（文件/文本 → 解析 → 建案） -->
+      <!-- ════════════════════════════════════════ -->
+      <section v-if="view==='intake'" class="fade-in">
+        <div class="page-title">新增投诉</div>
+        <div v-if="currentTicketId" class="intake-banner" role="alert">
+          <div class="ib-text"><i class="bi bi-exclamation-triangle"></i>
+            正在更新现有工单：<b>[[ qr && qr.name ? qr.name : '该工单' ]]</b>（工单号 …末6位 [[ currentTicketId.slice(-6) ]]）。提交将覆盖该工单；如需新建投诉请点【新建空白投诉】。
           </div>
-
-        <!-- 第二步：学员信息 -->
-        <div v-if="qr && qr.name && page==='process'" class="card fade-in">
-          <div class="card-header d-flex align-center justify-between flex-wrap" style="gap:8px">
-            <div><i class="bi bi-person text-primary"></i> 学员信息 — <strong>[[ qr.name ]]</strong></div>
-            <div class="d-flex gap-2 align-center flex-wrap">
-              <!-- 来源状态：图标 + 文字 -->
-              <span v-if="qr.sources" style="display:inline-flex;gap:10px;font-size:12px;align-items:center">
-                <span :style="{color: sourceStatusColor(qr.sources.internal)}">
-                  <i class="bi" :class="sourceStatusIcon(qr.sources.internal)"></i>
-                  内部：[[ sourceStatusText('internal', qr.sources.internal) ]]<span v-if="qr.query_durations_ms?.internal"> · [[ (qr.query_durations_ms.internal / 1000).toFixed(1) ]]s</span>
-                </span>
-                <span :style="{color: sourceStatusColor(qr.sources.third)}">
-                  <i class="bi" :class="sourceStatusIcon(qr.sources.third)"></i>
-                  第三：[[ sourceStatusText('third', qr.sources.third) ]]<span v-if="qr.query_durations_ms?.third"> · [[ (qr.query_durations_ms.third / 1000).toFixed(1) ]]s</span>
-                </span>
-                <span :style="{color: sourceStatusColor(qr.sources.driving)}">
-                  <i class="bi" :class="sourceStatusIcon(qr.sources.driving)"></i>
-                  驾培：[[ sourceStatusText('driving', qr.sources.driving) ]]<span v-if="qr.query_durations_ms?.driving"> · [[ (qr.query_durations_ms.driving / 1000).toFixed(1) ]]s</span><span v-if="drivingContractTag(qr)" class="text-muted" style="font-size:11px;margin-left:4px">（[[ drivingContractTag(qr) ]]）</span>
-                </span>
-              </span>
-              <span class="badge" :class="stBadge(qr.student_status)">[[ qr.student_status ]]</span>
-              <!-- 合规状态提醒 -->
-              <span v-if="qr.contract_status || qr.skill_cert_status" style="display:inline-flex;gap:6px;font-size:12px;align-items:center;border-left:1px solid var(--gray-200);padding-left:10px;margin-left:2px">
-                <span v-if="qr.contract_status"
-                  :style="{color: qr.contract_status==='expired' ? '#dc2626' : '#16a34a', fontWeight:500}">
-                  <i :class="qr.contract_status==='expired' ? 'bi-x-circle-fill' : 'bi-check-circle-fill'"></i>
-                  合同：[[ qr.contract_status==='expired' ? '已过期' : '有效' ]]
-                </span>
-                <span v-if="qr.skill_cert_status"
-                  :style="{color: qr.skill_cert_status==='expired' ? '#dc2626' : qr.skill_cert_status==='no_subject1' ? '#9ca3af' : '#16a34a', fontWeight:500}">
-                  <i :class="qr.skill_cert_status==='expired' ? 'bi-x-circle-fill' : qr.skill_cert_status==='no_subject1' ? 'bi-dash-circle' : 'bi-check-circle-fill'"></i>
-                  技能证：[[ qr.skill_cert_status==='expired' ? '已过期' : qr.skill_cert_status==='no_subject1' ? '未过文科' : '有效' ]]
-                </span>
-              </span>
+          <button class="btn btn-secondary btn-sm" @click="reset()"><i class="bi bi-file-earmark-plus"></i> 新建空白投诉</button>
+        </div>
+        <div class="card panel"><div class="card-header"><i class="bi bi-paperclip text-primary"></i> ① 粘贴或上传投诉材料</div><div class="card-body">
+          <div class="intake-row" @dragover="onDragOver" @dragenter="onDragEnter" @dragleave="onDragLeave" @drop="onDrop" :class="{'drag-over': isDragOver}">
+            <div class="intake-icon" @click="triggerFileInput" title="点击上传文件">
+              <i v-if="!intakeLoading && !intakeResult" class="bi bi-paperclip"></i>
+              <i v-if="intakeLoading" class="bi bi-arrow-repeat spin"></i>
+              <i v-if="intakeResult && !intakeLoading" class="bi bi-check-circle-fill" style="color:var(--color-success)"></i>
             </div>
+            <textarea id="intake-paste-input" aria-label="粘贴投诉内容" class="form-control intake-input" v-model="intakeText" rows="3"
+              placeholder="粘贴投诉内容、截图，或拖拽文件至此..." @input="autoResizeTextarea" @paste="onIntakePaste" ref="intakeTextareaRef"></textarea>
+            <button class="btn btn-primary btn-sm" @click="handleIntakeText" :disabled="intakeLoading || !intakeText.trim()">[[ intakeLoading ? '提取中...' : '提取线索' ]]</button>
+            <input type="file" ref="fileInputRef" accept=".pdf,.png,.jpg,.jpeg,.docx,.xlsx,.txt" @change="onFileSelected" style="display:none">
           </div>
-          <div class="card-body">
-            <div class="tabs">
-              <div v-for="(label, key) in tabs" :key="key" class="tab" :class="{active: tab === key}" @click="tab = key">[[ label ]]</div>
-            </div>
-
-            <!-- 基本信息 -->
-            <div v-show="tab === 'basic'">
-              <div class="info-grid">
-                <div class="info-item"><span class="info-label">姓名</span><span class="info-value">[[ qr.name || '-' ]]</span></div>
-                <div class="info-item"><span class="info-label">身份证</span><span class="info-value font-mono">[[ qr.id_card ]]</span></div>
-                <div class="info-item"><span class="info-label">手机号</span><span class="info-value">[[ qr.phone || '-' ]]</span></div>
-                <div class="info-item"><span class="info-label">车型</span><span class="info-value">[[ qr.license_type || '-' ]]</span></div>
-                <div class="info-item"><span class="info-label">驾校</span><span class="info-value">[[ qr.school_name || '-' ]]<span v-if="qr.school_short" style="color:var(--gray-400);font-size:12px">([[ qr.school_short ]])</span></span></div>
-                <div class="info-item"><span class="info-label">报名日期</span><span class="info-value">[[ qr.registration_date || '-' ]]</span></div>
-                <div class="info-item"><span class="info-label">当前阶段</span><span class="info-value text-primary font-semibold">[[ qr.exam_stage || '-' ]]</span></div>
-                <div class="info-item">
-                  <span class="info-label">技能证时间</span>
-                  <span class="info-value" v-if="qr.skill_cert_status === 'no_subject1'" style="color:var(--gray-400)">未过文科</span>
-                  <span class="info-value" v-else-if="qr.skill_cert_status === 'expired'"><span style="color:var(--red)">[[ qr.skill_cert_date ]]（已过期）</span></span>
-                  <span class="info-value" v-else>[[ qr.skill_cert_date || '-' ]]</span>
-                </div>
-                <!-- 费用概览（AI 分析后显示） -->
-                <template v-if="ar">
-                  <div class="info-item"><span class="info-label">合同金额</span><span class="info-value">[[ ar?.total_fee ? '¥' + ar.total_fee : '-' ]]</span></div>
-                  <div class="info-item"><span class="info-label">实际已交</span><span class="info-value">[[ ar?.actual_paid ? '¥' + ar.actual_paid : '-' ]]</span></div>
-                  <div class="info-item"><span class="info-label">总扣费</span><span class="info-value" style="color:var(--danger)">[[ ar?.total_deduction ? '¥' + ar.total_deduction : '-' ]]</span></div>
-                  <div class="info-item"><span class="info-label">应退金额</span><span class="info-value" style="color:var(--success)">[[ ar?.refund ? '¥' + ar.refund : '-' ]]</span></div>
-                </template>
-              </div>
-            </div>
-
-            <!-- 考试情况 -->
-            <div v-show="tab === 'exam'">
-              <div v-if="qr.exam_counts && Object.keys(qr.exam_counts).length" class="subject-summary-grid exam-summary-grid">
-                <div v-for="(subject, idx) in ['科目一','科目二','科目三','科目四']" :key="subject"
-                  class="subject-summary-card"
-                  :class="{'is-active': (qr.exam_counts[subject] || 0) > 0, 'is-muted': (qr.exam_counts[subject] || 0) === 0}">
-                  <div class="subject-summary-head">
-                    <span class="subject-summary-index">[[ idx + 1 ]]</span>
-                    <strong>[[ subject ]]</strong>
-                  </div>
-                  <div class="subject-summary-main">
-                    <span>[[ qr.exam_counts[subject] || 0 ]]</span><small>次</small>
-                  </div>
-                  <div class="subject-summary-label">考试次数</div>
-                </div>
-              </div>
-              <div v-else class="empty-state"><i class="bi bi-inbox icon"></i><div class="text">暂无考试记录</div></div>
-            </div>
-
-            <!-- 培训时长 -->
-            <div v-show="tab === 'training'">
-              <div v-if="qr.training_details && qr.training_details.length" class="training-duration-grid">
-                <div v-for="(subject, idx) in ['科目二','科目三']" :key="subject" class="training-duration-card">
-                  <div class="training-duration-head">
-                    <span class="subject-summary-index">[[ idx + 2 ]]</span>
-                    <strong>[[ subject ]]</strong>
-                  </div>
-                  <div class="subject-summary-metrics">
-                    <div>
-                      <span class="metric-label">总时长</span>
-                      <strong>[[ getTrainingTime(qr.training_details, subject, 'training_time') ]]</strong>
-                    </div>
-                    <div>
-                      <span class="metric-label">共享时长</span>
-                      <strong class="text-success">[[ getTrainingTime(qr.training_details, subject, 'platform_time') ]]</strong>
-                    </div>
-                  </div>
-                </div>
-              </div>
-              <div v-else class="empty-state"><i class="bi bi-inbox icon"></i><div class="text">该学员在计时培训管理平台无培训记录</div></div>
+          <div class="hint" style="margin-top:8px">支持上传 PDF / 图片 / Word / Excel / 文本文件，或直接输入/粘贴投诉内容（Ctrl+V 粘贴图片也可识别）。自动提取仅身份证号与手机号。</div>
+          <div v-if="intakeResult && !intakeLoading" class="clues">
+            <span class="clues-head"><i class="bi bi-check-circle-fill"></i> 已识别查询线索</span>
+            <span v-if="intakeResult.id_card" class="clue-chip">身份证 <b>[[ intakeResult.id_card ]]</b></span>
+            <span v-if="intakeResult.phone" class="clue-chip">手机号 <b>[[ intakeResult.phone ]]</b></span>
+            <span v-if="!intakeResult.id_card && !intakeResult.phone" class="hint">未识别到身份证号或手机号，请在下方手动填写</span>
+          </div>
+          <div v-if="intakeErr" class="alert alert-danger mt-2">[[ intakeErr ]]</div>
+        </div></div>
+
+        <div class="card panel" style="margin-top:16px"><div class="card-header"><i class="bi bi-card-checklist text-primary"></i> ② 登记投诉信息</div><div class="card-body">
+          <div>
+            <div class="fgroup-title" style="margin-top:16px"><i class="bi bi-person-badge"></i> 学员身份</div>
+            <div class="query-form" style="margin-top:0">
+              <div><label class="form-label" for="f_id_card">身份证号<span class="req">*</span></label><input id="f_id_card" class="form-control" :class="{pulse:pulseIdCard}" v-model="form.id_card" placeholder="大陆身份证/港澳/台湾/居留证"></div>
+              <div><label class="form-label" for="f_phone">手机号<span class="req">*</span></label><input id="f_phone" class="form-control" :class="{pulse:pulsePhone}" v-model="form.phone" placeholder="11位" maxlength="11"></div>
             </div>
-
-            <!-- 收费记录（东莞驾培） -->
-            <div v-show="tab === 'fees'">
-              <template v-if="qr.driving_fee && (qr.driving_fee.contract_fee > 0 || (qr.driving_fee.orders && qr.driving_fee.orders.length))">
-                <div class="analysis-cards" style="margin-bottom:12px">
-                  <div class="analysis-card">
-                    <div class="analysis-label">监管日期</div>
-                    <div class="analysis-value">[[ qr.driving_fee.supervise_date ? qr.driving_fee.supervise_date.slice(0, 10) : '-' ]]</div>
-                  </div>
-                  <div class="analysis-card">
-                    <div class="analysis-label">合同金额</div>
-                    <div class="analysis-value">[[ qr.driving_fee.contract_fee ]]元</div>
-                  </div>
-                  <div class="analysis-card">
-                    <div class="analysis-label">监管资金</div>
-                    <div class="analysis-value">[[ qr.driving_fee.supervise_fee ]]元</div>
-                  </div>
-                  <div class="analysis-card">
-                    <div class="analysis-label">监管余额</div>
-                    <div class="analysis-value">[[ qr.driving_fee.residue_supervise_amt ]]元</div>
-                  </div>
-                </div>
-
-                <div class="analysis-section-head"><strong>费用构成</strong></div>
-                <div class="table-wrap mb-2">
-                  <table><thead><tr><th>项目</th><th>金额</th></tr></thead>
-                    <tbody>
-                      <tr v-for="(item, i) in getDrivingFeeBreakdown(qr.driving_fee.breakdown)" :key="i">
-                        <td>[[ item.label ]]</td>
-                        <td class="font-semibold">[[ item.value > 0 ? item.value + ' ' + item.unit : '-' ]]</td>
-                      </tr>
-                    </tbody>
-                  </table>
-                </div>
-
-                <div class="analysis-section-head"><strong>交费订单（[[ qr.driving_fee.orders ? qr.driving_fee.orders.length : 0 ]]笔）</strong></div>
-                <div class="table-wrap">
-                  <table><thead><tr><th>订单号</th><th>支付时间</th><th>交易金额</th><th>科二科三监管费</th><th>手续费</th><th>状态</th></tr></thead>
-                    <tbody>
-                      <tr v-for="(o, i) in qr.driving_fee.orders" :key="i">
-                        <td>[[ o.order_no || '-' ]]</td>
-                        <td>[[ o.pay_time || o.order_time || '-' ]]</td>
-                        <td class="font-semibold">[[ o.total_fee > 0 ? o.total_fee : '-' ]]</td>
-                        <td>[[ o.supervise_fee > 0 ? o.supervise_fee : '-' ]]</td>
-                        <td>[[ o.commission_fee > 0 ? o.commission_fee : '-' ]]</td>
-                        <td>[[ o.is_pay === 1 ? '已支付' : o.is_pay === 2 ? '未支付' : '-' ]]</td>
-                      </tr>
-                    </tbody>
-                  </table>
-                </div>
-              </template>
-              <div v-else class="empty-state"><i class="bi bi-inbox icon"></i><div class="text">东莞驾培暂无收费记录</div></div>
+          </div>
+          <div>
+            <div class="fgroup-title"><i class="bi bi-journal-bookmark"></i> 工单属性</div>
+            <div class="query-form" style="margin-top:0">
+              <div><label class="form-label" for="f_source">来源渠道</label><select id="f_source" class="form-control" v-model="form.source_channel" @change="onSourceChange">
+                <option value="12345">12345</option><option value="交通部门">交通部门</option><option value="电话来访">电话来访</option>
+                <option value="信访">信访</option><option value="邮件投诉">邮件投诉</option><option value="驾培协会">驾培协会</option><option value="其他途径">其他途径</option>
+              </select></div>
+              <div v-if="form.source_channel==='其他途径'"><label class="form-label" for="f_other_channel">具体途径</label><input id="f_other_channel" class="form-control" v-model="form.other_channel" placeholder="请填写具体渠道"></div>
+              <div><label class="form-label" for="f_date">投诉日期</label><input id="f_date" type="date" class="form-control" v-model="form.complaint_date"></div>
+              <div><label class="form-label" for="f_type">投诉类型</label><select id="f_type" class="form-control" v-model="form.complaint_type">
+                <option value="A">A-退费纠纷</option><option value="B">B-教学服务</option><option value="C">C-考试安排</option>
+                <option value="D">D-合同争议</option><option value="E">E-其他</option>
+              </select></div>
+              <div><label class="form-label" for="f_handler">处理人</label><input id="f_handler" class="form-control" v-model="form.handler_name" placeholder="请输入处理人"></div>
             </div>
-
-            <!-- 时间轴 -->
-            <div v-show="tab === 'timeline'">
-              <div v-if="qr.timeline_display && qr.timeline_display.length" class="timeline">
-                <div v-for="(event, i) in qr.timeline_display" :key="i" class="timeline-item" :class="event.status">
-                  <div class="timeline-node"></div>
-                  <div class="timeline-content-wrapper">
-                    <div class="timeline-content">
-                      <span class="timeline-date">[[ event.date ]]</span>
-                      <span class="timeline-event">[[ event.title ]]</span>
-                    </div>
-                  </div>
-                </div>
-              </div>
-              <div v-else class="empty-state"><i class="bi bi-inbox icon"></i><div class="text">暂无时间轴记录</div></div>
+          </div>
+          <div style="display:flex;gap:10px;margin-top:16px;align-items:center">
+            <button class="btn btn-primary" @click="queryAllAndGo" :disabled="querying">
+              <span v-if="querying" class="spinner-custom spinner-custom-sm me-1"></span>
+              <i v-else class="bi bi-search"></i> [[ querying ? '查询三系统并建案...' : '查询三系统并建案' ]]
+            </button>
+            <span class="hint">建案后立即计入统计，并自动进入工作台</span>
+          </div>
+          <div v-if="queryProgress.show" class="progress-card">
+            <div class="progress-bar-bg"><div class="progress-bar-fill" :style="{width: queryProgress.percent + '%'}"></div></div>
+            <div class="progress-status">
+              <span v-for="(item, key) in queryProgress.items" :key="key" class="badge" :class="item.class"><i class="bi" :class="item.icon"></i> [[ item.label ]]<span v-if="item.duration"> · [[ item.duration ]]</span></span>
             </div>
+            <div v-if="queryProgress.message" class="hint" style="margin-top:6px">[[ queryProgress.message ]] <span v-if="!queryProgress.done">([[ queryProgress.elapsed ]]s)</span></div>
           </div>
-        </div>
+          <div v-if="qErr" class="alert alert-danger mt-2">[[ qErr ]]</div>
+        </div></div>
+      </section>
 
-        <!-- 第三步：处理流程 -->
-        <div v-if="qr && qr.name && page==='process'" class="card fade-in">
-          <div class="card-header d-flex align-center justify-between">
-            <div><i class="bi bi-gear text-primary"></i> 处理流程</div>
-            <span class="badge" :class="workflowStep === 1 ? 'badge-pending' : workflowStep === 2 ? 'badge-processing' : 'badge-completed'">[[ workflowStatusText ]]</span>
+      <!-- ════════════════════════════════════════ -->
+      <!-- 视图三：学员信息（原工作台，两栏核心：主列 + 档案栏） -->
+      <!-- ════════════════════════════════════════ -->
+      <section v-if="view==='workbench'" class="fade-in">
+        <div v-if="!selectedTicketId" class="card"><div class="card-body">
+          <div class="hint">请从「投诉列表」选择案件进入，或在「新增投诉」页新建并自动进入。</div>
+          <div class="query-form" style="margin-top:16px">
+            <div style="grid-column:1/-1"><label class="form-label">快速打开工单</label>
+              <select class="form-control" v-model="selectedTicketId" @change="openCase(selectedTicketId)">
+                <option value="">— 选择工单 —</option>
+                <optgroup label="在途">
+                  <option v-for="t in ticketsOpen" :value="t.id">[[ t.student_name ]]（[[ t.school_short ]]）· [[ t.complaint_date ]]</option>
+                </optgroup>
+                <optgroup label="已归档">
+                  <option v-for="t in ticketsArchived" :value="t.id">[[ t.student_name ]]（[[ t.school_short ]]）· [[ t.complaint_date ]]</option>
+                </optgroup>
+                <optgroup label="已撤诉">
+                  <option v-for="t in ticketsWithdrawn" :value="t.id">[[ t.student_name ]]（[[ t.school_short ]]）· [[ t.complaint_date ]]</option>
+                </optgroup>
+              </select>
+            </div>
           </div>
-          <div class="card-body">
-            <!-- 步骤指示器 - 5步 -->
-            <div class="workflow-steps">
-              <div class="workflow-step" :class="{active: workflowStep === 1, completed: workflowStep > 1}">
-                <div class="step-icon"><i class="bi bi-file-earmark-text"></i></div>
-                <div class="step-text">1. 合同获取</div>
-              </div>
-              <div class="step-line"></div>
-              <div class="workflow-step" :class="{active: workflowStep === 2, completed: workflowStep > 2}">
-                <div class="step-icon"><i class="bi bi-robot"></i></div>
-                <div class="step-text">2. 退费分析</div>
-              </div>
-              <div class="step-line"></div>
-              <div class="workflow-step" :class="{active: workflowStep === 3, completed: workflowStep > 3}">
-                <div class="step-icon"><i class="bi bi-chat-dots"></i></div>
-                <div class="step-text">3. 沟通记录</div>
-              </div>
-              <div class="step-line"></div>
-              <div class="workflow-step" :class="{active: workflowStep === 4, completed: workflowStep > 4}">
-                <div class="step-icon"><i class="bi bi-file-word"></i></div>
-                <div class="step-text">4. 结果归档/文档</div>
-              </div>
-              <div class="step-line"></div>
-              <div class="workflow-step" :class="{active: workflowStep === 5}">
-                <div class="step-icon"><i class="bi bi-send"></i></div>
-                <div class="step-text">5. 飞书(可选)</div>
+        </div></div>
+
+        <div v-else>
+          <div class="case-head">
+            <div>
+              <div class="who">[[ selectedTicket.student_name || qr.name || '—' ]] <span class="code">（[[ (selectedTicket.school_short) || (qr.school_short) || '未归属' ]]）</span></div>
+              <div class="meta">
+                <span>🗓 [[ selectedTicket.complaint_date || form.complaint_date ]]</span>
+                <span>🚗 [[ qr.license_type || 'C1' ]] · [[ qr.exam_stage || selectedTicket.exam_stage || '—' ]]</span>
+                <span class="badge badge-processing">[[ selectedTicket.complaint_type || form.complaint_type ]]</span>
+                <span class="badge" :class="selectedTicket.withdraw_status==='已撤诉' ? 'badge-muted' : (selectedTicket.archive_status==='已归档' ? 'badge-completed' : 'badge-processing')">[[ selectedTicket.archive_status==='已归档' ? '已归档' : (selectedTicket.withdraw_status==='已撤诉' ? '已撤诉' : (selectedTicket.handle_status || '在途')) ]]</span>
               </div>
             </div>
-
-            <div v-if="feeConfirmed || archivedCase" class="card mb-3" style="border-left:3px solid var(--warning)">
-              <div class="card-body" style="padding:14px">
-                <h6 style="font-size:14px;font-weight:600;margin-bottom:8px"><i class="bi bi-pencil-square"></i> 费用方案更正</h6>
-                <div style="font-size:12px;color:var(--text-secondary);margin-bottom:10px">
-                  重新打开会保留旧版本，并要求新版本重新确认、沟通和归档；旧回复函将标记为过期。
-                </div>
-                <div class="d-flex gap-2 align-center flex-wrap">
-                  <input class="form-control" style="flex:1;min-width:220px" v-model="feeReopenReason" placeholder="必填：说明合同、进度或金额更正原因">
-                  <button class="btn btn-secondary" @click="reopenFeePlan(currentTicketId)" :disabled="feeReopening">
-                    [[ feeReopening ? '重新打开中...' : '重新打开费用方案' ]]
-                  </button>
-                </div>
-              </div>
+            <div class="actions">
+              <button class="btn btn-ghost btn-sm" @click="askWithdraw()"><i class="bi bi-flag"></i> 标记撤诉</button>
             </div>
+          </div>
 
-            <!-- 步骤1: 合同 -->
-            <div v-if="workflowStep === 1" class="fade-in">
-              <h5 class="mb-3"><i class="bi bi-file-earmark-text text-primary"></i> 获取合同</h5>
-
-              <!-- 三系统数据就绪提示 -->
-              <div v-if="!threeSystemReady" class="alert alert-warning mb-3">
-                <i class="bi bi-exclamation-triangle"></i> 三系统学员数据未就绪（学时/考试/驾培合同缺失）。请先在受理页完成「查询三系统」，否则退费金额将基于不完整数据计算。
-              </div>
-
-              <!-- 电子合同状态提示 -->
-              <div v-if="qr.contract_available && !cPath" class="alert alert-info mb-3">
-                <i class="bi bi-info-circle"></i> 系统检测到该学员在东莞驾培系统中有电子合同
-              </div>
-              <div v-if="qr.contract_check_deferred && !cPath" class="alert alert-info mb-3">
-                <i class="bi bi-info-circle"></i> 为提高受理速度，系统已延后检查电子合同；点击下载时会连接东莞驾培检查并下载。
-              </div>
-              <div v-if="!qr.contract_available && !qr.contract_check_deferred" class="alert alert-warning mb-3" style="font-size:15px;font-weight:600">
-                <i class="bi bi-exclamation-triangle"></i> <template v-if="qr.sources?.driving === 'no_contract'">该学员 2024年3月15日前报名，东莞驾培无电子合同，请上传合同文件或手动填写费用信息</template><template v-else>该学员在东莞驾培系统中无电子合同记录，请上传合同文件或手动填写费用信息</template>
-              </div>
-
-              <div class="source-cards mb-3">
-                <div v-if="qr.contract_available || qr.contract_check_deferred" class="source-card" :class="{active: cSrc === 'download' || (!cPath && cSrc !== 'upload' && cSrc !== 'manual'), 'loading': cLoading}" @click="cSrc = 'download'; if(!cPath) dlContract(qr.id_card, qr.name, qr.school_short)">
-                  <i v-if="cLoading" class="bi bi-arrow-repeat spin fs-3 text-primary"></i>
-                  <i v-else class="bi bi-cloud-download fs-3 text-primary"></i>
-                  <h6>下载电子合同</h6>
-                  <p>[[ cLoading ? '正在连接驾培系统...' : (qr.contract_check_deferred ? '检查并下载电子合同' : '从驾培系统下载') ]]</p>
-                </div>
-                <div class="source-card" :class="{active: cSrc === 'upload'}" style="position:relative;overflow:hidden;">
-                  <!-- 文件上传 input：直接可见，样式美化成按钮 -->
-                  <input type="file" id="uploadInput" accept=".pdf,.jpg,.jpeg,.png" multiple
-                         style="position:absolute;top:0;left:0;width:100%;height:100%;opacity:0;cursor:pointer;z-index:10;"
-                         @change="ulContract">
-                  <div style="pointer-events:none;">
-                    <i class="bi bi-upload fs-3 text-success"></i>
-                    <h6>上传文件</h6>
-                    <p>上传PDF或图片合同</p>
-                  </div>
-                </div>
-                <div class="source-card" :class="{active: cSrc === 'manual'}" @click="cSrc = 'manual'">
-                  <i class="bi bi-pencil-square fs-3 text-warning"></i>
-                  <h6>手动填写</h6>
-                  <p>手动输入费用信息</p>
-                </div>
-              </div>
-
-              <!-- 缓存合同提示（紧凑版） -->
-              <div v-if="cSrc === 'download' && qr.contract_available && cPath && cCached" class="d-flex align-items-center justify-between mt-2 p-2 rounded" style="background:var(--success-50);border:1px solid var(--success-border);font-size:13px">
-                <span><i class="bi bi-check-circle text-success"></i> 已缓存：[[ cName ]] <small class="text-gray">([[ cCachedAt ]])</small></span>
-                <button class="btn btn-sm btn-outline-secondary" @click="cPath=''; cCached=false; dlContract(qr.id_card, qr.name, qr.school_short)" :disabled="cLoading">
-                  <i class="bi bi-arrow-repeat"></i> 重下
-                </button>
-              </div>
-
-              <div v-if="cSrc === 'upload'">
-                <div v-if="cName" class="mt-2 text-success"><i class="bi bi-check-circle"></i> 已选择：[[ cName ]]</div>
-              </div>
-
-              <div v-if="cSrc === 'manual'" class="form-row">
-                <div><label class="form-label">合同总金额 <span class="text-danger">*</span></label><input type="number" class="form-control" v-model.number="manualContract.total_fee" placeholder="合同总金额"></div>
-                <div><label class="form-label">已缴费用 <span class="text-danger">*</span></label><input type="number" class="form-control" v-model.number="manualContract.paid_amount" placeholder="实际已缴金额"></div>
-                <div><label class="form-label">合同编号</label><input type="text" class="form-control" v-model="manualContract.contract_code" placeholder="可选"></div>
-                <div>
-                  <label class="form-label">合同含考试费</label>
-                  <select class="form-control" v-model="manualContract.includes_exam">
-                    <option :value="true">是（已含考试费）</option>
-                    <option :value="false">否（不含考试费）</option>
-                  </select>
-                </div>
-                <div>
-                  <label class="form-label">合同含补考费</label>
-                  <select class="form-control" v-model="manualContract.includes_makeup">
-                    <option :value="true">是（已含补考费）</option>
-                    <option :value="false">否（不含补考费）</option>
-                  </select>
-                </div>
-              </div>
-
-              <div v-if="cPath" class="alert alert-success mt-3" style="padding:10px 14px">
-                <div class="d-flex align-center justify-between flex-wrap" style="gap:8px">
-                  <div><i class="bi bi-check-circle"></i> 合同已确认：[[ cName ]]</div>
-                  <button class="btn btn-sm btn-outline-primary" @click="openPreview(cPath, cName)" style="font-size:12px">
-                    <i class="bi bi-eye"></i> 预览合同
-                  </button>
-                </div>
-                <div v-if="uploadedFiles && uploadedFiles.length > 0" class="mt-2" style="display:flex;flex-wrap:wrap;gap:4px">
-                  <span v-for="(f, fi) in uploadedFiles" :key="fi"
-                    class="file-chip"
-                    @click="openPreview(f.filepath, f.filename)"
-                    :title="f.filename">
-                    <i :class="f.filename && (f.filename.endsWith('.pdf') || f.filename.endsWith('.PDF')) ? 'bi bi-filetype-pdf' : 'bi bi-file-earmark-image'"></i>
-                    [[ f.filename && f.filename.length > 30 ? f.filename.substring(0,28)+'...' : f.filename || '文件' ]]
+          <div class="wb">
+            <!-- 主列：①三系统查询 → ②扣费明细 → ③回复函 → ④处理情况 → ⑤归档（同列堆叠） -->
+            <div class="wb-main">
+              <div class="card panel"><div class="card-header"><i class="bi bi-search text-primary"></i> ① 三系统查询
+                  <span v-if="qr && qr.name" class="sys-strip" style="margin-left:auto">
+                    <span class="sys-pill" :class="qr.sources?.internal==='success' ? 'ok':'warn'"><i class="bi" :class="sourceStatusIcon(qr.sources?.internal)"></i> 内部系统</span>
+                    <span class="sys-pill" :class="qr.sources?.third==='success' ? 'ok':'warn'"><i class="bi" :class="sourceStatusIcon(qr.sources?.third)"></i> 第三系统</span>
+                    <span class="sys-pill" :class="qr.sources?.driving==='success' ? 'ok':'warn'"><i class="bi" :class="sourceStatusIcon(qr.sources?.driving)"></i> 东莞驾培</span>
                   </span>
                 </div>
-              </div>
-
-              <div class="d-flex justify-end mt-2">
-                <!-- 手动填写模式：直接进入扣费明细编辑 -->
-                <button v-if="cSrc === 'manual' && manualContract.total_fee > 0" class="btn btn-primary" @click="startManualEdit" :disabled="querying">
-                  开始填写明细 <i class="bi bi-arrow-right"></i>
-                </button>
-                <!-- AI/上传/下载模式：调用AI分析 -->
-                <button v-else class="btn btn-primary" @click="confirmContract" :disabled="!canProceedToAnalysis || !threeSystemReady || querying">
-                  AI分析合同 <i class="bi bi-arrow-right"></i>
-                </button>
-              </div>
-              <div v-if="querying" class="text-gray text-end mt-2" style="font-size:12px">
-                三系统查询完成后可进行扣费分析
-              </div>
-            </div>
-
-            <!-- 步骤2: 合同分析（显式启动） -->
-            <div v-if="workflowStep === 2" class="fade-in">
-              <div class="d-flex align-center justify-between flex-wrap mb-3" style="gap:8px">
-                <h5 style="margin:0"><i class="bi bi-robot text-primary"></i> AI退费分析</h5>
-                <div class="d-flex align-center" style="gap:8px">
-                  <button class="btn btn-sm btn-outline-secondary" @click="workflowStep = 1">← 返回修改合同</button>
-                  <button class="btn btn-sm btn-outline-primary" @click="cPath ? openPreview(cPath, cName) : (toast('暂无可预览的合同', '请先在「获取合同」步骤下载或上传合同', 'warning'))" style="font-size:12px">
-                    <i class="bi bi-eye"></i> 合同预览
-                  </button>
-                </div>
-              </div>
-
-              <!-- 分析进度指示 -->
-              <div v-if="aLoading" class="analysis-progress text-center py-4">
-                <div style="font-size:14px;font-weight:600;color:var(--gray-700)" v-if="analysisProgress === 0">📄 正在读取合同文件...</div>
-                <div style="font-size:14px;font-weight:600;color:var(--gray-700)" v-else-if="analysisProgress === 1">🔍 正在提取退费条款...</div>
-                <div style="font-size:14px;font-weight:600;color:var(--gray-700)" v-else-if="analysisProgress === 2">🧮 正在计算各项费用...</div>
-                <div style="font-size:14px;font-weight:600;color:var(--gray-700)" v-else>⏳ 正在生成分析结果...</div>
-                <div class="text-gray" style="font-size:12px;margin-top:8px">
-                  已耗时 [[ analysisElapsed ]] 秒<span v-if="analysisElapsed > 15">，LLM 正在逐项分析合同条款</span><span v-if="analysisElapsed > 30">，仍在运行中，请耐心等待...</span>
-                </div>
-                <div style="width:200px;height:4px;background:var(--gray-200);border-radius:2px;overflow:hidden;margin:8px auto 0">
-                  <div style="height:100%;width:25%;background:linear-gradient(90deg, var(--blue), var(--cyan));border-radius:2px;transition:width .5s" :style="{width: (analysisProgress + 1) * 25 + '%'}"></div>
-                </div>
-              </div>
-
-              <div v-if="aErr" class="alert alert-danger">[[ aErr ]]</div>
-
-              <div v-if="!aLoading && !ar" class="d-flex justify-end mt-3">
-                <button class="btn btn-primary" @click="doAnalyze(qr.id_card, qr.exam_stage, qr.training_hours, currentTicketId)">
-                  [[ aErr ? '重试分析' : '开始分析合同' ]]
-                </button>
-              </div>
-
-              <div v-if="ar && !aLoading">
-                <!-- 退费计算总览 -->
-                <div class="analysis-cards mb-3">
-                  <div class="analysis-card">
-                    <div class="analysis-label">合同总额</div>
-                    <div class="analysis-value">[[ ar.total_fee || 0 ]]元</div>
-                  </div>
-                  <label class="analysis-card analysis-card-editable">
-                    <span class="analysis-label">实际已交 · 可修改</span>
-                    <span class="analysis-input-wrap">
-                      <input
-                        class="analysis-amount-input"
-                        type="number"
-                        v-model.number="ar.actual_paid"
-                        @input="recalc"
-                        aria-label="实际已交金额">
-                      <em>元</em>
-                    </span>
-                  </label>
-                  <div class="analysis-card analysis-card-deduction">
-                    <div class="analysis-label">总扣费</div>
-                    <div class="analysis-value text-danger">[[ ar.total_deduction || 0 ]]元</div>
-                  </div>
-                  <div class="analysis-card analysis-card-refund">
-                    <div class="analysis-label">应退金额</div>
-                    <div class="analysis-value text-success">[[ ar.refund > 0 ? ar.refund : 0 ]]元</div>
-                  </div>
-                </div>
-                <div v-if="ar.blockers && ar.blockers.length" class="alert alert-warning" style="margin-top:8px;font-size:13px">
-                  <div style="font-weight:700;margin-bottom:4px"><i class="bi bi-exclamation-triangle"></i> 暂不能确认正式费用方案</div>
-                  <div v-for="(b, bi) in ar.blockers" :key="bi">[[ b ]]</div>
-                  <div v-if="ar.unclear_fields && ar.unclear_fields.length" style="margin-top:4px">
-                    模糊字段：[[ ar.unclear_fields.join('、') ]]
-                  </div>
-                </div>
-		                <div v-if="fromHistoryLabel" class="from-history-label">
-		                  <i class="bi bi-clock-history"></i> [[ fromHistoryLabel ]]
-		                </div>
-
-                <div class="analysis-section-head">
-                  <strong>扣费明细（[[ ar.deductions?.length || 0 ]]项）</strong>
-                  <span>修改金额后点击重新计算</span>
-                </div>
-
-                <!-- 扣费校验 -->
-                <div v-if="deductionMismatch" class="mismatch-strip">
-                  ⚠️ 总扣费与明细之和不一致：明细合计 [[ deductionSum ]] 元，<br>去"重新计算"修正
-                </div>
-
-                <div class="table-wrap mb-3">
-                  <table class="fee-table">
-                    <thead><tr>
-                      <th class="col-name th-center">项目</th>
-                      <th class="col-duration th-center">时长/说明</th>
-                      <th class="col-price th-center">单价 <span class="col-sub">(元/学时)</span></th>
-                      <th class="col-amount th-center">金额（元）</th>
-                      <th class="col-reason th-center">依据</th>
-                      <th class="col-action"></th>
-                    </tr></thead>
-                    <tbody>
-                      <tr v-for="(ded, i) in ar.deductions" :key="i"
-                          :class="{'tr-capped': ded.max_amount && Number(ded.amount) >= Number(ded.max_amount)}">
-                        <td data-label="项目">
-                          <div v-if="ded.item === '违约金'" class="penalty-cell">
-                            <span>违约金</span>
-                            <input type="number" class="ci ci-short" :value="ar.penalty_amount || ded.amount" @change="updatePenaltyRate($event.target.value)" placeholder="金额（元）">
-                          </div>
-                          <input v-else class="ci" v-model="ded.item">
-                        </td>
-                        <td v-if="ded.item === '违约金'" class="td-mute" data-label="时长/说明">固定金额（元）</td>
-                        <td v-else-if="!['综合服务费','理论培训费'].includes(ded.item)" data-label="时长/说明">
-                          <input class="ci ci-duration" :value="ded.duration" @input="ded.duration=$event.target.value" placeholder="14时43分">
-                        </td>
-                        <td v-else class="td-mute" data-label="时长/说明">-</td>
-                        <td v-if="ded.item === '违约金'" class="td-mute" data-label="单价">-</td>
-                        <td v-else-if="!['综合服务费','理论培训费'].includes(ded.item)" data-label="单价">
-                          <input class="ci ci-short" :value="ded.unit_price" @input="ded.unit_price=$event.target.value">
-                        </td>
-                        <td v-else class="td-mute" data-label="单价">-</td>
-                        <td data-label="金额">
-                          <div class="aw">
-                            <input type="number" class="ai" :class="{'ai-capped': ded.max_amount && Number(ded.amount) >= Number(ded.max_amount)}" v-model.number="ded.amount">
-                            <span v-if="ded.max_amount && Number(ded.amount) >= Number(ded.max_amount)" class="cap-badge">已达上限</span>
-                          </div>
-                          <span v-if="ded.raw_amount" class="raw-hint">原算 [[ ded.raw_amount ]] 元</span>
-                        </td>
-                        <td data-label="依据"><input class="ci ci-wide" v-model="ded.reason" placeholder="合同条款依据"></td>
-                        <td class="fee-row-action"><button class="del-btn" @click="removeDeduction(i)" aria-label="删除扣费项">✕</button></td>
-                      </tr>
-                    </tbody>
-                  </table>
-                </div>
+                <div class="card-body">
+                  <div v-if="!qr || !qr.name" class="hint">尚无查询结果。请到「新增投诉」页查询，或在上方选择已查询工单。</div>
+                  <template v-else>
+                    <div class="qtabbar">
+                      <button class="chip" :class="{on: tab==='basic'}" @click="tab='basic'">基本信息</button>
+                      <button class="chip" :class="{on: tab==='exam'}" @click="tab='exam'">考试情况</button>
+                      <button class="chip" :class="{on: tab==='training'}" @click="tab='training'">培训学时</button>
+                      <button class="chip" :class="{on: tab==='fees'}" @click="tab='fees'">收费记录</button>
+                      <button class="chip" :class="{on: tab==='timeline'}" @click="tab='timeline'">时间轴</button>
+                    </div>
 
-                <details
-                  v-if="ar.contract_fields"
-                  class="contract-fields-panel"
-                  :open="ar.can_confirm_fee_plan === false">
-                  <summary class="contract-fields-head">
-                    <span>
-                      <strong>合同字段核对</strong>
-                      <small>识别来源、条款和计费参数</small>
-                    </span>
-                    <span class="contract-fields-status" :class="{'is-warning': ar.contract_fields.status === 'needs_review'}">
-                      [[ ar.contract_fields.status === 'needs_review' ? '需补充合同' : '待人工核对' ]]
-                    </span>
-                  </summary>
-                  <div class="contract-fields-content">
-                    <div v-if="ar.extraction_source || ar.contract_text_preview" class="contract-recognition">
-                      <strong>识别来源：</strong>
-                      [[ ar.extraction_source === 'pdf_text' ? '文字PDF文本层' : ar.extraction_source === 'vision_text' ? '多模态识别' : ar.extraction_source === 'local_ocr' ? '本地OCR' : ar.extraction_source || '未知' ]]
+                    <!-- 基本信息（含身份证号，仅此页展示） -->
+                    <div v-show="tab==='basic'">
+                      <div class="info-grid">
+                        <div class="ig"><span class="k">姓名</span><span class="v">[[ qr.name || '-' ]]</span></div>
+                        <div class="ig"><span class="k">身份证</span><span class="v mono">[[ qr.id_card ]]</span></div>
+                        <div class="ig"><span class="k">手机号</span><span class="v">[[ qr.phone || '-' ]]</span></div>
+                        <div class="ig"><span class="k">车型</span><span class="v">[[ qr.license_type || '-' ]]</span></div>
+                        <div class="ig"><span class="k">驾校</span><span class="v">[[ qr.school_name || '-' ]]<span v-if="qr.school_short" style="color:var(--color-text-secondary);font-size:12px">([[ qr.school_short ]])</span></span></div>
+                        <div class="ig"><span class="k">报名日期</span><span class="v">[[ qr.registration_date || '-' ]]</span></div>
+                        <div class="ig"><span class="k">当前阶段</span><span class="v" style="color:var(--color-primary);font-weight:600">[[ qr.exam_stage || '-' ]]</span></div>
+                        <div class="ig"><span class="k">学员状态</span><span class="v">[[ qr.student_status || '-' ]]</span></div>
+                      </div>
+                      <p class="hint" style="margin-top:10px">口径：基本信息以内部系统为准；实操学时取审核有效学时；合同与收费记录来自东莞驾培平台。</p>
                     </div>
-                    <div class="contract-fields-grid">
-                      <label class="contract-field-item"><span>合同总培训费</span><input type="number" v-model.number="ar.contract_fields.total_fee.value" @change="applyContractFields"><em>元</em></label>
-                      <label class="contract-field-item"><span>综合服务费</span><input type="number" v-model.number="ar.contract_fields.service_fee.value" @change="applyContractFields"><em>元</em></label>
-                      <label class="contract-field-item"><span>建档费</span><input type="number" v-model.number="ar.contract_fields.archive_fee.value" @change="applyContractFields"><em>元</em></label>
-                      <label class="contract-field-item"><span>学员IC卡费</span><input type="number" v-model.number="ar.contract_fields.ic_card_fee.value" @change="applyContractFields"><em>元</em></label>
-                      <label class="contract-field-item"><span>理论培训费</span><input type="number" v-model.number="ar.contract_fields.theory_fee.value" @change="applyContractFields"><em>元</em></label>
-                      <label class="contract-field-item"><span>科二单价/上限</span><input type="number" v-model.number="ar.contract_fields.subject2_unit_price.value" @change="applyContractFields"><small>/</small><input type="number" v-model.number="ar.contract_fields.subject2_cap.value" @change="applyContractFields"></label>
-                      <label class="contract-field-item"><span>科三单价/上限</span><input type="number" v-model.number="ar.contract_fields.subject3_unit_price.value" @change="applyContractFields"><small>/</small><input type="number" v-model.number="ar.contract_fields.subject3_cap.value" @change="applyContractFields"></label>
-                      <label class="contract-field-item"><span>科一考试/补考</span><input type="number" v-model.number="ar.contract_fields.subject1_exam_fee.value" @change="applyContractFields"><small>/</small><input type="number" v-model.number="ar.contract_fields.subject1_makeup_fee.value" @change="applyContractFields"></label>
-                      <label class="contract-field-item"><span>科二考试/补考</span><input type="number" v-model.number="ar.contract_fields.subject2_exam_fee.value" @change="applyContractFields"><small>/</small><input type="number" v-model.number="ar.contract_fields.subject2_makeup_fee.value" @change="applyContractFields"></label>
-                      <label class="contract-field-item"><span>科三考试/补考</span><input type="number" v-model.number="ar.contract_fields.subject3_exam_fee.value" @change="applyContractFields"><small>/</small><input type="number" v-model.number="ar.contract_fields.subject3_makeup_fee.value" @change="applyContractFields"></label>
-                      <label class="contract-field-item"><span>违约金（元）</span><input type="number" step="0.01" v-model.number="ar.contract_fields.penalty_amount.value" @change="applyContractFields"><em>固定金额，如 328</em></label>
-                      <label class="contract-field-item"><span>考试费含在总费内</span><input type="checkbox" v-model="ar.contract_fields.includes_exam_fee.value" @change="applyContractFields"><em>默认包含</em></label>
-                      <label class="contract-field-item"><span>补考费含在总费内</span><input type="checkbox" v-model="ar.contract_fields.includes_makeup_fee.value" @change="applyContractFields"><em>默认包含</em></label>
-                      <label class="contract-field-item contract-field-clause">
-                        <span>退费条款</span>
-                        <textarea v-model="ar.contract_fields.refund_clause.value" @change="applyContractFields" rows="2" placeholder="请核对或补录合同中的退学退费条款"></textarea>
-                      </label>
+                    <!-- 考试情况 -->
+                    <div v-show="tab==='exam'">
+                      <div v-if="qr.exam_counts && Object.keys(qr.exam_counts).length" class="subject-summary-grid" style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px">
+                        <div v-for="(subject, idx) in ['科目一','科目二','科目三','科目四']" :key="subject" style="border:1px solid var(--color-border);border-radius:var(--radius);padding:10px;text-align:center">
+                          <div style="font-weight:600">[[ subject ]]</div>
+                          <div style="font-size:20px;font-weight:800;color:var(--color-primary)">[[ qr.exam_counts[subject] || 0 ]]</div>
+                          <div class="hint">次</div>
+                        </div>
+                      </div>
+                      <div v-else class="hint">暂无考试记录</div>
                     </div>
-                    <div v-if="ar.contract_fields.uncertain_fields && ar.contract_fields.uncertain_fields.length" class="contract-fields-unclear">
-                      需补充：[[ ar.contract_fields.uncertain_fields.join('、') ]]
+                    <!-- 培训学时 -->
+                    <div v-show="tab==='training'">
+                      <div v-if="qr.training_details && qr.training_details.length" style="display:grid;grid-template-columns:repeat(2,1fr);gap:10px">
+                        <div v-for="subject in ['科目二','科目三']" :key="subject" style="border:1px solid var(--color-border);border-radius:var(--radius);padding:10px">
+                          <div style="font-weight:600;margin-bottom:4px">[[ subject ]]</div>
+                          <div>总时长：<b>[[ getTrainingTime(qr.training_details, subject, 'training_time') ]]</b></div>
+                          <div>共享时长：<b style="color:var(--color-success)">[[ getTrainingTime(qr.training_details, subject, 'platform_time') ]]</b></div>
+                        </div>
+                      </div>
+                      <div v-else class="hint">该学员在计时培训管理平台无培训记录</div>
                     </div>
-                    <div v-if="ar.fee_basis_warning" class="contract-fields-unclear">
-                      [[ ar.fee_basis_warning ]]
+                    <!-- 收费记录 -->
+                    <div v-show="tab==='fees'">
+                      <template v-if="qr.driving_fee && (qr.driving_fee.contract_fee > 0 || (qr.driving_fee.orders && qr.driving_fee.orders.length))">
+                        <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:12px">
+                          <div class="kpi" style="padding:10px"><div class="num" style="font-size:16px">[[ qr.driving_fee.supervise_date ? qr.driving_fee.supervise_date.slice(0,10) : '-' ]]</div><div class="lbl">监管日期</div></div>
+                          <div class="kpi" style="padding:10px"><div class="num" style="font-size:16px">[[ qr.driving_fee.contract_fee ]]</div><div class="lbl">合同金额</div></div>
+                          <div class="kpi" style="padding:10px"><div class="num" style="font-size:16px">[[ qr.driving_fee.supervise_fee ]]</div><div class="lbl">监管资金</div></div>
+                          <div class="kpi" style="padding:10px"><div class="num" style="font-size:16px">[[ qr.driving_fee.residue_supervise_amt ]]</div><div class="lbl">监管余额</div></div>
+                        </div>
+                        <div v-if="qr.driving_fee.orders && qr.driving_fee.orders.length">
+                          <div class="hint" style="margin-bottom:6px">交费订单（[[ qr.driving_fee.orders.length ]]笔）</div>
+                          <table class="table"><thead><tr><th>订单号</th><th>支付时间</th><th>金额</th><th>状态</th></tr></thead>
+                            <tbody><tr v-for="(o,i) in qr.driving_fee.orders" :key="i">
+                              <td class="mono">[[ o.order_no || '-' ]]</td><td>[[ o.pay_time || o.order_time || '-' ]]</td>
+                              <td style="font-weight:600">[[ o.total_fee > 0 ? o.total_fee : '-' ]]</td>
+                              <td>[[ o.is_pay===1 ? '已支付' : '未支付' ]]</td>
+                            </tr></tbody></table>
+                        </div>
+                      </template>
+                      <div v-else class="hint">东莞驾培暂无收费记录</div>
                     </div>
-                    <details v-if="ar.contract_text_preview" class="contract-fields-evidence">
-                      <summary>查看识别摘要</summary>
-                      <div>[[ ar.contract_text_preview ]]</div>
-                    </details>
-                    <details v-if="ar.contract_fields.evidence_snippets && ar.contract_fields.evidence_snippets.length" class="contract-fields-evidence">
-                      <summary>查看字段依据</summary>
-                      <div v-for="(ev, ei) in ar.contract_fields.evidence_snippets" :key="ei">[[ ev ]]</div>
-                    </details>
-                  </div>
-                </details>
-
-                <div class="toolbar">
-                  <div class="toolbar-left">
-                    <button class="btn-tool" @click="addDeduction">＋ 添加扣费项</button>
-                    <button class="btn-tool" @click="recalc">↻ 重新计算</button>
-                  </div>
-                  <div>
-                    <button class="btn btn-outline" @click="workflowStep = 1">← 返回修改合同</button>
-	                    <button class="btn btn-primary" @click="handleSaveAndConfirm(currentTicketId)" :disabled="feeConfirming || (ar && ar.can_confirm_fee_plan === false)">
-	                      [[ feeConfirming ? '确认中...' : (ar && ar.can_confirm_fee_plan === false ? '需补充合同后确认' : '人工确认费用方案 →') ]]
-	                    </button>
-                  </div>
-                </div>
-              </div>
-            </div>
-
-            <!-- 步骤3: 沟通记录与结果确认 -->
-            <div v-if="workflowStep === 3" class="fade-in">
-              <h5 class="mb-3"><i class="bi bi-chat-dots text-primary"></i> 沟通记录与处理结果</h5>
-
-              <div class="card mb-3" style="background:var(--bg);border-left:4px solid var(--primary)">
-                <div class="card-body" style="padding:14px">
-                  <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:12px">
-                    <div><span class="info-label">合同总金额</span><div class="info-value">[[ ar?.total_fee || 0 ]]元</div></div>
-                    <div><span class="info-label">实际已交</span><div class="info-value">[[ ar?.actual_paid || ar?.total_fee || 0 ]]元</div></div>
-                    <div><span class="info-label">总扣费</span><div class="info-value" style="color:var(--danger)">[[ ar?.total_deduction || 0 ]]元</div></div>
-                    <div><span class="info-label">应退金额</span><div class="info-value" style="color:var(--success);font-size:16px">[[ ar?.refund > 0 ? ar.refund : 0 ]]元</div></div>
-                    <div><span class="info-label">当前阶段</span><div class="info-value">[[ qr.exam_stage || '-' ]]</div></div>
-                    <div><span class="info-label">费用状态</span><div class="info-value" style="color:var(--success)">[[ ar?.fee_plan_status === 'confirmed' ? '正式确认' : (ar?.fee_plan_status === 'provisional' ? '阶段确认' : '待确认') ]]</div></div>
-                  </div>
-                </div>
-              </div>
-
-              <!-- 合同条款摘要 -->
-              <div v-if="ar && ar.clauses && ar.clauses.length" class="mb-3">
-                <details style="cursor:pointer">
-                  <summary style="font-size:13px;font-weight:600;color:var(--text-secondary);padding:6px 0">
-                    <i class="bi bi-journal-text"></i> 合同条款摘要（[[ ar.clauses.length ]]条）
-                  </summary>
-                  <div class="card mt-2" style="max-height:240px;overflow-y:auto">
-                    <div class="card-body" style="padding:10px 14px">
-                      <div v-for="(clause, i) in ar.clauses" :key="i" style="font-size:12px;padding:6px 0;border-bottom:1px solid var(--border)">
-                        <strong>[[ clause.number || clause.title ]]:</strong>
-                        <span style="color:var(--text-secondary)">[[ clause.content ]]</span>
+                    <!-- 时间轴 -->
+                    <div v-show="tab==='timeline'">
+                      <div v-if="qr.timeline_display && qr.timeline_display.length">
+                        <div v-for="(ev,i) in qr.timeline_display" :key="i" class="tl-item"><span class="d">[[ ev.date ]]</span>[[ ev.title ]]</div>
                       </div>
+                      <div v-else class="hint">暂无时间轴记录</div>
                     </div>
-                  </div>
-                </details>
-              </div>
-
-              <!-- 手写标注 -->
-              <div v-if="ar && ar.handwritten_annotations && ar.handwritten_annotations.length" class="mb-3">
-                <div class="alert alert-info" style="font-size:12px;padding:8px 12px">
-                  <i class="bi bi-pencil"></i> <strong>检测到手写内容 [[ ar.handwritten_annotations.length ]] 处：</strong>
-                  <span v-for="(h, i) in ar.handwritten_annotations" :key="i">
-                    [[ h.location ]]: 「[[ h.content ]]」<span v-if="i < ar.handwritten_annotations.length - 1">；</span>
-                  </span>
-                </div>
-              </div>
-
-              <!-- 特殊退费警告 -->
-              <div v-if="ar && ar.special_warnings && ar.special_warnings.length" class="mb-3">
-                <div v-for="(w, i) in ar.special_warnings" :key="i"
-                  class="alert alert-danger" style="padding:10px 14px;margin-bottom:6px;font-weight:600;font-size:13px">
-                  <i class="bi bi-exclamation-triangle-fill"></i> [[ w.message ]]
+                  </template>
                 </div>
               </div>
 
-              <div class="mb-3">
-                <label class="form-label mb-2"><i class="bi bi-clock-history"></i> 沟通记录（[[ communications.length ]]次）</label>
-                <div v-if="communications.length === 0" class="text-muted" style="font-size:12px;padding:8px 0">
-                  暂无沟通记录，点击下方「新增沟通」登记第一次联系。
-                </div>
-                <div v-else class="comm-timeline">
-                  <div v-for="rec in communications" :key="rec.id" class="comm-timeline-item call">
-                    <div class="comm-timeline-head">
-                      <span class="comm-timeline-time">[[ rec.contact_time || rec.created_at?.substring(0,16) ]]</span>
-                      <span class="comm-timeline-tag"><i class="bi bi-telephone"></i> [[ rec.contact_method || '-' ]]</span>
-                      <span class="comm-timeline-tag">[[ rec.student_intention || '-' ]]</span>
-                    </div>
-                    <div class="comm-timeline-body">[[ rec.summary ]]</div>
-                    <div v-if="rec.next_follow_up" class="comm-timeline-foot">
-                      <span><i class="bi bi-calendar-event"></i> 下次跟进：[[ rec.next_follow_up ]]</span>
+              <!-- ② 扣费明细表 -->
+              <div class="card panel"><div class="card-header"><i class="bi bi-calculator text-primary"></i> ② 扣费明细表
+                <span class="badge" :class="(selectedTicket.fee_plan_status==='confirmed') ? 'badge-completed':'badge-pending'">[[ selectedTicket.fee_plan_status==='confirmed' ? '已确认' : '编辑中' ]]</span>
+                <span style="flex:1"></span>
+                <template v-if="!ar">
+                  <button class="btn btn-primary btn-sm" @click="dlContractAnalyze()" :disabled="cLoading || aLoading">
+                    <span v-if="cLoading" class="spinner-custom spinner-custom-sm me-1"></span>
+                    <i v-else class="bi bi-cloud-download"></i> [[ cLoading ? '下载合同中…' : (aLoading ? 'AI分析中…' : '下载合同并分析') ]]
+                  </button>
+                  <button class="btn btn-secondary btn-sm" @click="contractFileInput && contractFileInput.click()" :disabled="cLoading || aLoading"><i class="bi bi-upload"></i> 上传合同分析</button>
+                  <input ref="contractFileInput" type="file" multiple accept=".pdf,.png,.jpg,.jpeg" style="display:none" @change="ulContractAnalyze($event)">
+                  <button class="btn btn-secondary btn-sm" @click="startManualFeeEntry()" :disabled="cLoading || aLoading"><i class="bi bi-pencil-square"></i> 手动录入费用</button>
+                </template>
+                <button class="btn btn-secondary btn-sm" @click="openContract()"><i class="bi bi-eye"></i> 预览合同</button>
+                <button class="btn btn-secondary btn-sm" @click="exportDeductions()" v-if="ar"><i class="bi bi-download"></i> 导出Excel</button>
+                <button class="btn btn-secondary btn-sm" @click="addDeduction()" v-if="ar"><i class="bi bi-plus-lg"></i> 添加行</button>
+                <button class="btn btn-secondary btn-sm" @click="unlockFee()" v-if="selectedTicket.fee_plan_status==='confirmed' && !feeUnlocked"><i class="bi bi-unlock"></i> 解锁修改</button>
+                <button class="btn btn-primary btn-sm" @click="handleSaveAndConfirm(selectedTicketId)" v-if="ar && selectedTicket.fee_plan_status!=='confirmed'" :disabled="feeConfirming">[[ feeConfirming ? '确认中...' : '✔ 确认明细' ]]</button>
+              </div>
+                <div class="card-body">
+                  <template v-if="!ar">
+                    <div class="hint">点击「下载合同并分析」：自动从东莞驾培获取电子合同 → AI 提取扣费明细与实缴金额，一步完成。纸质合同请用「上传合同分析」；2024-03-15 前报名学员东莞驾培无电子合同。<b>AI 分析失败或无合同时，可点「手动录入费用」直接建表录入，流程不受阻。</b></div>
+                    <div v-if="aLoading" class="hint" style="margin-top:4px"><span class="spinner-custom spinner-custom-sm me-1"></span>AI 分析中… 已用时 [[ analysisElapsed ]] 秒（OCR 识别 + 大模型提取合同字段，约需几十秒）</div>
+                    <div v-if="aErr" class="alert alert-danger mt-2">[[ aErr ]]</div>
+                  </template>
+                  <template v-else>
+                    <p class="hint" style="margin-bottom:10px">来源：东莞驾培电子合同 AI 提取，可直接修改任意单元格；点击「预览合同」可对照合同原文核对扣费依据。<b>确认后作为归档与回复函金额口径，仍可解锁重编。</b></p>
+                    <table class="table"><thead><tr><th style="width:24%">扣费项目</th><th style="width:13%;text-align:center">金额（元）</th><th>扣费依据</th><th style="width:40px"></th></tr></thead>
+                      <tbody>
+                        <tr v-for="(d,i) in ar.deductions" :key="i">
+                          <td><input class="form-control" v-model="d.item" style="border:none;padding:2px 4px;background:transparent" @input="recalc()"></td>
+                          <td class="amt-td"><input class="form-control mono" type="number" v-model.number="d.amount" style="text-align:center;border:none;padding:2px 4px;background:transparent" @input="recalc()"></td>
+                          <td><input class="form-control" v-model="d.reason" style="border:none;padding:2px 4px;background:transparent" placeholder="合同条款依据"></td>
+                          <td><button class="btn btn-ghost btn-sm" @click="removeDeduction(i)"><i class="bi bi-x"></i></button></td>
+                        </tr>
+                      </tbody>
+                    </table>
+                    <div class="fee-total">
+                      <span>合同总额 <input v-if="ar.source==='manual'" class="form-control mono" type="number" min="0" style="width:110px;display:inline-block;height:26px;padding:2px 6px;text-align:center" v-model.number="ar.total_fee" @input="recalc()"><b v-else>¥[[ money(ar.total_fee) ]]</b></span>
+                      <span>实缴 <input v-if="ar.source==='manual'" class="form-control mono" type="number" min="0" style="width:110px;display:inline-block;height:26px;padding:2px 6px;text-align:center" v-model.number="ar.actual_paid" @input="recalc()"><b v-else>¥[[ money(ar.actual_paid || ar.paid_amount) ]]</b></span>
+                      <span>扣费合计 <b>¥[[ money(ar.total_deduction) ]]</b></span>
+                      <span style="margin-left:auto">应退 <span class="big">¥[[ money(Math.max((ar.actual_paid||ar.paid_amount||0) - (ar.total_deduction||0), 0)) ]]</span></span>
                     </div>
-                  </div>
+                  </template>
                 </div>
               </div>
 
-              <div class="card mb-3" style="border-left:3px solid var(--success)">
-                <div class="card-body" style="padding:14px">
-                  <div class="d-flex align-center justify-between flex-wrap" style="gap:8px">
-                    <h6 style="font-size:13px;font-weight:600;margin:0"><i class="bi bi-pencil-square"></i> 新增沟通记录</h6>
-                    <button class="btn btn-sm btn-primary" @click="commFormOpen = !commFormOpen">
-                      <i class="bi" :class="commFormOpen ? 'bi-chevron-up' : 'bi-plus-lg'"></i>
-                      [[ commFormOpen ? '收起表单' : '新增沟通' ]]
-                    </button>
-                  </div>
-                  <div v-show="commFormOpen" style="margin-top:12px">
-                    <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(160px,1fr));gap:12px">
-                      <div>
-                        <label class="form-label">联系时间</label>
-                        <input type="datetime-local" class="form-control" v-model="commForm.contact_time">
-                      </div>
-                      <div>
-                        <label class="form-label">联系方式</label>
-                        <select class="form-control" v-model="commForm.contact_method">
-                          <option>电话</option>
-                          <option>微信</option>
-                          <option>短信</option>
-                          <option>现场</option>
-                          <option>上级部门转达</option>
-                        </select>
-                      </div>
-                      <div>
-                        <label class="form-label">学员意愿</label>
-                        <select class="form-control" v-model="commForm.student_intention">
-                          <option>继续协商</option>
-                          <option>接受扣费方案</option>
-                          <option>要求降低扣费</option>
-                          <option>拒绝协商</option>
-                          <option>无法联系</option>
-                          <option>继续培训/转校</option>
-                        </select>
-                      </div>
-                      <div>
-                        <label class="form-label">下次跟进</label>
-                        <input type="datetime-local" class="form-control" v-model="commForm.next_follow_up">
-                      </div>
-                    </div>
-                    <div class="mb-3">
-                      <label class="form-label">沟通摘要 <span class="text-danger">*</span></label>
-                      <textarea class="form-control" v-model="commForm.summary" placeholder="记录本次沟通内容、学员诉求、协商口径和下一步动作" rows="3"></textarea>
-                    </div>
-                    <div class="d-flex justify-end">
-                      <button class="btn btn-secondary" @click="addCommunication(currentTicketId)" :disabled="communicationsLoading">
-                        [[ communicationsLoading ? '保存中...' : '保存本次沟通' ]]
-                      </button>
+              <!-- ④ 回复函 -->
+              <div class="card panel"><div class="card-header"><i class="bi bi-envelope-paper text-primary"></i> ③ 投诉回复函
+                <span class="badge" :class="replySaved ? 'badge-completed':'badge-processing'">[[ replySaved ? '已生成落盘' : '草稿编辑中' ]]</span>
+                <span class="hint" style="font-weight:400">与写不写都计入统计</span>
+                <span style="flex:1"></span>
+                <button class="btn btn-secondary btn-sm" @click="aiPolishReply()" v-if="!replySaved"><i class="bi bi-stars"></i> 一键AI修改</button>
+                <button class="btn btn-primary btn-sm" @click="generateReply()" v-if="!replySaved"><i class="bi bi-file-earmark-word"></i> 审阅无误，生成回复函</button>
+              </div>
+                <div class="card-body">
+                  <div v-if="!ar" class="hint">完成费用分析后，系统将按当前扣费明细生成致交通局的回复函初稿。</div>
+                  <template v-else>
+                    <div class="rte-bar" v-if="!replySaved">
+                      <button title="加粗" @click="fmt('bold')"><b>B</b></button>
+                      <button title="斜体" @click="fmt('italic')"><i>I</i></button>
+                      <button title="下划线" @click="fmt('underline')"><u>U</u></button>
+                      <span class="sep"></span>
+                      <button @click="fmt('justifyLeft')">居左</button>
+                      <button @click="fmt('justifyCenter')">居中</button>
+                      <button @click="fmt('justifyRight')">居右</button>
+                      <span class="sep"></span>
+                      <select @change="fmt('fontSize', $event.target.value); $event.target.selectedIndex=0">
+                        <option value="">字号</option><option value="2">小</option><option value="3" selected>标准</option><option value="4">中大</option><option value="5">大</option><option value="6">特大</option>
+                      </select>
+                      <button @click="fmt('undo')">↩ 撤销</button>
                     </div>
-                  </div>
+                    <div id="re-letter" class="letter" aria-label="回复函正文，可编辑" :class="{ro: replySaved}" contenteditable="[[ !replySaved ]]" @input="replyHtml=$event.target.innerHTML; replyEdited=true" v-html="replySaved ? replyHtml : buildLetter(replyPolished)"></div>
+                    <div v-if="replySaved" class="reply-preview" v-html="replyHtml"></div>
+                  </template>
                 </div>
               </div>
 
-              <!-- 最终结果归档（合并原协商结果登记，一次填完） -->
-              <div class="card mb-3" style="border-left:3px solid var(--primary)">
-                <div class="card-body" style="padding:14px">
-                  <h6 style="font-size:13px;font-weight:600;margin-bottom:10px"><i class="bi bi-archive"></i> 最终结果归档</h6>
-                  <label class="form-label mb-2">最终处理结果</label>
-                  <div class="outcome-grid">
-                    <div class="outcome-card" :class="{selected: finalOutcome === '同意合同扣费'}" @click="selectOutcomeCard('同意合同扣费')">
-                      <i class="bi bi-hand-thumbs-up"></i>
-                      <div class="title">同意扣费</div>
-                      <div class="hint">按合同执行</div>
-                    </div>
-                    <div class="outcome-card" :class="{selected: finalOutcome === '投诉撤销'}" @click="selectOutcomeCard('投诉撤销')">
-                      <i class="bi bi-check-circle"></i>
-                      <div class="title">投诉撤销</div>
-                      <div class="hint">学员主动撤诉</div>
-                    </div>
-                    <div class="outcome-card" :class="{selected: finalOutcome === '不同意合同扣费但协商一致'}" @click="selectOutcomeCard('不同意合同扣费但协商一致')">
-                      <i class="bi bi-handshake"></i>
-                      <div class="title">协商一致</div>
-                      <div class="hint">达成新方案</div>
-                    </div>
-                    <div class="outcome-card" :class="{selected: finalOutcome === '不同意合同扣费且协商失败'}" @click="selectOutcomeCard('不同意合同扣费且协商失败')">
-                      <i class="bi bi-x-octagon"></i>
-                      <div class="title">协商失败</div>
-                      <div class="hint">需上级介入</div>
-                    </div>
-                  </div>
-
-                  <div class="compact-row">
-                    <div>
-                      <label class="form-label">其他情况（少用）</label>
-                      <select class="form-control" v-model="otherOutcome" @change="onOtherOutcomeChange">
-                        <option value="">无需选择</option>
-                        <option value="无法联系">无法联系</option>
-                        <option value="继续培训/转校">继续培训/转校</option>
-                      </select>
-                    </div>
-                    <div>
-                      <label class="form-label">是否撤诉</label>
-                      <div class="toggle-group">
-                        <div class="toggle-option" :class="{active: withdrawStatus === '未撤诉'}" @click="withdrawStatus = '未撤诉'">未撤诉</div>
-                        <div class="toggle-option" :class="{active: withdrawStatus === '已撤诉'}" @click="withdrawStatus = '已撤诉'">已撤诉</div>
-                      </div>
-                    </div>
-                  </div>
-
-                  <div>
-                    <label class="form-label mb-2">分校/分店配合度</label>
-                    <div class="tag-group">
-                      <button class="tag-btn" :class="{selected: branchCooperation === ''}" @click="branchCooperation = ''">未记录</button>
-                      <button v-for="opt in COOPERATION_OPTIONS" :key="opt" class="tag-btn"
-                        :class="{selected: branchCooperation === opt}" @click="branchCooperation = opt">[[ opt ]]</button>
-                    </div>
+              <!-- ⑤ 处理情况 + 配合度 -->
+              <div class="card panel"><div class="card-header"><i class="bi bi-clipboard-check text-primary"></i> ④ 处理情况 与 分校/分店配合度</div>
+                <div class="card-body">
+                  <div style="display:flex;align-items:center;margin-bottom:6px">
+                    <label class="form-label" for="f_handling_notes" style="margin:0">处理情况</label>
+                    <span style="flex:1"></span>
+                    <button class="btn btn-secondary btn-sm" @click="aiOptimizeNotes()"><i class="bi bi-stars"></i> AI优化</button>
+                    <button class="btn btn-secondary btn-sm" @click="saveProgress()"><i class="bi bi-save"></i> 保存进度</button>
                   </div>
-
-                  <div class="collapse-toggle" @click="remarkOpen = !remarkOpen">
-                    <i class="bi" :class="remarkOpen ? 'bi-chevron-up' : 'bi-chevron-down'"></i>
-                    [[ remarkOpen ? '收起备注' : '展开备注' ]]
+                  <textarea id="f_handling_notes" class="form-control" rows="3" v-model="handlingNotes" placeholder="例如：已电话联系学员说明扣费依据，约定本周退款……（将引用至归档登记表）"></textarea>
+                  <p class="hint" style="margin-top:6px">随手记几个关键词即可，「AI优化」会整理成通顺、有条理的正式表述。</p>
+                  <div style="margin-top:14px">
+                    <label class="form-label" for="f_student_name_edit">学员姓名<span class="hint" style="font-weight:400;margin-left:4px">（三系统未命中时可手动补录）</span></label>
+                    <input id="f_student_name_edit" class="form-control" style="max-width:240px" v-model="studentNameEdit" placeholder="学员姓名或代号">
                   </div>
-                  <div v-show="remarkOpen" class="collapse-content">
-                    <input type="text" class="form-control" v-model="branchCooperationNote" placeholder="如响应速度、是否提供合同/协助沟通等">
-                  </div>
-
-                  <div v-if="withdrawUpdatedAt" class="text-muted" style="font-size:11px;margin-top:10px">
-                    撤诉状态更新于 [[ withdrawUpdatedAt ]]
-                    <button class="btn btn-link btn-sm" style="font-size:11px;padding:0 4px" @click="updateWithdrawStatus(currentTicketId)" :disabled="!currentTicketId">
-                      <i class="bi bi-arrow-repeat"></i> 重新保存
-                    </button>
+                  <div style="margin-top:14px">
+                    <label class="form-label">配合度评价（[[ selectedTicket.school_short || qr.school_short || '—' ]]）</label>
+                    <span v-for="k in coopOptions" :key="k" class="coop-opt" :class="{['on-'+k]: branchCooperation===k}" @click="branchCooperation=k">
+                      <span class="cico" :style="{background: k==='好' ? 'var(--color-success)' : (k==='中' ? 'var(--color-warning)' : 'var(--color-danger)')}">[[ k==='好' ? '✓' : (k==='中' ? '—' : '✕') ]]</span>[[ k ]]
+                    </span>
                   </div>
                 </div>
               </div>
 
-              <div class="d-flex justify-between">
-                <button class="btn btn-secondary" @click="workflowStep = 2"><i class="bi bi-arrow-left"></i> 返回退费分析</button>
-                <button class="btn btn-primary" @click="saveCaseOutcome(currentTicketId)" :disabled="archiveSaving">
-                  <span v-if="archiveSaving"><i class="bi bi-arrow-repeat spin"></i></span>
-                  <i v-else class="bi bi-archive"></i>
-                  [[ archiveSaving ? '归档中...' : '保存结果并进入文档' ]]
-                </button>
-              </div>
-            </div>
-
-            <!-- 步骤4: 结果归档/文档生成 -->
-            <div v-if="workflowStep === 4" class="fade-in">
-              <h5 class="mb-3"><i class="bi bi-file-word text-primary"></i> 结果归档与文档生成</h5>
-
-              <!-- 文档生成说明 -->
-              <div class="mb-3 p-3 rounded" style="background:var(--bg);border:1px solid var(--border)">
-                <div style="font-size:13px;color:var(--text-secondary);line-height:1.6">
-                  <i class="bi bi-info-circle text-primary"></i>
-	                  当前案件最终结果为 <strong>「[[ finalOutcome || '未归档' ]]」</strong>。
-	                  投诉登记表用于内部归档；回复函仅在上级部门要求、协商失败或处理人主动需要时生成。
-                </div>
-              </div>
-
-              <!-- 投诉登记表 -->
-              <div class="card mb-3" style="border-left:3px solid var(--primary)">
-                <div class="card-body" style="padding:14px">
-                  <div class="d-flex align-center justify-between flex-wrap" style="gap:8px">
+              <!-- ⑥ 归档 -->
+              <div class="card panel"><div class="card-header"><i class="bi bi-archive text-primary"></i> ⑤ 归档</div>
+                <div class="card-body">
+                  <div class="grid-2">
                     <div>
-                      <h6 style="font-size:14px;font-weight:600;margin-bottom:4px"><i class="bi bi-file-text text-primary"></i> 学员投诉登记表</h6>
-                      <div style="font-size:12px;color:var(--text-secondary)">记录学员基本信息和投诉处理经过</div>
-                    </div>
-                    <div class="d-flex gap-2">
-                      <button class="btn btn-primary btn-sm" @click="genRegistrationForm(currentTicketId)" :disabled="formLoading">
-                        <span v-if="formLoading"><i class="bi bi-arrow-repeat spin"></i></span>
-                        <i v-else class="bi bi-file-earmark-word"></i>
-                        [[ formLoading ? '生成中...' : '生成登记表' ]]
+                      <div class="gate-item" :class="archiveGates.notes ? 'gate-ok':'gate-no'"><i class="bi" :class="archiveGates.notes ? 'bi-check-circle-fill':'bi-x-circle'"></i> 处理情况已填写</div>
+                      <div class="gate-item" :class="archiveGates.coop ? 'gate-ok':'gate-no'"><i class="bi" :class="archiveGates.coop ? 'bi-check-circle-fill':'bi-x-circle'"></i> 配合度已评价</div>
+                      <div class="gate-item" :class="archiveGates.fee ? 'gate-ok':'gate-no'"><i class="bi" :class="archiveGates.fee ? 'bi-check-circle-fill':'bi-x-circle'"></i> 扣费明细表已确认（金额口径 · 不落盘）</div>
+                      <p class="hint" style="padding:2px 0 6px">— 回复函非必需</p>
+                      <button class="btn btn-primary" style="margin-top:10px" :disabled="!canArchive" @click="doArchive()">
+                        <i class="bi bi-archive-fill"></i> [[ selectedTicket.archive_status==='已归档' ? '✓ 已归档' : '📦 归档到此路径' ]]
                       </button>
+                      <p v-if="!canArchive" class="hint" style="margin-top:6px">未满足：[[ gateErrors.join('；') ]]</p>
                     </div>
-                  </div>
-                  <div v-if="formResult" class="alert alert-success mt-2" style="font-size:12px;padding:8px 12px;margin-bottom:0">
-                    <i class="bi bi-check-circle"></i> [[ formResult.filename ]]
-                    <a :href="'/api/reply/download?path=' + encodeURIComponent(formResult.filepath)" class="btn btn-sm btn-success ml-2" style="font-size:11px" target="_blank"><i class="bi bi-download"></i> 下载</a>
-                  </div>
-                </div>
-              </div>
-
-              <!-- 投诉回复函 -->
-              <div class="card mb-3" style="border-left:3px solid var(--success)">
-                <div class="card-body" style="padding:14px">
-                  <div class="d-flex align-center justify-between flex-wrap" style="gap:8px">
                     <div>
-                      <h6 style="font-size:14px;font-weight:600;margin-bottom:4px"><i class="bi bi-file-earmark-word text-success"></i> 投诉回复函</h6>
-	                      <div style="font-size:12px;color:var(--text-secondary)">按需生成给上级部门/驾培协会的正式公文</div>
-                    </div>
-                    <div class="d-flex gap-2">
-                      <button class="btn btn-success btn-sm" @click="genReply(qr.id_card, qr, currentTicketId)" :disabled="rpLoading">
-                        <span v-if="rpLoading"><i class="bi bi-arrow-repeat spin"></i></span>
-                        <i v-else class="bi bi-file-earmark-word"></i>
-                        [[ rpLoading ? '生成中...' : '生成回复函' ]]
+                      <button class="btn btn-secondary btn-sm" style="margin-bottom:6px" @click="genRegistrationForm(selectedTicketId)" :disabled="formLoading || !selectedTicketId">
+                        <i class="bi bi-file-earmark-text"></i> [[ formLoading ? '登记表生成中…' : '生成登记表' ]]
                       </button>
+                      <div class="path-tree">案件归档/<br>&nbsp;└─ [[ (selectedTicket.school_short) || '未知' ]]/<br>
+                        &nbsp;&nbsp;&nbsp;&nbsp;├─ [[ selectedTicket.complaint_date ]]_[[ selectedTicket.student_name ]]_投诉登记表.docx <span class="ok" v-if="selectedTicket.registration_form_path">✓ 已生成</span><span class="dim" v-else>未生成·可点上方按钮生成</span><br>
+                        &nbsp;&nbsp;&nbsp;&nbsp;└─ [[ selectedTicket.complaint_date ]]_[[ selectedTicket.student_name ]]_投诉回复函.docx <span class="dim">[[ replySaved ? '✓ 已生成' : '（可选·未生成）' ]]</span></div>
                     </div>
                   </div>
-                  <div v-if="rpErr" class="alert alert-danger mt-2" style="font-size:12px;padding:8px 12px;margin-bottom:0">[[ rpErr ]]</div>
-                  <div v-if="rpResult" class="alert mt-2" :class="rpResult.outdated ? 'alert-warning' : 'alert-success'" style="font-size:12px;padding:8px 12px;margin-bottom:0">
-                    <span v-if="rpResult.outdated" class="badge bg-danger" style="margin-right:6px">已过时</span>
-                    <i :class="rpResult.outdated ? 'bi bi-exclamation-triangle' : 'bi bi-check-circle'"></i> 回复函：[[ rpResult.filename ]]
-                    <a :href="'/api/reply/download?path=' + encodeURIComponent(rpResult.filepath)" class="btn btn-sm btn-success ml-2" style="font-size:11px" target="_blank"><i class="bi bi-download"></i> 下载</a>
-                  </div>
-                  <!-- 回复函版本列表 -->
-                  <div v-if="rpResult && rpResult.all_versions && rpResult.all_versions.length > 1" class="mt-2" style="font-size:12px;padding:6px 10px;background:var(--bg);border-radius:6px">
-                    <span style="color:var(--text-secondary)"><i class="bi bi-files"></i> 全部版本：</span>
-                    <span v-for="(ver, vi) in rpResult.all_versions" :key="vi" style="margin-right:10px">
-                      <a :href="'/api/reply/download?path=' + encodeURIComponent(ver.filepath)" target="_blank" style="font-size:12px">[[ ver.filename ]]</a>
-                    </span>
-                  </div>
                 </div>
               </div>
-
-              <div class="d-flex justify-between">
-                <button class="btn btn-secondary" @click="loadCommunications(currentTicketId); workflowStep = 3"><i class="bi bi-arrow-left"></i> 返回沟通</button>
-                <button class="btn btn-primary" @click="workflowStep = 5">
-                  进入飞书可选归档 <i class="bi bi-arrow-right"></i>
-                </button>
-              </div>
             </div>
 
-            <!-- 步骤5: 飞书可选归档 -->
-            <div v-if="workflowStep === 5" class="fade-in">
-              <h5 class="mb-3"><i class="bi bi-send text-primary"></i> 飞书可选归档</h5>
-              <div class="card mb-3" style="background:var(--bg)">
-                <div class="card-body" style="padding:14px">
-                  <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:12px">
-                    <div><span class="info-label">学员姓名</span><div class="info-value">[[ qr.name ]]</div></div>
-                    <div><span class="info-label">身份证号</span><div class="info-value font-mono">[[ qr.id_card ]]</div></div>
-                    <div><span class="info-label">所属驾校</span><div class="info-value">[[ qr.school_short ]]</div></div>
-                    <div><span class="info-label">应退金额</span><div class="info-value" style="color:var(--success)">[[ ar ? ar.refund : 0 ]]元</div></div>
-	                    <div><span class="info-label">最终结果</span><div class="info-value">[[ finalOutcome || '-' ]]</div></div>
-	                    <div><span class="info-label">分校配合度</span><div class="info-value">[[ branchCooperation || '-' ]]</div></div>
-                  </div>
+            <!-- 右栏：学员档案 / 时间线 / 在途案件 -->
+            <aside class="rail">
+              <div class="card student-card"><div class="card-header"><i class="bi bi-person-badge"></i> 学员档案</div>
+                <div class="card-body" style="padding:8px 16px">
+                  <div class="row"><span class="k">姓名（代号）</span><b class="v">[[ (selectedTicket.student_name) || (qr.name) || '—' ]]（[[ (selectedTicket.school_short) || (qr.school_short) || '—' ]]）</b></div>
+                  <div class="row"><span class="k">车型</span><b class="v">[[ qr.license_type || 'C1' ]]</b></div>
+                  <div class="row"><span class="k">报名日期</span><b class="v">[[ qr.registration_date || '-' ]]</b></div>
+                  <div class="row"><span class="k">当前进度</span><b class="v">[[ qr.exam_stage || selectedTicket.exam_stage || '-' ]]</b></div>
+                  <div class="row"><span class="k">实缴学费</span><b class="v">¥[[ money(ar ? (ar.actual_paid || ar.paid_amount) : ((qr.driving_fee && qr.driving_fee.pay_fee) || 0)) ]]<span v-if="!ar && qr.driving_fee && qr.driving_fee.pay_fee" class="hint" style="font-weight:400">（平台已缴，待合同分析确认）</span></b></div>
+                  <div class="row"><span class="k">监管剩余</span><b class="v">[[ qr.driving_fee && qr.driving_fee.residue_supervise_amt != null ? '¥' + money(qr.driving_fee.residue_supervise_amt) : '-' ]]</b></div>
+                  <div class="row"><span class="k">经营单位</span><b class="v">[[ selectedTicket.school_name || qr.school_name || '—' ]]</b></div>
+                  <div class="row"><span class="k">合同状态</span><b class="v">[[ qr.contract_available ? '有电子合同' : (qr.contract_check_deferred ? '延后检查' : '无合同') ]]</b></div>
                 </div>
               </div>
-              <div v-if="fsResult" class="alert alert-success">
-                <i class="bi bi-check-circle"></i> 已提交到飞书，处理编号: [[ fsResult.handle_no ]]
-                <div class="mt-2" style="font-size:13px">
-                  <div><i class="bi bi-file-earmark-word"></i> 已生成文档：</div>
-                  <div v-if="formResult" style="margin:4px 0">
-                    <a :href="'/api/reply/download?path=' + encodeURIComponent(formResult.filepath)" target="_blank" style="font-size:12px"><i class="bi bi-download"></i> [[ formResult.filename ]]</a>
-                  </div>
-                  <div v-if="rpResult" style="margin:4px 0">
-                    <a :href="'/api/reply/download?path=' + encodeURIComponent(rpResult.filepath)" target="_blank" style="font-size:12px"><i class="bi bi-download"></i> [[ rpResult.filename ]]</a>
-                  </div>
-                  <div v-if="rpResult && rpResult.all_versions && rpResult.all_versions.length > 1" style="margin:2px 0 0 16px;font-size:11px;color:var(--text-secondary)">
-                    历史版本：
-                    <span v-for="(ver, vi) in rpResult.all_versions" :key="vi" style="margin-right:8px">
-                      <a :href="'/api/reply/download?path=' + encodeURIComponent(ver.filepath)" target="_blank">[[ ver.filename ]]</a>
-                    </span>
-                  </div>
+              <div class="card"><div class="card-header"><i class="bi bi-clock-history"></i> 办理时间线</div>
+                <div class="card-body" style="font-size:12px">
+                  <div class="tl-item"><span class="d">[[ selectedTicket.complaint_date || form.complaint_date ]]</span>投诉登记，计入统计</div>
+                  <div class="tl-item"><span class="d">—</span>三系统查询<span v-if="qr && qr.name">完成</span><span v-else>待查询</span></div>
+                  <div class="tl-item"><span class="d">—</span>扣费明细<span :class="selectedTicket.fee_plan_status==='confirmed' ? 'gate-ok':'gate-no'">[[ selectedTicket.fee_plan_status==='confirmed' ? '已确认' : '待确认' ]]</span></div>
+                  <div class="tl-item"><span class="d">—</span>[[ selectedTicket.archive_status==='已归档' ? '已归档' : '待归档' ]]</div>
                 </div>
-                <div class="mt-2">
-                  <button class="btn btn-sm btn-success" @click="updateTicketStatus(currentTicketId, '已完结')">
-                    <i class="bi bi-check2"></i> 标记为已完结
-                  </button>
-                </div>
-              </div>
-              <div v-if="!fsResult" class="d-flex justify-between">
-                <button class="btn btn-secondary" @click="workflowStep = 4"><i class="bi bi-arrow-left"></i> 返回文档</button>
-	                <button class="btn btn-primary" @click="submitFeishu({ticket_id: currentTicketId, id_card: qr.id_card, name: qr.name, phone: qr.phone, school_short: qr.school_short, license_type: qr.license_type, exam_stage: qr.exam_stage, complaint_channel: form.source_channel, complaint_date: form.complaint_date, complaint_type: form.complaint_type, registration_fee: ar ? ar.total_fee : 0, actual_paid: ar ? ar.actual_paid : 0, refund: ar ? ar.refund : 0, deduction: ar ? ar.total_deduction : 0, final_outcome: finalOutcome, branch_cooperation: branchCooperation, handle_status: '已完结'})" :disabled="fsLoading">
-                  <span v-if="fsLoading"><i class="bi bi-arrow-repeat spin"></i></span>
-                  <i v-else class="bi bi-send"></i>
-                  [[ fsLoading ? '提交中...' : '提交到飞书' ]]
-                </button>
               </div>
-            </div>
-          </div>
-        </div>
-      </div>
-      <!-- ════════════════════════════════════════ -->
-      <!-- 处理历史页面（仪表盘 + 列表） -->
-      <!-- ════════════════════════════════════════ -->
-      <div v-if="page==='history'" class="fade-in">
-        <div class="page-title"><i class="bi bi-clock-history text-primary"></i> 处理历史</div>
-
-        <!-- 仪表盘：时间筛选 + 统计卡片 -->
-        <div class="card mb-3">
-          <div class="card-header d-flex align-center justify-between flex-wrap" style="gap:8px">
-            <div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap">
-              <div><i class="bi bi-bar-chart"></i> 数据看板</div>
-              <div class="d-flex gap-1" style="background:var(--bg);border-radius:8px;padding:3px">
-                <button class="btn btn-sm" :class="statScope==='all' ? 'btn-primary' : 'btn-secondary'" @click="setStatScope('all')" style="font-size:11px">全部</button>
-                <button class="btn btn-sm" :class="statScope==='branch' ? 'btn-primary' : 'btn-secondary'" @click="setStatScope('branch')" style="font-size:11px">分校</button>
-                <button class="btn btn-sm" :class="statScope==='store' ? 'btn-primary' : 'btn-secondary'" @click="setStatScope('store')" style="font-size:11px">分店</button>
-              </div>
-              <select class="form-control form-control-sm" style="width:auto;font-size:12px" v-model="drillCode" @change="onDrillSelect" title="按代号筛选（看板与工单列表联动）">
-                <option value="">全部代号</option>
-                <option v-for="item in schoolOptions" :key="item.code" :value="item.code">[[ item.name ]]（[[ item.code ]]）</option>
-              </select>
-            </div>
-            <div class="d-flex gap-2 flex-wrap align-center">
-              <span style="font-size:11px;color:var(--gray-500)">时间：</span>
-              <input type="date" class="form-control" style="max-width:120px;font-size:12px" v-model="chartDateStart" @change="onDateChange" title="开始日期">
-              <span style="color:var(--gray-400)">~</span>
-              <input type="date" class="form-control" style="max-width:120px;font-size:12px" v-model="chartDateEnd" @change="onDateChange" title="结束日期">
-              <button class="btn btn-sm btn-secondary" @click="setChartPeriod('week')" style="font-size:11px">本周</button>
-              <button class="btn btn-sm btn-secondary" @click="setChartPeriod('month')" style="font-size:11px">本月</button>
-              <button class="btn btn-sm btn-secondary" @click="setChartPeriod('year')" style="font-size:11px">本年</button>
-              <button class="btn btn-sm btn-secondary" @click="setChartPeriod('all')" style="font-size:11px">全部</button>
-              <button class="btn btn-sm btn-outline-primary" @click="loadVehicleCounts(); vehicleModalOpen=true" style="font-size:11px"><i class="bi bi-truck"></i> 维护车辆数</button>
-            </div>
-          </div>
-          <div class="card-body">
-            <!-- 数字卡片 -->
-              <div class="stats-row">
-                <div class="stat-card"><div class="stat-value text-primary">[[ periodStat.this_total ]]</div><div class="stat-label">选定时间</div></div>
-                <div class="stat-card"><div class="stat-value text-warning">[[ periodStat.pending ]]</div><div class="stat-label">待处理</div></div>
-                <div class="stat-card"><div class="stat-value text-info">[[ periodStat.processing ]]</div><div class="stat-label">处理中</div></div>
-                <div class="stat-card"><div class="stat-value text-success">[[ periodStat.completed ]]</div><div class="stat-label">已完结</div></div>
-                <div class="stat-card"><div class="stat-value text-danger">[[ durationStats?.overdue_count || 0 ]]</div><div class="stat-label">超时未处理</div></div>
-                <div class="stat-card"><div class="stat-value text-primary">[[ durationStats?.avg_processing_hours || 0 ]]<small style="font-size:14px">h</small></div><div class="stat-label">平均处理时长</div></div>
-                <div class="stat-card"><div class="stat-value text-danger">[[ boardComplaintRate != null ? boardComplaintRate + '%' : '-' ]]</div><div class="stat-label">投诉率（有效÷车辆）</div></div>
-              </div>
-              <div v-if="periodStat.integrity_issues && periodStat.integrity_issues.total" class="alert alert-warning mt-3 mb-0" style="font-size:12px;padding:8px 12px">
-                <i class="bi bi-exclamation-triangle"></i>
-                历史案件需补全闭环信息：[[ periodStat.integrity_issues.total ]] 条（归档状态冲突 [[ periodStat.integrity_issues.archived_not_completed ]] 条，已完结缺最终结果 [[ periodStat.integrity_issues.completed_without_final_outcome ]] 条）。请通过“详情 / 重处理”复核后重新归档。
-              </div>
-
-            <!-- 图表区域 -->
-            <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:16px;margin-top:16px">
-              <!-- 趋势图 -->
-              <div class="chart-card">
-                <h6 class="chart-title"><i class="bi bi-graph-up"></i> 投诉趋势</h6>
-                <div ref="trendChartRef" style="width:100%;height:200px"></div>
-              </div>
-              <!-- 驾校排行 -->
-              <div class="chart-card">
-                <h6 class="chart-title"><i class="bi bi-building"></i> 投诉量 TOP10 <span style="font-size:11px;color:var(--gray-500);font-weight:400">（点击网点看详情）</span></h6>
-                <div ref="schoolChartRef" style="width:100%;height:200px"></div>
-                <div v-if="stats.by_school && stats.by_school.length" style="margin-top:8px;border-top:1px solid var(--border);padding-top:6px">
-                  <div v-for="item in stats.by_school.slice(0,5)" :key="item.code" style="display:flex;justify-content:space-between;font-size:12px;padding:3px 0">
-                    <a href="javascript:void(0)" @click="drillInto(item.code, item.school)" style="color:var(--primary)"><b>[[ item.school ]]</b> <span style="color:var(--gray-500)">[[ item.code ]]</span></a>
-                    <span>[[ item.count ]] 起<span v-if="item.complaint_rate != null" style="color:var(--gray-500)"> · 率 [[ item.complaint_rate ]]%</span></span>
-                  </div>
+              <div class="card">
+                <div class="acc-h" :class="{open: railOpen}" @click="railOpen=!railOpen">
+                  <i class="bi bi-folder2-open"></i> 在途案件（[[ ticketsOpen.length ]]）<span class="arr">▸</span>
                 </div>
-              </div>
-              <!-- 投诉率排行 -->
-              <div class="chart-card">
-                <h6 class="chart-title"><i class="bi bi-percent"></i> 投诉率 TOP10 <span style="font-size:11px;color:var(--gray-500);font-weight:400">有效投诉÷车辆</span></h6>
-                <div v-if="stats.by_rate && stats.by_rate.length">
-                  <div v-for="(item, idx) in stats.by_rate.slice(0,10)" :key="item.code" style="display:flex;justify-content:space-between;align-items:center;font-size:12px;padding:4px 0;border-bottom:1px solid var(--border)">
-                    <span><span class="badge" :class="idx<3 ? 'bg-danger' : 'bg-secondary'" style="font-size:10px">[[ idx+1 ]]</span>
-                      <a href="javascript:void(0)" @click="drillInto(item.code, item.school)" style="color:var(--primary)">[[ item.school ]]</a>
-                      <span style="color:var(--gray-500)">[[ item.code ]]</span>
-                    </span>
-                    <strong style="color:var(--danger)">[[ item.complaint_rate ]]%</strong>
+                <div v-show="railOpen">
+                  <div v-if="!ticketsOpen.length" class="hint" style="padding:10px 16px">暂无在途案件</div>
+                  <div v-for="x in ticketsOpen" :key="x.id" class="mini-case" :class="{on: x.id===selectedTicketId}" @click="openCase(x.id)">
+                    <div class="l1">[[ x.student_name ]]<span class="uc">[[ x.school_short ]]</span><span style="flex:1"></span><span class="badge" :class="x.fee_plan_status==='confirmed'?'badge-completed':'badge-pending'">[[ x.fee_plan_status==='confirmed'?'✓':'·' ]]</span></div>
+                    <div class="l2">[[ x.id_card ]]</div>
+                    <div class="l3"><span>[[ x.complaint_date ]]</span><span>[[ x.exam_stage || x.handle_status ]]</span></div>
                   </div>
                 </div>
-                <div v-else style="font-size:12px;color:var(--text-secondary)">暂无投诉率（需先维护车辆数）</div>
-              </div>
-              <!-- 投诉类型 -->
-              <div class="chart-card">
-                <h6 class="chart-title"><i class="bi bi-pie-chart"></i> 投诉类型分布</h6>
-                <div ref="typeChartRef" style="width:100%;height:200px"></div>
               </div>
-              <!-- 处理状态 -->
-	              <div class="chart-card">
-	                <h6 class="chart-title"><i class="bi bi-check-circle"></i> 处理状态</h6>
-	                <div ref="statusChartRef" style="width:100%;height:200px"></div>
-	              </div>
-	              <div class="chart-card">
-	                <h6 class="chart-title"><i class="bi bi-archive"></i> 最终结果</h6>
-	                <div v-if="stats.by_outcome && stats.by_outcome.length">
-	                  <div v-for="item in stats.by_outcome" :key="item.outcome" class="d-flex justify-between" style="font-size:12px;padding:6px 0;border-bottom:1px solid var(--border)">
-	                    <span>[[ item.outcome ]]</span><strong>[[ item.count ]]</strong>
-	                  </div>
-	                </div>
-	                <div v-else style="font-size:12px;color:var(--text-secondary)">暂无归档结果</div>
-	              </div>
-	              <div class="chart-card">
-	                <h6 class="chart-title"><i class="bi bi-building-check"></i> 分校配合度</h6>
-	                <div v-if="stats.by_branch_cooperation && stats.by_branch_cooperation.length">
-	                  <div v-for="item in stats.by_branch_cooperation" :key="item.cooperation" class="d-flex justify-between" style="font-size:12px;padding:6px 0;border-bottom:1px solid var(--border)">
-	                    <span>[[ item.cooperation ]]</span><strong>[[ item.count ]]</strong>
-	                  </div>
-	                </div>
-	                <div v-else style="font-size:12px;color:var(--text-secondary)">暂无配合度记录</div>
-	              </div>
-	              <div class="chart-card" style="grid-column:1/-1">
-	                <h6 class="chart-title"><i class="bi bi-arrow-repeat"></i> 网点环比 / 同比 <span style="font-size:11px;color:var(--gray-500);font-weight:400">本月 vs 上月 vs 去年同月 · 点击代号下钻</span></h6>
-	                <table style="width:100%;font-size:12px;border-collapse:collapse">
-	                  <tr style="color:var(--gray-500)">
-	                    <th style="text-align:left;padding:4px 6px;border-bottom:1px solid var(--border)">网点</th>
-	                    <th style="padding:4px 6px;border-bottom:1px solid var(--border)">车辆</th>
-	                    <th style="padding:4px 6px;border-bottom:1px solid var(--border)">本月投诉</th>
-	                    <th style="padding:4px 6px;border-bottom:1px solid var(--border)">上月</th>
-	                    <th style="padding:4px 6px;border-bottom:1px solid var(--border)">环比</th>
-	                    <th style="padding:4px 6px;border-bottom:1px solid var(--border)">投诉率</th>
-	                  </tr>
-	                  <tr v-for="item in stats.by_school" :key="item.code">
-	                    <td style="text-align:left;padding:4px 6px;border-bottom:1px solid var(--border)">
-	                      <a href="javascript:void(0)" @click="drillInto(item.code, item.school)" style="color:var(--primary)">[[ item.school ]] <span style="color:var(--gray-500)">[[ item.code ]]</span></a>
-	                    </td>
-	                    <td style="padding:4px 6px;border-bottom:1px solid var(--border)">[[ item.vehicle_count || '-' ]]</td>
-	                    <td style="padding:4px 6px;border-bottom:1px solid var(--border)">[[ item.count ]]</td>
-	                    <td style="padding:4px 6px;border-bottom:1px solid var(--border)">-</td>
-	                    <td style="padding:4px 6px;border-bottom:1px solid var(--border)">-</td>
-	                    <td style="padding:4px 6px;border-bottom:1px solid var(--border)">[[ item.complaint_rate != null ? item.complaint_rate + '%' : '-' ]]</td>
-	                  </tr>
-	                </table>
-	                <div v-if="stats.monthly_compare && stats.monthly_compare.current_month" style="margin-top:8px;font-size:12px;color:var(--text-secondary)">
-	                  全机构：[[ stats.monthly_compare.current_month ]] 投诉 [[ stats.monthly_compare.current_month_count ]] 起，上月 [[ stats.monthly_compare.previous_month_count ]] 起
-	                  <span v-if="stats.monthly_compare.mom_change != null" :style="{color: stats.monthly_compare.mom_change > 0 ? 'var(--danger)' : 'var(--success)'}">
-	                    （环比 [[ stats.monthly_compare.mom_change > 0 ? '+' : '' ]][[ stats.monthly_compare.mom_change ]]%）
-	                  </span>
-	                </div>
-	              </div>
-	            </div>
-
+            </aside>
           </div>
         </div>
-
-        <!-- 工单列表 --><!-- 工单列表 -->
-        <div class="card">
-          <div class="card-header"><i class="bi bi-clock-history"></i> 工单列表</div>
-          <div class="card-body">
-            <!-- 筛选栏 -->
-            <div class="d-flex gap-2 mb-3 flex-wrap" style="align-items:flex-end">
-              <div>
-                <label class="form-label" style="font-size:11px">关键词</label>
-                <input type="text" class="form-control" style="max-width:160px" v-model="hSearch" @keyup.enter="hPage=1;loadHist()" placeholder="姓名/身份证/工单号">
-              </div>
-              <div>
-                <label class="form-label" style="font-size:11px">状态</label>
-                <select class="form-control" style="max-width:110px" v-model="statusFilter" @change="hPage=1;loadHist()">
-                  <option value="">全部</option>
-                  <option value="待处理">待处理</option>
-                  <option value="处理中">处理中</option>
-                  <option value="已完结">已完结</option>
-                </select>
-              </div>
-              <div style="display:flex;align-items:flex-end;gap:6px">
-                <span style="font-size:11px;color:var(--gray-500);padding-bottom:8px">代号与时间跟随上方看板筛选</span>
-              </div>
-              <div>
-                <button class="btn btn-primary" @click="hPage=1;loadHist()" style="height:34px">
-                  <i class="bi bi-search"></i> 查询
-                </button>
-              </div>
-              <div>
-                <button class="btn btn-secondary" @click="exportTickets()" style="height:34px" :disabled="hLoading">
-                  <i class="bi bi-download"></i> 导出Excel
-                </button>
-              </div>
-            </div>
-
-            <!-- 表格 -->
-            <div class="table-wrap">
-              <table class="history-table">
-                <thead>
-                  <tr>
-                    <th style="width:40px">#</th>
-                    <th style="width:50px">代号</th>
-                    <th style="width:80px">姓名</th>
-                    <th style="width:160px">身份证</th>
-                    <th style="width:70px">来源</th>
-                    <th style="width:90px">日期</th>
-                    <th style="width:50px">次数</th>
-                    <th style="width:70px">应退</th>
-                    <th style="width:70px">状态</th>
-                    <th style="width:140px">操作</th>
-                  </tr>
-                </thead>
-                <tbody>
-                  <template v-for="(group, idx) in groupedHistory" :key="group.id_card">
-                    <!-- 聚合行（同一学员多次投诉） -->
-                    <tr v-if="group.items.length > 1" class="group-row" @click="group.expanded = !group.expanded">
-                      <td style="color:var(--gray-400);text-align:center">[[ (hPage-1)*hLimit + idx + 1 ]]</td>
-                      <td class="text-center">[[ group.school_short || '-' ]]</td>
-                      <td class="font-semibold">[[ group.student_name ]]</td>
-                      <td class="font-mono" style="font-size:12px">[[ group.id_card ]]</td>
-                      <td class="text-center">[[ group.source_channel || '-' ]]</td>
-                      <td style="white-space:nowrap;font-size:12px">[[ group.latest_date ? group.latest_date.substring(5) : '-' ]]</td>
-                      <td style="text-align:center">
-                        <span class="badge badge-warning" style="font-size:10px">[[ group.items.length ]]次</span>
-                      </td>
-                      <td style="text-align:right;font-weight:600;color:var(--green-600)">[[ group.total_refund || 0 ]]元</td>
-                      <td>
-                        <span style="display:inline-flex;align-items:center;gap:2px">
-                          <select class="form-control form-control-sm" :value="group.status"
-                            @click.stop @change="updateTicketStatus(group.items[0]?.id, $event.target.value)"
-                            style="font-size:10px;padding:2px 12px 2px 4px;min-width:50px;border:1px solid transparent;border-radius:4px;cursor:pointer;font-weight:600;-webkit-appearance:none;appearance:none;background:transparent"
-                            :style="{color: group.status === '已完结' ? 'var(--green)' : group.status === '处理中' ? 'var(--blue)' : '#d97706'}"
-                            @mouseenter="$event.target.style.borderColor='var(--gray-300)'"
-                            @mouseleave="$event.target.style.borderColor='transparent'">
-                            <option value="待处理">待处理</option>
-                            <option value="处理中">处理中</option>
-                            <option value="已完结">已完结</option>
-                          </select>
-                          <i class="bi bi-chevron-down" style="font-size:8px;color:var(--gray-400);margin-left:-14px;pointer-events:none"></i>
-                        </span>
-                      </td>
-                      <td>
-                        <button class="btn btn-sm btn-secondary" @click.stop="loadFromHist(group.items[0])">
-                          <i class="bi bi-arrow-clockwise"></i> 重处理
-                        </button>
-                      </td>
-                    </tr>
-                    <!-- 展开的子行 -->
-                    <tr v-if="group.expanded" v-for="item in group.items" :key="item.id" class="sub-row">
-                      <td></td>
-                      <td></td>
-                      <td></td>
-                      <td class="font-mono" style="font-size:11px;color:var(--gray-400)">[[ item.id_card ]]</td>
-                      <td class="text-center" style="font-size:12px">[[ item.source_channel || '-' ]]</td>
-                      <td style="white-space:nowrap;font-size:12px">[[ item.complaint_date ? item.complaint_date.substring(5) : '-' ]]</td>
-                      <td></td>
-                      <td style="text-align:right;font-size:12px">[[ item.refund_fee || 0 ]]元</td>
-                      <td>
-                        <span style="display:inline-flex;align-items:center;gap:2px">
-                          <select class="form-control form-control-sm" :value="item.handle_status"
-                            @change="updateTicketStatus(item.id, $event.target.value)"
-                            style="font-size:10px;padding:2px 12px 2px 4px;min-width:50px;border:1px solid transparent;border-radius:4px;cursor:pointer;font-weight:600;-webkit-appearance:none;appearance:none;background:transparent"
-                            :style="{color: item.handle_status === '已完结' ? 'var(--green)' : item.handle_status === '处理中' ? 'var(--blue)' : '#d97706'}"
-                            @mouseenter="$event.target.style.borderColor='var(--gray-300)'"
-                            @mouseleave="$event.target.style.borderColor='transparent'">
-                            <option value="待处理">待处理</option>
-                            <option value="处理中">处理中</option>
-                            <option value="已完结">已完结</option>
-                          </select>
-                          <i class="bi bi-chevron-down" style="font-size:8px;color:var(--gray-400);margin-left:-14px;pointer-events:none"></i>
-                        </span>
-                      </td>
-                      <td>
-                        <button class="btn btn-sm btn-outline" @click="showTicketDetail(item.id)" style="font-size:12px"><i class="bi bi-eye"></i> 详情</button>
-                      </td>
-                    </tr>
-                    <!-- 单行（仅一次投诉） -->
-                    <tr v-if="group.items.length === 1" :key="group.items[0].id">
-                      <td style="color:var(--gray-400);text-align:center">[[ (hPage-1)*hLimit + idx + 1 ]]</td>
-                      <td class="text-center">[[ group.school_short || '-' ]]</td>
-                      <td class="font-semibold">[[ group.student_name ]]</td>
-                      <td class="font-mono" style="font-size:12px">[[ group.id_card ]]</td>
-                      <td class="text-center">[[ group.source_channel || '-' ]]</td>
-                      <td style="white-space:nowrap;font-size:12px">[[ group.latest_date ? group.latest_date.substring(5) : '-' ]]</td>
-                      <td style="text-align:center;color:var(--gray-300)">-</td>
-                      <td style="text-align:right;font-weight:600;color:var(--green-600)">[[ group.total_refund || 0 ]]元</td>
-                      <td>
-                        <span style="display:inline-flex;align-items:center;gap:2px">
-                          <select class="form-control form-control-sm" :value="group.items[0].handle_status"
-                            @change="updateTicketStatus(group.items[0].id, $event.target.value)"
-                            style="font-size:10px;padding:2px 12px 2px 4px;min-width:50px;border:1px solid transparent;border-radius:4px;cursor:pointer;font-weight:600;-webkit-appearance:none;appearance:none;background:transparent"
-                            :style="{color: group.items[0].handle_status === '已完结' ? 'var(--green)' : group.items[0].handle_status === '处理中' ? 'var(--blue)' : '#d97706'}"
-                            @mouseenter="$event.target.style.borderColor='var(--gray-300)'"
-                            @mouseleave="$event.target.style.borderColor='transparent'">
-                            <option value="待处理">待处理</option>
-                            <option value="处理中">处理中</option>
-                            <option value="已完结">已完结</option>
-                          </select>
-                          <i class="bi bi-chevron-down" style="font-size:8px;color:var(--gray-400);margin-left:-14px;pointer-events:none"></i>
-                        </span>
-                      </td>
-                      <td>
-                        <button class="btn btn-sm btn-outline" @click="showTicketDetail(group.items[0].id)" style="font-size:12px">
-                          <i class="bi bi-eye"></i> 详情
-                        </button>
-                        <button class="btn btn-sm btn-secondary" @click="loadFromHist(group.items[0])">
-                          <i class="bi bi-arrow-clockwise"></i> 重处理
-                        </button>
-                        <button class="btn btn-sm btn-primary" @click="goFeishu(group.items[0])" v-if="group.items[0].student_name" style="margin-left:4px">
-                          <i class="bi bi-send"></i> 飞书
-                        </button>
-                      </td>
-                    </tr>
-                  </template>
-                  <tr v-if="!groupedHistory.length"><td colspan="10" class="empty-state"><i class="bi bi-inbox icon"></i><div class="text">暂无记录</div></td></tr>
-                </tbody>
-              </table>
-            </div>
-
-            <!-- 分页 -->
-            <div class="d-flex justify-between align-center mt-3 flex-wrap" style="gap:8px">
-              <div class="d-flex gap-2 align-center">
-                <span style="font-size:12px;color:var(--gray-500)">共 [[ hTotal ]] 条</span>
-                <select class="form-control form-control-sm" style="width:auto" v-model="hLimit" @change="loadHist">
-                  <option value="10">10条/页</option>
-                  <option value="20">20条/页</option>
-                  <option value="50">50条/页</option>
-                </select>
-              </div>
-              <div class="d-flex gap-1">
-                <button class="btn btn-sm btn-secondary" @click="hPage=1;loadHist()" :disabled="hPage<=1"><i class="bi bi-chevron-double-left"></i></button>
-                <button class="btn btn-sm btn-secondary" @click="hPage>1 && (hPage--,loadHist())" :disabled="hPage<=1"><i class="bi bi-chevron-left"></i></button>
-                <span style="line-height:30px;font-size:12px;color:var(--gray-600);padding:0 8px">[[ hPage ]] / [[ hTotalPages ]]</span>
-                <button class="btn btn-sm btn-secondary" @click="hPage<hTotalPages && (hPage++,loadHist())" :disabled="hPage>=hTotalPages"><i class="bi bi-chevron-right"></i></button>
-                <button class="btn btn-sm btn-secondary" @click="hPage=hTotalPages;loadHist()" :disabled="hPage>=hTotalPages"><i class="bi bi-chevron-double-right"></i></button>
-              </div>
-            </div>
-          </div>
-        </div>
-      </div>
+      </section>
 
       <!-- ════════════════════════════════════════ -->
-      <!-- 系统设置页面 -->
+      <!-- 视图四：统计看板 -->
       <!-- ════════════════════════════════════════ -->
-      <div v-if="page==='settings' && cfg" class="fade-in">
-        <div class="page-title"><i class="bi bi-gear text-primary"></i> 系统设置</div>
-        <div class="card">
-          <div class="card-header"><i class="bi bi-hdd-network"></i> 三系统登录配置</div>
-          <div class="card-body">
-            <!-- 水平三系统配置 -->
-            <div class="sys-config-grid">
-              <div class="sys-config-card">
-                <h6 class="src-internal"><i class="bi bi-database"></i> 内部系统</h6>
-                <div class="form-group">
-                  <div><label class="form-label">地址</label><input class="form-control" v-model="cfg.internal_system.base_url"></div>
-                  <div><label class="form-label">用户名</label><input class="form-control" v-model="cfg.internal_system.username"></div>
-                  <div><label class="form-label">密码</label><input type="password" class="form-control" v-model="cfg.internal_system.password"></div>
-                </div>
-              </div>
-              <div class="sys-config-card">
-                <h6 class="src-third"><i class="bi bi-diagram-3"></i> 第三系统</h6>
-                <div class="form-group">
-                  <div><label class="form-label">地址</label><input class="form-control" v-model="cfg.third_system.base_url"></div>
-                  <div><label class="form-label">用户名</label><input class="form-control" v-model="cfg.third_system.username"></div>
-                  <div><label class="form-label">密码</label><input type="password" class="form-control" v-model="cfg.third_system.password"></div>
-                </div>
-              </div>
-              <div class="sys-config-card">
-                <h6 class="src-driving"><i class="bi bi-car-front"></i> 驾培系统</h6>
-                <div class="form-group">
-                  <div><label class="form-label">地址</label><input class="form-control" v-model="cfg.driving_system.base_url"></div>
-                  <div><label class="form-label">用户名</label><input class="form-control" v-model="cfg.driving_system.username"></div>
-                  <div><label class="form-label">密码</label><input type="password" class="form-control" v-model="cfg.driving_system.password"></div>
-                </div>
-              </div>
+      <section v-if="view==='kanban'" class="fade-in">
+        <div class="page-title">投诉统计 <span class="hint" style="font-size:12px;font-weight:400">口径：投诉即入账；撤诉不计有效投诉、单独列示</span></div>
+
+        <!-- 筛选工具条：时间 + 范围（图表与明细全局联动） -->
+        <div class="kb-filter">
+          <div class="kb-group">
+            <span class="kb-label"><i class="bi bi-calendar3"></i> 时间</span>
+            <div class="kb-seg">
+              <button :class="{active:chartPeriod==='week'}" @click="setChartPeriod('week')">本周</button>
+              <button :class="{active:chartPeriod==='month'}" @click="setChartPeriod('month')">本月</button>
+              <button :class="{active:chartPeriod==='year'}" @click="setChartPeriod('year')">今年</button>
+              <button :class="{active:chartPeriod==='all'}" @click="setChartPeriod('all')">全部</button>
+              <button :class="{active:chartPeriod==='custom'}" @click="setChartPeriod('custom')">自定义</button>
             </div>
+            <template v-if="chartPeriod==='custom'">
+              <input type="date" class="form-control" style="width:auto;padding:5px 8px" v-model="chartDateStart" @change="onDateChange()">
+              <span class="hint">至</span>
+              <input type="date" class="form-control" style="width:auto;padding:5px 8px" v-model="chartDateEnd" @change="onDateChange()">
+            </template>
           </div>
-        </div>
-
-        <div class="card">
-          <div class="card-header"><i class="bi bi-cpu"></i> AI大模型配置</div>
-          <div class="card-body">
-            <div style="font-size:12px;color:var(--text-secondary);margin-bottom:10px">API地址可填写 OpenAI 兼容 base_url，例如 /compatible-mode/v1，保存时会自动补全 /chat/completions。</div>
-            <div class="form-row">
-              <div><label class="form-label">默认API地址</label><input class="form-control" v-model="cfg.llm.api_url"></div>
-              <div><label class="form-label">默认模型</label><input class="form-control" v-model="cfg.llm.model"></div>
-              <div><label class="form-label">默认API Key</label><input type="password" class="form-control" v-model="cfg.llm.api_key"></div>
-              <div><label class="form-label">默认Max Tokens</label><input type="number" class="form-control" v-model="cfg.llm.max_tokens"></div>
-            </div>
-            <div class="form-row" style="margin-top:12px">
-              <div><label class="form-label">工单识别 API地址</label><input class="form-control" v-model="cfg.llm_intake.api_url" placeholder="留空则使用默认配置"></div>
-              <div><label class="form-label">工单识别模型</label><input class="form-control" v-model="cfg.llm_intake.model"></div>
-              <div><label class="form-label">工单识别 API Key</label><input type="password" class="form-control" v-model="cfg.llm_intake.api_key"></div>
-              <div><label class="form-label">Max Tokens</label><input type="number" class="form-control" v-model="cfg.llm_intake.max_tokens"></div>
-            </div>
-            <div class="form-row" style="margin-top:12px">
-              <div><label class="form-label">合同图片识别 API地址</label><input class="form-control" v-model="cfg.llm_contract_vision.api_url" placeholder="建议使用多模态模型"></div>
-              <div><label class="form-label">合同图片识别模型</label><input class="form-control" v-model="cfg.llm_contract_vision.model"></div>
-              <div><label class="form-label">合同图片识别 API Key</label><input type="password" class="form-control" v-model="cfg.llm_contract_vision.api_key"></div>
-              <div><label class="form-label">Max Tokens</label><input type="number" class="form-control" v-model="cfg.llm_contract_vision.max_tokens"></div>
-            </div>
-            <div class="form-row" style="margin-top:12px">
-              <div><label class="form-label">合同文本分析 API地址</label><input class="form-control" v-model="cfg.llm_contract_text.api_url" placeholder="可使用文本模型"></div>
-              <div><label class="form-label">合同文本分析模型</label><input class="form-control" v-model="cfg.llm_contract_text.model"></div>
-              <div><label class="form-label">合同文本分析 API Key</label><input type="password" class="form-control" v-model="cfg.llm_contract_text.api_key"></div>
-              <div><label class="form-label">Max Tokens</label><input type="number" class="form-control" v-model="cfg.llm_contract_text.max_tokens"></div>
+          <div class="kb-group">
+            <span class="kb-label"><i class="bi bi-diagram-3"></i> 范围</span>
+            <div class="kb-seg">
+              <button :class="{active:statScope==='all' && !drillCode}" @click="setStatScope('all')">全部网点</button>
+              <button :class="{active:statScope==='branch' && !drillCode}" @click="setStatScope('branch')">仅分校</button>
+              <button :class="{active:statScope==='store' && !drillCode}" @click="setStatScope('store')">仅分店</button>
             </div>
+            <select class="form-control" style="width:auto;max-width:220px;padding:5px 8px" v-model="drillCode" @change="onDrillSelect()">
+              <option value="">指定网点下钻…</option>
+              <optgroup label="分校">
+                <option v-for="o in schoolOptions.filter(u=>u.type==='分校')" :key="'b-'+o.code" :value="o.code">[[ o.name ]]（[[ o.code ]]）</option>
+              </optgroup>
+              <optgroup label="分店">
+                <option v-for="o in schoolOptions.filter(u=>u.type!=='分校')" :key="'s-'+o.code" :value="o.code">[[ o.name ]]（[[ o.code ]]）</option>
+              </optgroup>
+            </select>
+            <span v-if="drill" class="drill-chip" @click="closeDrill()" title="点击清除下钻">[[ drill.name ]] <i class="bi bi-x-lg" style="font-size:10px"></i></span>
           </div>
+          <div style="flex:1"></div>
+          <span class="hint">[[ chartDateStart ? ('统计区间：'+chartDateStart+' ~ '+chartDateEnd) : '统计区间：全部时间' ]]</span>
+          <button class="btn btn-secondary btn-sm" @click="vehicleModalOpen=true; loadVehicleCounts()"><i class="bi bi-truck"></i> 车辆数维护</button>
+          <button class="btn btn-secondary btn-sm" @click="exportTickets()"><i class="bi bi-download"></i> 导出Excel</button>
         </div>
 
-        <div class="card">
-          <div class="card-header"><i class="bi bi-chat-dots"></i> 飞书配置</div>
-          <div class="card-body">
-            <div class="form-row">
-              <div><label class="form-label">App ID</label><input class="form-control" v-model="cfg.feishu.app_id"></div>
-              <div><label class="form-label">App Secret</label><input type="password" class="form-control" v-model="cfg.feishu.app_secret"></div>
-              <div><label class="form-label">App Token</label><input class="form-control" v-model="cfg.feishu.bitable_app_token"></div>
-              <div><label class="form-label">Table ID</label><input class="form-control" v-model="cfg.feishu.bitable_table_id"></div>
-            </div>
-          </div>
-        </div>
-
-        <div class="d-flex justify-between align-center">
-          <button class="btn btn-secondary" @click="loadCfg"><i class="bi bi-arrow-clockwise"></i> 重置</button>
-          <div class="d-flex align-center gap-3">
-            <span v-if="cfgMsg" :class="cfgOk ? 'text-success' : 'text-danger'">[[ cfgMsg ]]</span>
-            <button class="btn btn-primary" @click="saveCfg" :disabled="cfgSaving">
-              <span v-if="cfgSaving"><i class="bi bi-arrow-repeat spin"></i></span>
-              <i v-else class="bi bi-check-lg"></i>
-              [[ cfgSaving ? '保存中...' : '保存配置' ]]
-            </button>
-          </div>
+        <!-- KPI 卡片 -->
+        <div class="kpi-row">
+          <div class="kpi"><div class="num">[[ stats.total || 0 ]]</div><div class="lbl">累计投诉</div></div>
+          <div class="kpi"><div class="num">[[ stats.effective_total || 0 ]]<span class="mom-tag" :class="(stats.monthly_compare&&stats.monthly_compare.mom_change!=null)?(stats.monthly_compare.mom_change>=0?'mom-up':'mom-down'):''" v-if="stats.monthly_compare && stats.monthly_compare.mom_change!=null">环比 [[ stats.monthly_compare.mom_change>=0?'+':'' ]][[ stats.monthly_compare.mom_change ]]%</span></div><div class="lbl">有效投诉（不含撤诉）</div></div>
+          <div class="kpi"><div class="num" style="color:var(--color-primary)">[[ (stats.pending||0)+(stats.processing||0) ]]<span class="overdue-pill" v-if="(durationStats.overdue_count||0)>0"><i class="bi bi-exclamation-triangle"></i>超48h [[ durationStats.overdue_count ]]</span></div><div class="lbl">在途（待处理+处理中）</div></div>
+          <div class="kpi"><div class="num" style="color:var(--color-success)">[[ stats.completed || 0 ]]</div><div class="lbl">已完结归档</div></div>
+          <div class="kpi"><div class="num" style="color:var(--color-text-secondary)">[[ stats.withdrawn_total || 0 ]]</div><div class="lbl">已撤诉</div></div>
+          <div class="kpi"><div class="num">[[ ((durationStats.avg_processing_hours||0)/24).toFixed(1) ]]<span class="sub-num">天</span></div><div class="lbl">平均处理时长</div></div>
+          <div class="kpi"><div class="num">[[ boardComplaintRate==null?'—':boardComplaintRate ]]<span class="sub-num">件/百车</span></div><div class="lbl">投诉率（÷车辆数）</div></div>
         </div>
-      </div>
 
-      <!-- 工单详情弹窗 -->
-      <div v-if="detailTicket" class="modal-overlay" @click.self="detailTicket=null">
-        <div class="modal-content modal-lg">
-          <div class="modal-header">
-            <h5><i class="bi bi-info-circle"></i> 工单详情</h5>
-            <button class="btn-close" @click="detailTicket=null">&times;</button>
-          </div>
-          <div class="modal-body" style="max-height:70vh;overflow-y:auto">
-            <div v-if="detailLoading" class="text-center py-4"><i class="bi bi-arrow-repeat spin"></i> 加载中...</div>
-            <div v-else>
-              <!-- 基本信息 -->
-              <div class="card mb-3">
-                <div class="card-body" style="padding:12px">
-                  <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(160px,1fr));gap:8px;font-size:13px">
-                    <div><span class="info-label">学员姓名</span><div class="info-value">[[ detailTicket.student_name ]]</div></div>
-                    <div><span class="info-label">身份证号</span><div class="info-value font-mono">[[ detailTicket.id_card ]]</div></div>
-                    <div><span class="info-label">所属驾校</span><div class="info-value">[[ detailTicket.school_short ]]</div></div>
-                    <div><span class="info-label">投诉日期</span><div class="info-value">[[ detailTicket.complaint_date ]]</div></div>
-                    <div><span class="info-label">投诉来源</span><div class="info-value">[[ detailTicket.source_channel ]]</div></div>
-                    <div><span class="info-label">处理状态</span><div class="info-value">
-                      <span :style="{color: detailTicket.handle_status === '已完结' ? 'var(--green)' : detailTicket.handle_status === '处理中' ? 'var(--blue)' : '#d97706'}">[[ detailTicket.handle_status ]]</span>
-                    </div></div>
-                  </div>
+        <!-- 图表区 -->
+        <div class="kb-grid">
+          <div class="card"><div class="card-header"><i class="bi bi-graph-up"></i> 月度趋势</div>
+            <div class="card-body"><div ref="trendChartRef" style="height:280px"></div></div></div>
+          <div class="card"><div class="card-header"><i class="bi bi-bar-chart"></i> 单位排行
+            <span style="flex:1"></span>
+            <span class="rank-tabs">
+              <button :class="{active:rankMode==='count'}" @click="rankMode='count';updateCharts()">按数量</button>
+              <button :class="{active:rankMode==='rate'}" @click="rankMode='rate';updateCharts()">按每百车率</button>
+            </span></div>
+            <div class="card-body"><div ref="schoolChartRef" style="height:280px"></div></div></div>
+          <div class="card"><div class="card-header"><i class="bi bi-pie-chart"></i> 投诉类型分布</div>
+            <div class="card-body"><div ref="typeChartRef" style="height:280px"></div></div></div>
+          <div class="card"><div class="card-header"><i class="bi bi-diagram-3"></i> 处理状态</div>
+            <div class="card-body"><div ref="statusChartRef" style="height:280px"></div></div></div>
+          <div class="card"><div class="card-header"><i class="bi bi-signpost-split"></i> 来源渠道 TOP5</div>
+            <div class="card-body">
+              <div class="mini-list">
+                <div class="mini-item" v-for="c in topChannels" :key="c.source">
+                  <div class="mini-row"><b>[[ c.source || '未知' ]]</b><span class="hint">[[ c.count ]] 件 · [[ c.pct ]]%</span></div>
+                  <div class="mini-track"><div class="mini-fill" :style="{width:c.pct+'%'}"></div></div>
                 </div>
               </div>
-
-              <!-- 退费信息 -->
-              <div class="card mb-3">
-                <div class="card-body" style="padding:12px">
-	                  <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:8px;font-size:13px">
-	                    <div><span class="info-label">合同总金额</span><div class="info-value">[[ detailTicket.total_fee || 0 ]]元</div></div>
-	                    <div><span class="info-label">实际已交</span><div class="info-value">[[ detailTicket.actual_paid || 0 ]]元</div></div>
-	                    <div><span class="info-label">总扣费</span><div class="info-value" style="color:var(--danger)">[[ detailTicket.deduction_fee || 0 ]]元</div></div>
-	                    <div><span class="info-label">应退金额</span><div class="info-value" style="color:var(--success)">[[ detailTicket.refund_fee || 0 ]]元</div></div>
-	                    <div><span class="info-label">合同编号</span><div class="info-value">[[ detailTicket.contract_code || '-' ]]</div></div>
-	                    <div><span class="info-label">最终结果</span><div class="info-value">[[ detailTicket.final_outcome || '-' ]]</div></div>
-	                    <div><span class="info-label">配合度</span><div class="info-value">[[ detailTicket.branch_cooperation || '-' ]]</div></div>
-	                  </div>
-                </div>
-              </div>
-
-              <!-- 扣费明细 -->
-              <div v-if="detailDeductions && detailDeductions.length" class="card mb-3">
-                <div class="card-header" style="font-size:13px"><i class="bi bi-list-check"></i> 扣费明细</div>
-                <div class="card-body" style="padding:8px">
-                  <table class="fee-table" style="font-size:12px">
-                    <thead><tr>
-                      <th class="th-center">项目</th>
-                      <th class="th-center">金额</th>
-                      <th class="th-center">依据</th>
-                    </tr></thead>
-                    <tbody>
-                      <tr v-for="(ded, di) in detailDeductions" :key="di">
-                        <td>[[ ded.item ]]</td>
-                        <td style="text-align:right">[[ ded.amount ]]元</td>
-                        <td style="color:var(--text-secondary);font-size:11px">[[ ded.reason ]]</td>
-                      </tr>
-                    </tbody>
-                  </table>
-                </div>
-	              </div>
-
-	              <!-- 沟通记录 -->
-	              <div v-if="detailCommunicationRecords && detailCommunicationRecords.length" class="card mb-3">
-	                <div class="card-header" style="font-size:13px"><i class="bi bi-chat-dots"></i> 沟通记录（[[ detailCommunicationRecords.length ]]次）</div>
-	                <div class="card-body" style="padding:8px">
-	                  <ul class="visit-records-list">
-	                    <li v-for="(rec, ri) in detailCommunicationRecords" :key="ri" class="visit-record-item">
-	                      <span class="visit-record-date">[[ rec.contact_time || rec.created_at?.substring(0,16) ]]</span>
-	                      <span class="visit-record-status">[[ rec.contact_method || '-' ]]</span>
-	                      <span class="visit-record-remark">[[ rec.summary ]]</span>
-	                      <span class="visit-record-status" style="color:var(--primary)">[[ rec.student_intention || '-' ]]</span>
-	                    </li>
-	                  </ul>
-	                </div>
-	              </div>
-
-              <!-- 文档 -->
-              <div v-if="detailDocuments && detailDocuments.length" class="card">
-                <div class="card-header" style="font-size:13px"><i class="bi bi-file-earmark-word"></i> 文档</div>
-                <div class="card-body" style="padding:12px">
-                  <div v-for="(doc, di) in detailDocuments" :key="di" style="padding:6px 0;border-bottom:1px solid var(--border)">
-                    <a :href="'/api/reply/download?path=' + encodeURIComponent(doc.filepath)" target="_blank" style="font-size:13px">
-                      <i class="bi bi-download"></i> [[ doc.filename ]]
-                    </a>
+            </div></div>
+          <div class="card"><div class="card-header"><i class="bi bi-arrow-left-right"></i> 环比对比
+            <span class="hint" style="margin-left:auto;font-weight:400;font-size:11px">[[ compare.windowText ]]</span></div>
+            <div class="card-body">
+              <template v-if="compare.rows.length">
+                <div class="cmp-row" v-for="r in compare.rows" :key="r.label">
+                  <div class="cmp-label">[[ r.label ]]<span class="hint" style="display:block">[[ r.unit ]]</span></div>
+                  <div class="cmp-bars">
+                    <div class="cmp-line"><span class="cmp-tag cur">本期</span><div class="mini-track"><div class="mini-fill" :style="{width:r.wCur+'%'}"></div></div><b>[[ r.cur ]]</b></div>
+                    <div class="cmp-line"><span class="cmp-tag prev">上期</span><div class="mini-track"><div class="mini-fill prev-fill" :style="{width:r.wPrev+'%'}"></div></div><b>[[ r.prev ]]</b></div>
                   </div>
+                  <div class="cmp-delta" :class="r.cls"><i :class="'bi '+r.icon"></i> [[ r.deltaText ]]</div>
                 </div>
-              </div>
-            </div>
-          </div>
-          <div class="modal-footer">
-            <button class="btn btn-secondary" @click="detailTicket=null">关闭</button>
-          </div>
+              </template>
+              <div v-else class="cmp-empty"><i class="bi bi-calendar-x"></i>选择具体时间段后展示环比<br><span class="hint">「全部时间」无上一周期可比</span></div>
+            </div></div>
         </div>
-      </div>
 
-      <!-- 车辆数维护弹层 -->
-      <div v-if="vehicleModalOpen" class="modal-overlay" @click.self="vehicleModalOpen=false">
-        <div class="modal-content modal-lg">
-          <div class="modal-header">
-            <h5 style="margin:0;font-size:14px"><i class="bi bi-truck"></i> 维护网点车辆数 <span style="font-size:12px;color:var(--gray-500);font-weight:400">（投诉率 = 有效投诉 ÷ 车辆数）</span></h5>
-            <button class="btn btn-sm btn-secondary" @click="vehicleModalOpen=false">&times;</button>
+        <!-- 投诉信息明细 -->
+        <div class="card" style="margin-top:16px">
+          <div class="card-header"><i class="bi bi-list-ul"></i> 投诉信息明细</div>
+          <div class="kb-toolbar">
+            <input class="form-control kb-search" v-model="hSearch" @input="hPage=1;debSearch()" placeholder="搜索学员姓名 / 代号 / 身份证号…">
+            <select class="form-control" style="width:auto" v-model="statusFilter" @change="hPage=1;loadHist()">
+              <option value="">全部状态</option>
+              <option value="待处理">待处理</option>
+              <option value="处理中">处理中</option>
+              <option value="已完结">已完结</option>
+            </select>
+            <label class="hint" style="display:flex;align-items:center;gap:4px;cursor:pointer;margin:0">
+              <input type="checkbox" v-model="repeatOnly" @change="hPage=1;loadHist()"> 只看重复投诉
+            </label>
+            <span style="flex:1"></span>
+            <button class="btn btn-secondary btn-sm" @click="masked=!masked"><i class="bi" :class="masked?'bi-eye':'bi-eye-slash'"></i> [[ masked ? '显示完整身份证' : '隐藏身份证' ]]</button>
           </div>
-          <div class="modal-body" style="max-height:65vh;overflow-y:auto">
-            <div v-if="vehicleLoading" style="font-size:12px;color:var(--gray-500)">加载中...</div>
-            <table v-else style="width:100%;font-size:12px;border-collapse:collapse">
-              <tr style="color:var(--gray-500)">
-                <th style="text-align:left;padding:4px 6px;border-bottom:1px solid var(--border)">类型</th>
-                <th style="text-align:left;padding:4px 6px;border-bottom:1px solid var(--border)">网点</th>
-                <th style="text-align:left;padding:4px 6px;border-bottom:1px solid var(--border)">代号</th>
-                <th style="text-align:left;padding:4px 6px;border-bottom:1px solid var(--border)">车辆数</th>
-                <th style="text-align:left;padding:4px 6px;border-bottom:1px solid var(--border)">操作</th>
-              </tr>
-              <tr v-for="(item, i) in vehicleItems" :key="item.unit_code + '_' + i">
-                <td style="padding:4px 6px;border-bottom:1px solid var(--border)">
-                  <select class="form-control" style="max-width:90px;font-size:12px" v-model="item.unit_type">
-                    <option value="分校">分校</option>
-                    <option value="分店">分店</option>
-                  </select>
-                </td>
-                <td style="padding:4px 6px;border-bottom:1px solid var(--border)">
-                  <input type="text" class="form-control" style="font-size:12px" v-model="item.unit_name" placeholder="网点名称">
-                </td>
-                <td style="padding:4px 6px;border-bottom:1px solid var(--border)">
-                  <input type="text" class="form-control" style="max-width:80px;font-size:12px" v-model="item.unit_code" placeholder="代号">
-                </td>
-                <td style="padding:4px 6px;border-bottom:1px solid var(--border)">
-                  <input type="number" min="0" class="form-control" style="max-width:90px;font-size:12px" v-model.number="item.vehicle_count">
-                </td>
-                <td style="padding:4px 6px;border-bottom:1px solid var(--border)">
-                  <button class="btn btn-sm btn-outline-danger" style="font-size:11px" @click="removeVehicleRow(i)"><i class="bi bi-trash"></i></button>
-                </td>
-              </tr>
+          <div class="card-body" style="padding:0;overflow-x:auto">
+            <table class="table">
+              <thead><tr><th style="width:52px">序号</th><th>投诉日期</th><th>学员姓名</th><th>身份证号</th><th>经营单位</th><th>代号</th><th>投诉类型</th><th>来源渠道</th><th>状态</th><th>配合度</th></tr></thead>
+              <tbody>
+                <tr v-for="(t,i) in hList" :key="t.id" style="cursor:pointer" @click="openCase(t.id)">
+                  <td class="kb-seq">[[ (hPage-1)*hLimit + i + 1 ]]</td>
+                  <td>[[ t.complaint_date || '-' ]]</td>
+                  <td><b>[[ t.student_name || '-' ]]</b><span v-if="t._repeat" class="repeat-flag" title="该身份证存在多条投诉记录"><i class="bi bi-arrow-repeat"></i></span></td>
+                  <td class="kb-idcard">[[ maskId(t.id_card) ]]</td>
+                  <td>[[ t.organization_unit_name || t.school_short || '-' ]]</td>
+                  <td class="kb-muted">[[ t.organization_unit_code || t.school_short || '-' ]]</td>
+                  <td>[[ {A:'退费纠纷',B:'教学服务',C:'考试安排',D:'合同争议',E:'其他'}[t.complaint_type] || t.complaint_type || '-' ]]</td>
+                  <td>[[ t.source_channel || '-' ]]</td>
+                  <td><span class="badge" :class="t.archive_status==='已归档'?'badge-completed':(t.withdraw_status==='已撤诉'?'badge-muted':(t.handle_status==='处理中'?'badge-processing':'badge-pending'))">[[ t.archive_status==='已归档'?'已归档':(t.withdraw_status==='已撤诉'?'已撤诉':(t.handle_status||'待处理')) ]]</span></td>
+                  <td>[[ t.branch_cooperation || '—' ]]</td>
+                </tr>
+                <tr v-if="!hList.length"><td colspan="10" style="text-align:center;padding:40px;color:#8F959E"><i class="bi bi-inbox"></i> 当前筛选条件下暂无数据</td></tr>
+              </tbody>
             </table>
-            <div v-if="!vehicleLoading" class="mt-2">
-              <button class="btn btn-sm btn-outline-primary" style="font-size:11px" @click="addVehicleRow"><i class="bi bi-plus-lg"></i> 添加网点</button>
-            </div>
           </div>
-          <div class="modal-footer d-flex justify-between">
-            <span style="font-size:12px;color:var(--gray-500)">共 [[ vehicleItems.length ]] 个网点 · 合计 [[ vehicleItems.reduce((s,i)=>s+(Number(i.vehicle_count)||0),0) ]] 辆</span>
-            <div class="d-flex gap-2">
-              <button class="btn btn-secondary" @click="vehicleModalOpen=false">取消</button>
-              <button class="btn btn-primary" :disabled="vehicleSaving" @click="saveVehicleCounts().then(ok=>{if(ok) vehicleModalOpen=false})">
-                [[ vehicleSaving ? '保存中...' : '保存' ]]
-              </button>
-            </div>
+          <div class="kb-pager">
+            <span class="hint">共 [[ hTotal ]] 条记录　第 [[ hPage ]]/[[ hTotalPages ]] 页</span>
+            <span style="flex:1"></span>
+            <span class="hint">每页</span>
+            <select class="form-control" style="width:auto;padding:4px 8px" v-model.number="hLimit" @change="hPage=1;loadHist()">
+              <option v-for="n in [10,20,50,100]" :key="n" :value="n">[[ n ]]</option>
+            </select>
+            <button class="pg-btn" :disabled="hPage<=1" @click="hPage--;loadHist()"><i class="bi bi-chevron-left"></i></button>
+            <button v-for="p in hPageButtons" :key="p.n" class="pg-btn" :class="{on:p.cur}" @click="hPage=p.n;loadHist()">[[ p.n ]]</button>
+            <button class="pg-btn" :disabled="hPage>=hTotalPages" @click="hPage++;loadHist()"><i class="bi bi-chevron-right"></i></button>
           </div>
         </div>
-      </div>
 
-      <!-- 合同预览抽屉（右侧滑出，左侧 AI 扣费明细保持可见，便于逐条核对） -->
-      <div v-if="previewVisible" class="drawer-overlay open" @click.self="closePreview">
-        <div class="drawer open">
-          <div class="drawer-header">
-            <div class="drawer-title">
-              <i class="bi bi-file-earmark-text" style="color:var(--primary)"></i>
-              电子合同 · [[ previewFilename ]]
+        <!-- ═══ 网点车辆数维护弹窗 ═══ -->
+        <div class="modal-overlay" v-if="vehicleModalOpen" @click.self="vehicleModalOpen=false">
+          <div class="modal-content">
+            <div class="m-head"><i class="bi bi-truck"></i> 网点车辆数维护 <span class="hint" style="font-weight:400">车辆数为「每百车投诉率」的分母，请与各网点实际在册车辆保持一致</span></div>
+            <div class="m-body">
+              <table class="table veh-table">
+                <thead><tr><th style="width:90px">类型</th><th style="width:130px">代号</th><th>网点名称</th><th style="width:110px">车辆数</th><th style="width:64px">操作</th></tr></thead>
+                <tbody>
+                  <tr v-for="(v,i) in vehicleItems" :key="i">
+                    <td><select class="form-control" style="padding:5px 8px" v-model="v.unit_type"><option>分校</option><option>分店</option></select></td>
+                    <td><input class="form-control" style="padding:5px 8px" v-model="v.unit_code" placeholder="如 DC-04"></td>
+                    <td><input class="form-control" style="padding:5px 8px" v-model="v.unit_name" placeholder="网点名称"></td>
+                    <td><input class="form-control" style="padding:5px 8px" type="number" min="0" v-model.number="v.vehicle_count"></td>
+                    <td><button class="btn btn-secondary btn-sm" style="color:var(--color-danger)" @click="removeVehicleRow(i)" title="删除该网点"><i class="bi bi-trash3"></i></button></td>
+                  </tr>
+                  <tr v-if="!vehicleItems.length"><td colspan="5" style="text-align:center;padding:24px;color:#8F959E">暂无网点，点击下方「新增网点」</td></tr>
+                </tbody>
+              </table>
+              <button class="btn btn-secondary btn-sm" @click="addVehicleRow()"><i class="bi bi-plus-lg"></i> 新增网点</button>
             </div>
-            <button class="drawer-close" @click="closePreview" aria-label="关闭">&times;</button>
-          </div>
-          <div class="drawer-body">
-            <iframe v-if="previewIsPdf" :src="previewUrl" class="drawer-frame" frameborder="0"></iframe>
-            <div v-else class="drawer-img-wrap">
-              <img :src="previewUrl" class="drawer-img">
+            <div class="m-foot">
+              <span class="hint">合计 [[ vehicleItems.reduce((a,v)=>a+(Number(v.vehicle_count)||0),0) ]] 台车 · [[ vehicleItems.length ]] 个网点　保存后「每百车投诉率」按新分母重算</span>
+              <span style="flex:1"></span>
+              <button class="btn btn-secondary" @click="vehicleModalOpen=false">取消</button>
+              <button class="btn btn-primary" @click="saveVehicleCounts()"><i class="bi bi-check2"></i> 保存</button>
             </div>
           </div>
-          <div class="drawer-footer">
-            <a :href="'/api/contract/download-file?path=' + encodeURIComponent(previewUrl.replace('/api/contract/preview?path=',''))" class="btn btn-sm btn-secondary" target="_blank">
-              <i class="bi bi-download"></i> 下载原文件
-            </a>
-            <button class="btn btn-sm btn-primary" @click="closePreview">
-              <i class="bi bi-check-lg"></i> 核对完成，关闭
-            </button>
+        </div>
+      </section>
+    </main>
+    </div><!-- /app-shell -->
+
+    <!-- ═══ 合同预览弹窗 ═══ -->
+    <div class="modal-overlay" v-if="contractModalOpen" @click.self="closeContract()">
+      <div class="modal-content">
+        <div class="m-head"><i class="bi bi-eye"></i> 合同原文对照 <span class="hint" style="font-weight:400">点击右侧扣费项 → 左侧自动定位并高亮对应条款</span></div>
+        <div class="m-body"><div class="contract-wrap">
+          <div class="contract-doc">
+            <h4>东莞市机动车驾驶员培训服务合同</h4>
+            <p>甲方（学员）：[[ selectedTicket.student_name || qr.name || '—' ]]　身份证号：[[ qr.id_card || selectedTicket.id_card || '—' ]]<br>
+               乙方：东莞市快捷汽车驾驶员培训有限公司（[[ selectedTicket.school_name || qr.school_name || '—' ]]）<br>
+               合同编码：HT-[[ selectedTicket.complaint_date ? selectedTicket.complaint_date.replace(/-/g,'') : '' ]]-047　签订日期：[[ qr.registration_date || '-' ]]</p>
+            <p v-for="c in contractClauses" :id="'cl-'+c[0]" :key="c[0]"><b>[[ c[1] ]]</b><br>[[ c[2] ]]</p>
+            <p style="text-indent:0;color:#94A3B8;font-size:12px">—— 以上为电子签章原件节选 · 来源：东莞驾培平台 ——</p>
+          </div>
+          <div class="contract-side">
+            <div style="font-weight:700;font-size:13px">扣费项对照</div>
+            <template v-if="ar && ar.deductions">
+              <div v-for="(r,i) in ar.deductions.filter(x=>Number(x.amount)>0)" :key="i" class="fi"
+                   @mouseenter="hlClause(((r.reason||'').match(/第([一二三四五六七八九十]+)条/)||[])[1])"
+                   @click="hlClause(((r.reason||'').match(/第([一二三四五六七八九十]+)条/)||[])[1])">
+                <b>[[ i+1 ]]. [[ r.item ]] — ¥[[ money(r.amount) ]]</b>
+                <span class="b">[[ r.reason ]]</span>
+              </div>
+            </template>
+            <div v-else class="hint">完成费用分析后展示扣费项对照</div>
           </div>
+        </div></div>
+        <div class="m-foot">
+          <a v-if="cPath" class="btn btn-secondary" :href="'/api/contract/download-file?path=' + encodeURIComponent(cPath)" target="_blank"><i class="bi bi-download"></i> 下载合同原件</a>
+          <button class="btn btn-primary" @click="closeContract()">我已核对，关闭</button>
         </div>
       </div>
+    </div>
 
-    <!-- ═══ Toast 通知 ═══ -->
-    <div class="toast-container" v-if="toasts.length">
-      <div v-for="(t, i) in toasts" :key="i" class="toast" :class="'toast-' + t.type">
-        <div class="toast-title">[[ t.title ]]</div>
-        <div>[[ t.message ]]</div>
+    <!-- ═══ 撤诉弹窗 ═══ -->
+    <div class="modal-overlay" v-if="withdrawModalOpen" @click.self="withdrawModalOpen=false">
+      <div class="modal-content" style="width:min(480px,92vw)">
+        <div class="m-head"><i class="bi bi-flag"></i> 标记撤诉</div>
+        <div class="m-body">
+          <p style="margin-bottom:10px">确认将 <b>[[ selectedTicket.student_name || qr.name || '—' ]]</b> 的投诉标记为「已撤诉」？</p>
+          <ul class="hint" style="padding-left:18px;line-height:1.9">
+            <li>统计中自动移出有效投诉，单独列入撤诉列</li>
+            <li>案件记录与档案资料永久保留</li>
+            <li>已归档案件将自动向原档案夹补充撤诉说明</li>
+          </ul>
+          <input class="form-control" style="margin-top:10px" v-model="withdrawReason" placeholder="撤诉原因（选填）">
+        </div>
+        <div class="m-foot"><button class="btn btn-secondary" @click="withdrawModalOpen=false">取消</button>
+          <button class="btn btn-danger" @click="confirmWithdraw()"><i class="bi bi-flag-fill"></i> 确认撤诉</button></div>
       </div>
     </div>
-  </div>
 
-  </div>
+    <!-- ═══ 批量转办弹窗 ═══ -->
+    <div class="modal-overlay" v-if="transferModalOpen" @click.self="transferModalOpen=false">
+      <div class="modal-content" style="width:min(440px,92vw)">
+        <div class="m-head"><i class="bi bi-arrow-left-right"></i> 批量转办</div>
+        <div class="m-body">
+          <p style="margin-bottom:10px">将选中的 <b>[[ clSelectedIds.length ]]</b> 条案件转交给：</p>
+          <input class="form-control" v-model.trim="transferTarget" placeholder="输入接收人姓名，如：张老师" @keyup.enter="confirmBatchTransfer()">
+        </div>
+        <div class="m-foot">
+          <button class="btn btn-secondary" @click="transferModalOpen=false">取消</button>
+          <button class="btn btn-primary" :disabled="transferSaving || !transferTarget" @click="confirmBatchTransfer()">
+            [[ transferSaving ? '转办中…' : '确认转办' ]]</button>
+        </div>
+      </div>
+    </div>
+
+    <!-- ═══ Toast ═══ -->
+    <div class="toast-container" v-if="toasts.length">
+      <div v-for="(t,i) in toasts" :key="i" class="toast" :class="'toast-'+t.type">
+        <div class="toast-title">[[ t.title ]]</div><div>[[ t.message ]]</div>
+      </div>
+    </div>
   </div>
 
   <script src="https://cdn.jsdelivr.net/npm/vue@3.4/dist/vue.global.prod.js"></script>
   <script type="importmap">
   {
     "imports": {
-      "api": "/static/js/utils/api.js?v=9",
-      "helpers": "/static/js/utils/helpers.js?v=9",
-      "useToast": "/static/js/composables/useToast.js?v=9",
-      "useComplaint": "/static/js/composables/useComplaint.js?v=9",
-      "useWorkflow": "/static/js/composables/useWorkflow.js?v=10",
-      "useSettings": "/static/js/composables/useSettings.js?v=9",
-      "useHistory": "/static/js/composables/useHistory.js?v=9"
+      "api": "/static/js/utils/api.js?v=14",
+      "helpers": "/static/js/utils/helpers.js?v=14",
+      "useToast": "/static/js/composables/useToast.js?v=14",
+      "useComplaint": "/static/js/composables/useComplaint.js?v=14",
+      "useWorkflow": "/static/js/composables/useWorkflow.js?v=14",
+      "useSettings": "/static/js/composables/useSettings.js?v=14",
+      "useHistory": "/static/js/composables/useHistory.js?v=14",
+      "useWorkbench": "/static/js/composables/useWorkbench.js?v=15"
     }
   }
   </script>
-  <script type="module" src="/static/js/app.js?v=10"></script>
+  <script type="module" src="/static/js/app.js?v=14"></script>
 </body>
 </html>
