# 办理时间线显示规则统一 + 扣费明细去红字

## 背景
右栏"办理时间线"卡片（`templates/index.html:1494-1508`）6 个环节没有完成/进行中/未开始的视觉区分，靠用户脑补进度。"扣费明细 · 待确认"用 `.gate-no` 染红字，红色在此上下文里语义过重（不是错误/危险）。

## 范围
- 文件：`templates/index.html`（模板 + 同一文件内的 `<style>`）
- 不动：后端逻辑、数据结构、其它卡片
- 不动：第二处时间线（`tl-exam` 学习时间轴 tab，1319-1325 行）
- 不动：归档闸门处"扣费明细表已确认"的颜色（行 1461，仍用 `gate-ok`/`gate-no`）

## 设计

### 1. 扣费明细（行 1502）
- 待确认 → 黑字（去掉 `gate-no`），保留文字"待确认"
- 已确认 → 保持绿字（`gate-ok`）
- 在"扣费明细"和状态词之间加 ` · ` 分隔（与卡片标题风格一致）

### 2. 办理时间线（行 1498-1506）三态
- **完成 done**：左侧绿圆点 + `bi-check-circle-fill` 绿勾，文字主色
- **进行中 doing**：左侧蓝圆点 + 蓝色光晕（`box-shadow` 0 0 0 3px rgba(37,99,235,.18)）+ `bi-circle-fill`，文字主色
- **未开始 todo**：左侧灰圆点 + `bi-circle` 灰空心，文字次要色

### 3. "进行中"判定（上一环节完 + 本环节未完）
| 环节 | 进行中条件 | 完成条件 |
|------|-----------|---------|
| 投诉登记 | 永不进行中 | 投诉日期已填 |
| 三系统查询 | 投诉日期已填 且 `!qr.name` | `qr && qr.name` |
| 扣费明细 | 三系统已查完 且 `fee_plan_status !== 'confirmed'` | `fee_plan_status === 'confirmed'` |
| 投诉登记表 | 扣费已确认 且 `!registration_form_path` | `registration_form_path` 有值 |
| 投诉回复函 | 登记表已生成 且 `!(replySaved \|\| reply_path)` | `replySaved \|\| reply_path` |
| 归档 | 回复函已生成 且 `archive_status !== '已归档'` | `archive_status === '已归档'` |

未达"进行中条件"也未达"完成条件"= 未开始 todo。

## 回测
- 启动 dev 服务，手动加载一个工单观察三态
- 跑 `pytest tests/ -x` 确保没破其它东西
- 重点检查：行 1332 "扣费明细"卡片自身的"编辑中"角标不受影响
- 重点检查：行 1461 归档闸门处 `.gate-ok/.gate-no` 不受影响
