# 驾校投诉处理系统 - 开发规划

## 需求确认

| 项目 | 决策 |
|------|------|
| 架构 | Flask + Vue 3 单页Web应用 |
| 自动化 | 高度自动化，一键查询后自动串联 |
| 退费计算 | AI大模型分析合同 |
| AI/回复函/飞书 | 可选按钮，用户点击才执行 |
| 无电子合同 | 上传 + 手动填写均支持 |
| 报名费 | AI从合同提取 / 用户手动填写 |
| 投诉类型 | 5类 (A退费纠纷/B教学服务/C考试安排/D合同争议/E其他) |
| 保存路径 | `/Users/master/Desktop/投诉处理系统/` |

---

## 系统架构

### 项目结构
```
/Users/master/Desktop/投诉处理系统/
├── app.py                      # Flask 主程序 (端口5003)
├── config.py                   # 配置管理
├── database.py                 # SQLite 本地数据库
├── requirements.txt            # Python 依赖
├── start.command               # macOS 双击启动脚本
├── data/
│   ├── config.json             # 用户配置 (账号密码/API/飞书)
│   └── complaints.db           # SQLite 数据库
├── templates/
│   └── index.html              # Vue 3 单页前端
├── static/
│   ├── css/
│   │   └── style.css           # 现代化样式
│   └── js/
│       └── app.js              # Vue 前端逻辑
├── crawlers/                   # 三系统爬虫 (复用现有代码)
│   ├── __init__.py
│   ├── internal.py             # 内部系统
│   ├── third.py                # 第三系统
│   └── driving.py              # 东莞驾培
├── services/                   # 业务服务层
│   ├── __init__.py
│   ├── query_service.py        # 三系统并行查询
│   ├── contract_service.py     # 合同下载 + AI分析
│   ├── reply_service.py        # 回复函生成 (Word)
│   ├── feishu_service.py       # 飞书多维表格
│   └── file_service.py         # 文件管理 (命名/路径)
├── 回复函/                      # 生成的回复函文件
├── 合同文件/                    # 下载/上传的合同文件
└── uploads/                     # 临时上传目录
```

### 技术栈
- **后端**: Python 3 + Flask
- **前端**: Vue 3 (CDN) + Bootstrap 5 + Bootstrap Icons
- **AI**: 阿里云百炼 (OpenAI兼容API)
- **文档**: python-docx (Word) / pdfplumber (PDF)
- **OCR**: ddddocr + PaddleOCR (验证码)
- **存储**: SQLite (本地) + 飞书多维表格 (云端)

---

## UI 设计 - 流程驱动型

### 核心理念
**不再是6个独立Tab**，而是一个围绕"处理一个投诉案件"的流水线页面。

### 页面结构

**顶部导航栏**: 3个区域
- 📋 **投诉处理** (主工作流 - 默认页)
- ⚙️ **系统设置** (配置页)
- 📊 **处理历史** (已处理记录列表)

### 主工作流页面 (单页滚动)

```
┌──────────────────────────────────────────────────────────┐
│  🛡️ 驾校投诉处理系统              [📊 历史] [⚙️ 设置]   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ── 第一步：投诉登记 ─────────────────────────────────   │
│  │ 身份证号: [__________] 投诉渠道: [交通局 ▼]       │   │
│  │ 投诉时间: [今天]       投诉类型: [A-退费纠纷 ▼]   │   │
│  │                   [🔍 查询三系统]                  │   │
│  └────────────────────────────────────────────────────   │
│                                                          │
│  ── 第二步：学员信息 (查询后自动展示) ────────────────   │
│  │ 基本信息 │ 考试情况 │ 培训时长 │ 收费记录 │ 时间轴│   │
│  └────────────────────────────────────────────────────   │
│                                                          │
│  ── 第三步：处理操作 (3个可展开面板) ─────────────────   │
│  │                                                    │   │
│  │  [📄 AI分析合同 ▸]  可展开面板                    │   │
│  │    合同来源选择 → AI分析 → 扣费明细 → 可编辑      │   │
│  │                                                    │   │
│  │  [📝 生成回复函 ▸]  可展开面板                    │   │
│  │    预览 → 可微调字段 → 生成Word → 下载             │   │
│  │                                                    │   │
│  │  [☁️ 提交飞书 ▸]   可展开面板                     │   │
│  │    自动填充表单 → 可微调 → 一键提交                │   │
│  │                                                    │   │
│  └────────────────────────────────────────────────────   │
└──────────────────────────────────────────────────────────┘
```

### 交互流程

```
用户输入身份证号 + 选择投诉渠道/类型
        ↓
   点击 [查询三系统]
        ↓
  ┌─────┼─────┐
  ↓     ↓     ↓
内部  第三  东莞驾培  ← 并行查询，实时显示进度
  ↓     ↓     ↓
  └─────┼─────┘
        ↓
 第二步自动展开，显示合并后的学员信息
        ↓
 第三步自动展开，3个操作按钮可用
        ↓
 用户按需点击：
   · [AI分析合同] → 展开面板 → 自动下载合同(如有) → AI分析 → 显示扣费
   · [生成回复函] → 展开面板 → 自动填充 → 预览 → 生成Word
   · [提交飞书]   → 展开面板 → 自动填充 → 确认 → 提交
```

### 数据自动流转
- 查询结果 → 自动填充到 AI分析面板 (身份证号、考试阶段、培训时长)
- 查询结果 + AI分析结果 → 自动填充到 回复函面板 (姓名、分校、扣费明细、应退金额)
- 查询结果 + AI分析结果 → 自动填充到 飞书面板 (全部字段)

---

## 开发阶段

### Phase 1: 项目骨架 + 配置
**目标**: 能启动、能访问、能读写配置

- Step 1.1: 创建项目目录结构
  - 输出: 所有目录和 `__init__.py` 创建完成
  - 验证: `ls -R` 确认结构

- Step 1.2: config.py - 配置管理
  - 输出: `data/config.json` 的读写，包含三系统账号、飞书配置、大模型API、文件路径
  - 验证: 导入模块，读写配置不报错

- Step 1.3: database.py - 本地数据库
  - 输出: SQLite CRUD (投诉记录表、操作日志表)
  - 验证: 建表 + 插入 + 查询成功

- Step 1.4: app.py 骨架 + 前端空壳
  - 输出: Flask 启动 → 浏览器可访问空白页
  - 验证: `python app.py` → 访问 localhost:5003

### Phase 2: 三系统爬虫 (复用现有代码)
**目标**: 三系统查询可用

- Step 2.1: crawlers/internal.py
  - 输出: 从现有 `internal_new.py` 迁移，验证码登录 + 学员查询
  - 验证: 用真实身份证号查询成功

- Step 2.2: crawlers/third.py
  - 输出: 从现有 `third_new.py` 迁移
  - 验证: 查询培训时长成功

- Step 2.3: crawlers/driving.py
  - 输出: 从现有 `driving_new.py` + `合同下载工具.py` 迁移
  - 验证: 查询 + 下载合同PDF成功

- Step 2.4: services/query_service.py
  - 输出: 三系统并行查询编排
  - 验证: 一次调用返回三系统合并数据

### Phase 3: 核心服务
**目标**: AI分析、回复函、飞书全部可用

- Step 3.1: services/file_service.py
  - 输出: 合同文件命名 `{日期}+{姓名}+{身份证}+{文件名}`、回复函路径管理
  - 验证: 生成正确的文件路径

- Step 3.2: services/contract_service.py
  - 输出: PDF文本提取 + 图片OCR + AI大模型分析退费
  - 验证: 上传合同 → 返回扣费JSON

- Step 3.3: services/reply_service.py
  - 输出: Word回复函生成，保存到 `回复函/` 目录
  - 验证: 生成的docx格式正确

- Step 3.4: services/feishu_service.py
  - 输出: 飞书Token获取 + 多维表格写入
  - 验证: 提交测试数据到飞书表格

### Phase 4: 后端API路由
**目标**: 所有前端需要的API就绪

- Step 4.1: 查询相关API
  - `POST /api/query` - 三系统查询
  - `POST /api/contract/download` - 下载合同
  - `POST /api/contract/upload` - 上传合同
  - `POST /api/contract/analyze` - AI分析合同

- Step 4.2: 操作相关API
  - `POST /api/reply/generate` - 生成回复函
  - `GET /api/reply/download` - 下载回复函
  - `POST /api/feishu/submit` - 提交飞书

- Step 4.3: 配置与记录API
  - `GET/PUT /api/config` - 配置读写
  - `GET /api/complaints` - 历史记录
  - `GET /api/feishu/test` - 飞书连接测试

### Phase 5: 前端实现
**目标**: 完整可用的UI

- Step 5.1: index.html - 页面结构
  - 导航栏 + 三步工作流布局 + 设置页 + 历史页
  
- Step 5.2: style.css - 现代化样式
  - 紧凑高效的信息密度、可展开面板动画、进度指示器

- Step 5.3: app.js - 主工作流逻辑
  - 第一步：输入 + 查询 (带进度动画)
  - 第二步：结果展示 (自动展开)
  - 第三步：三个可展开操作面板

- Step 5.4: app.js - 设置页 + 历史页
  - 设置页：三系统账号、大模型API、飞书、文件路径
  - 历史页：已处理投诉记录列表

### Phase 6: 测试与打磨
**目标**: 稳定可用

- Step 6.1: 端到端流程测试
  - 从输入身份证号到提交飞书的完整流程

- Step 6.2: 错误处理完善
  - 网络超时、验证码失败、API错误等异常情况

- Step 6.3: start.command 启动脚本
  - macOS双击启动，自动打开浏览器

---

## 关键设计决策

### 1. 数据流自动化
查询结果存储在 Vue 的响应式 `state` 中，所有下游面板自动读取：
```javascript
state = {
  queryResult: {},      // 三系统查询结果
  analysisResult: {},   // AI分析结果
  // → 回复函面板和飞书面板自动从这两个对象读取
}
```

### 2. 合同文件管理
```
合同文件/
└── 20260424+张三+110101199003070011+合同.pdf
```
- 自动下载的合同按此规则命名保存
- 用户上传的合同也按此规则重命名保存

### 3. 回复函文件管理
```
回复函/
└── 20260424张三110101199003070011投诉回复函南.docx
```

### 4. 大模型配置
```json
{
  "llm": {
    "api_url": "https://ws-c8mx6a1akv8midh2.cn-beijing.maas.aliyuncs.com/compatible-mode/v1/chat/completions",
    "api_key": "sk-a21853a78fdb4132b0bb12e169d2e9fe",
    "model": "qwen-plus"
  }
}
```

### 5. 前端技术选择
- Vue 3 CDN (无需构建工具)
- Bootstrap 5 CDN (快速布局)
- 自定义分隔符 `[[ ]]` (避免与 Jinja2 冲突)
