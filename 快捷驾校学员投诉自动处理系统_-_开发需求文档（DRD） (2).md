# 学员投诉自动处理系统 \- 开发需求文档（DRD）

文档版本：V1\.0

编写日期：2026\-05\-11

编写人：投诉管理组

适用对象：系统开发工程师、测试工程师、产品经理

文档说明：本文档详细定义了学员投诉自动处理系统的全部开发需求，包括系统架构、功能模块、业务逻辑、技术规范、UI要求等，是系统开发、测试、验收的唯一标准，严格遵循用户提供的工作流程，无任何篡改和遗漏。

## 一、引言

### 1\.1 项目背景

当前学员投诉处理依赖人工操作，流程繁琐，涉及多系统查询、合同分析、文档撰写、数据统计等重复性工作，效率低下且易出错。为提升投诉处理效率、规范处理流程、减轻工作人员负担，特开发本学员投诉自动处理系统，贴合上市公司（如东方时尚驾校）投诉管理标准，实现全流程自动化处理。

### 1\.2 项目目标

- 打造符合上市公司级别的专业投诉处理Web端后台管理系统，易用、美观、高效，完全贴合实际工作流程。

- 实现投诉工单接收、信息提取、三系统自动查询、合同智能分析、扣费自动计算、官方文档生成、状态流转、数据统计全流程自动化，最大限度减少人工操作。

- 确保系统稳定可靠，错误处理完善，数据安全有保障，所有业务逻辑严格按照需求实现，无功能遗漏。

- 支持LLM模型自由切换，适配不同厂商的大语言模型，满足合同分析的灵活性需求。

- 实现多维度数据统计分析，为驾校分校管理提供数据支撑，提升投诉管理水平。

### 1\.3 适用范围

本系统适用于投诉处理相关工作人员，用于处理学员通过交通部门、驾培协会、邮件信访、电话投诉等各类渠道提交的投诉工单，涵盖工单处理全流程，对接三个外部驾培系统，实现文档生成、状态管理和数据统计。

### 1\.4 术语定义

|术语|定义|
|---|---|
|内部系统|指内部管理系统，地址：http://jxywxt\.dgcheshang\.cn:10003/sypro\_jm/，用于查询学员分校信息、考试进度等。|
|第三系统|指车尚机动车驾驶人计时培训管理平台，地址：http://jppt\.dgcheshang\.cn:8899/platform/login\!login\.action，用于查询学员实操培训时长。|
|驾培系统|指管家鞋驾培平台，地址：https://www\.guanjiaxie\.com:8089/schoolLogin，用于查询学员电子合同。|
|LLM|大语言模型，用于分析学员电子合同，提取扣费规则，计算扣费明细。|
|工单|学员投诉的载体，包含学员基本信息、投诉内容、投诉渠道等核心信息，支持多格式导入。|
|处理时长|从工单接收（状态为“未处理”）到状态变更为“已完结”的总时间，用于统计和超时提醒。|

## 二、系统整体架构

### 2\.1 技术栈要求（明确指定，无歧义）

- 后端框架：Python \+ Flask（优先选择，兼顾轻量和高效，适配多系统接口调用场景）

- 前端框架：Vue\.js（搭配Element UI后台管理模板，提升开发效率，保证UI美观统一）

- 数据库：MySQL 8\.0（适配关系型数据存储，支持高并发查询，便于数据统计分析）

- OCR识别：集成Tesseract OCR（支持验证码自动识别，适配数字、字母、算术验证码）

- LLM集成：通过API接口集成，支持HTTP请求调用，适配各类主流LLM厂商（OpenAI、DeepSeek、通义千问等）

- 文档处理：集成python\-docx（生成Word文档）、pdfkit（生成PDF文档），支持在线编辑和打印

- 缓存机制：使用Redis缓存三系统Session Cookie，提升查询效率

- 请求工具：使用requests库封装HTTP请求，实现超时重试、异常捕获

### 2\.2 核心模块划分（明确模块职责，无遗漏）

1. **工单接收与解析模块**：负责接收各类渠道的投诉工单，支持多格式导入，自动解析工单内容，提取学员核心信息，完成工单初始化。
      

2. **三系统自动查询模块**（核心功能）：根据工单提取的学员身份证号或手机号，自动登录三个外部系统，后台完成学员信息、考试进度、培训时长、电子合同等数据的查询和采集，无需用户手动操作。

3. **LLM合同分析与扣费计算模块**：将学员电子合同（系统查询或用户上传）传入配置好的LLM，提取合同中的扣费规则、报名费用、交费记录等信息，结合学员实际学习进度，自动计算扣费明细和应退费金额，检测特殊退费限制情况。
      

4. **回访管理与文档生成模块**：支持用户选择回访状态，根据回访状态自动生成对应的官方文档（投诉登记表、投诉回复函），支持在线编辑、打印、导出（Word/PDF），严格匹配模板格式。
      

5. **投诉状态流转管理模块**：实现投诉工单“未处理→处理中→已完结”的状态流转，支持状态回退，记录每次状态变更的时间和备注，实现处理时效管理和超时提醒。
      

6. **统计分析与数据看板模块**：在“历史处理”页面展示数据看板，实现多维度统计分析，生成各类图表，支持驾校对分校投诉情况、处理效率等进行可视化管理。
      

7. **系统配置与管理模块**：负责LLM模型配置、业务参数配置（超时天数、投诉类型等），实现管理员账号管理和操作日志记录，无需角色权限区分。
      

### 2\.3 系统流程图（清晰展示全流程）

```mermaid

flowchart TD
    A[工单接收] -->|多格式导入/手动新建| B[自动解析提取学员信息]
    B --> C[三系统自动查询学员数据]
    C -->|内部系统：分校/进度等| C1[第三系统：科二/科三学时]
    C1 --> C2[驾培系统：电子合同/无则提示上传]
    C2 --> D[LLM合同分析+扣费计算]
    D -->|检测特殊情况并提醒| E[用户线下协商，选择回访状态]
    E -->|a.拒绝协商| F1[生成投诉登记表+回复函]
    E -->|b.无法联系| F2[生成投诉登记表]
    E -->|c.同意协商| F2
    E -->|d.其他情况| F3[用户自主选择生成文档]
    F1 --> G[状态变更为处理中/已完结，记录时间和备注]
    F2 --> G
    F3 --> G
    G --> H[数据统计更新，同步至数据看板]
    H --> I[工单归档，支持状态回退]
    style A fill:#f9f,stroke:#333,stroke-width:2px
    style C fill:#9ff,stroke:#333,stroke-width:2px
    ```

## 三、详细功能需求（按模块拆分，细化到每一个操作和字段）

### 3\.1 模块一：工单接收与解析

#### 3\.1\.1 工单导入功能

- 支持导入格式：PDF、Excel（\.xlsx/\.xls）、Word（\.docx/\.doc）、纯文本（\.txt）、图片（\.jpg/\.png/\.jpeg），导入时需提示用户选择文件类型。

- 导入方式：单文件导入（点击上传按钮选择文件），不支持批量导入。

- 手动新建工单：提供表单式录入界面，包含学员姓名、身份证号、手机号、投诉内容、投诉渠道、投诉类型、投诉日期等必填字段，提交后完成工单创建。

- 导入反馈：导入成功后提示“工单导入成功，已自动解析信息”；导入失败（格式不支持、文件损坏）提示具体错误原因，如“文件格式不支持，请选择PDF/Excel等指定格式”。

#### 3\.1\.2 自动信息提取（核心需求）

系统自动解析导入的工单文件，提取以下核心字段，提取失败时提示用户手动补充，补充后可重新触发解析：

|字段名称|字段类型|是否必填|提取要求|备注|
|---|---|---|---|---|
|学员姓名|字符串|是|从工单文本中提取中文姓名（2\-6个字符）|提取失败提示“请手动输入学员姓名”|
|身份证号码|字符串|是|提取18位身份证号（含X），支持识别不同格式的身份证号（如带空格、横线）|核心查询字段，提取失败无法触发三系统查询，必须手动补充|
|手机号码|字符串|是|提取11位中国大陆手机号，支持识别带区号、空格的手机号|用于辅助核对学员信息，提取失败可手动补充|
|投诉内容原文|长文本|是|完整提取工单中的投诉内容，保留原文格式（换行、标点）|支持手动编辑修改|
|投诉渠道|下拉选择|是|优先从工单中提取，无法提取时默认显示“其他”，用户可手动修改|选项见3\.1\.3|
|投诉日期|日期格式（YYYY\-MM\-DD）|是|从工单中提取投诉提交日期，无法提取时默认填充当前日期|支持手动修改日期|

#### 3\.1\.3 字段选项配置（固定配置，不可随意修改）

**投诉渠道（下拉选择，不可新增，顺序固定）：**

- 1\. 交通部门

- 2\. 驾培协会

- 3\. 邮件信访

- 4\. 电话投诉

- 5\. 其他

**投诉类型（单选，不可新增，顺序固定）：**

- A \- 退费纠纷

- B \- 教学服务

- C \- 考试安排

- D \- 合同争议

- E \- 其他（选择此项时，动态显示单行文本输入框，允许用户自定义填写，输入长度限制1\-50字符）

#### 3\.1\.4 工单初始化与存储

- 工单初始化：信息提取完成后，自动生成唯一工单编号（格式：年份\+月份\+3位流水号，如202605001），工单状态默认设置为“未处理”，处理时长开始计时。

- 工单存储：所有工单信息（含导入的原始文件、提取的字段、处理记录）均存储至MySQL数据库，原始文件存储路径为服务器本地指定目录，数据库中记录文件路径。

- 工单查询：提供简单查询功能，可通过工单编号、学员姓名、身份证号、投诉状态、投诉日期范围查询工单。

### 3\.2 模块二：三系统自动查询（核心功能，全自动无人工干预）

重要约束：全部查询流程在后台自动完成，无需用户手动点击任何查询按钮。用户只需完成工单信息提取（确保身份证号正确），系统自动触发三系统查询，查询结果自动关联至对应工单，查询失败时提示具体错误原因并允许用户重新触发查询。

#### 3\.2\.1 系统A：内部管理系统（jxywxt）

- 系统基础信息：
        

    - 登录地址：http://jxywxt\.dgcheshang\.cn:10003/sypro\_jm/

    - 登录账号：zhbgs

    - 登录密码：kj82828282

    - 字符编码：UTF\-8

    - 验证码类型：4位数字字母混合验证码

    - API前缀：/sypro\_jm

- 自动登录逻辑：
        

    - 系统启动时，自动读取账号密码，发送GET请求获取验证码（接口：/sypro\_jm/authImage），通过OCR识别验证码内容。

    - 发送POST请求（接口：/sypro\_jm/userController/login\.action），携带参数accounts（zhbgs）、pwd（明文kj82828282）、rand（OCR识别的验证码），完成登录。

    - 登录成功判断：响应JSON中obj\.loginName字段非空；登录失败（验证码错误、账号密码错误）时，自动重试，最多重试3次，重试失败提示“内部系统登录失败，请检查账号密码或稍后重试”。

    - Cookie缓存：登录成功后，自动缓存Session Cookie至Redis，有效期7200秒（2小时），Cookie过期后自动重新执行登录流程。

- 自动查询逻辑（按身份证号查询）：

    - 登录成功后，自动发送POST请求（接口：/sypro\_jm/xyxxController/listXyxx\.action），携带入参：sfzh（学员身份证号）、page（固定1）、rows（固定10）、sort（固定createtime）、order（固定desc）。

    - 查询学员基础信息：从响应data\.rows\[0\]中提取以下信息，缺失字段标记为“未查询到”：
    

        - 学员所属分校（代号：orgdh，如“明仔”“麻”“沥”）

        - 考试进度（结合xyzt学员状态码映射，如xyzt=2对应“科目一待考”）

        - 报名时间（bmrq，格式YYYY\-MM\-DD）

        - 技能证时间（学员第一次考试通过文科的时间，从时间轴数据中提取）

        - 学员状态（xyzt状态码及对应中文，如1=报名、7=已领证）

        - 收费记录（通过GET请求接口：/sypro\_jm/xyxxController/queryXySfList\.action，携带参数xyid（学员ID），提取sfxm、sfje、sfrq、bz字段）

    - 查询考试经历时间轴：自动发送GET请求（接口：/sypro\_jm/xyxxController/queryXySjz\.action），携带参数xyid（学员ID），解析data\.obj（JSON字符串），提取NodeTitle（节点标题）、NodeTime（节点时间），整理为考试经历列表。

- 学员状态码映射（固定配置，严格按此实现）：
        状态码中文说明状态码中文说明1报名9已退学2科目一待考10暂停培训3科目二待考12科二待考4科目三待考18科一未通过5科目四待考20科二收6已结业30科三收7已领证49科四收8已注销\-\-

#### 3\.2\.2 系统B：计时培训管理平台（jppt）

- 系统基础信息：
        

    - 登录地址：http://jppt\.dgcheshang\.cn:8899/platform/login\!login\.action

    - 登录账号：57kjjx

    - 登录密码：kj81035999

    - 字符编码：GBK

    - 验证码类型：4位数字字母混合验证码，验证码图片URL：/servlet/validate\_image

- 自动登录逻辑：
        

    - 自动发送GET请求获取验证码（接口：/servlet/validate\_image），通过OCR识别验证码内容。

    - 发送POST请求（接口：/platform/login\!login\.action），携带参数：loginId（57kjjx）、clear\_password（明文kj81035999）、password（密码MD5值，小写）、rand（OCR识别的验证码）、loginType（固定\&\#34;3\&\#34;）、customstyle（固定\&\#34;blue\&\#34;）。

    - 登录成功判断：响应HTML中包含“frameset”或“mainframe”标签；登录失败时，自动重试3次，重试失败提示“第三系统登录失败，请检查账号密码或稍后重试”。

    - Cookie缓存：登录成功后，缓存Session Cookie至Redis，有效期7200秒，过期自动重新登录。

- 自动查询逻辑（按身份证号查询）：
        

    - 登录成功后，自动发送POST请求（接口：/school/schoolOprAction\!xsjdshList\.action），携带入参：xyxshzVo\.sfzmhm（学员身份证号）、pageNumber（固定1）、pagesize（固定10）。

    - 查询结果解析：响应为GBK编码的HTML页面，通过BeautifulSoup解析页面中的\&lt;tr\&gt;表格行，判断学员是否存在（页面中包含该学员身份证号即为存在）。

    - 提取核心信息（重点获取科目二、科目三学时）：
                

        - 学员姓名（name）、车型（license\_type，C1/C2）

        - 科目二：平台总学时（第6列training\_time，格式如“15时49分”）、审核总学时（第9列audit\_time，格式如“13时4分”）

        - 科目三：平台总学时（第6列training\_time）、审核总学时（第9列audit\_time）

        - 培训里程（training\_km、platform\_km），作为辅助信息存储

    - 阶段编号对应科目（固定映射）：1=科目一、2=科目二、3=科目三、4=科目四，用于区分各阶段学时。

#### 3\.2\.3 系统C：东莞驾培系统（guanjiaxie，电子合同）

- 系统基础信息：
        

    - 登录地址：https://www\.guanjiaxie\.com:8089/schoolLogin

    - 登录账号：kuaijie

    - 登录密码：kj@82828282

    - 验证码类型：算术验证码（如“3\+5=?”），需计算结果作为验证码答案

- 自动登录逻辑：
        

    - 自动发送GET请求获取验证码（接口：/captcha/captchaImage?type=math\&amp;s=\{random\}，random为随机数），返回JPEG图片。

    - OCR识别图片中的数字和运算符，自动计算算术结果（如识别“3\+5=?”，计算结果为8），最多重试20次（每次尝试不同可能答案）。

    - 发送POST请求（接口：/schoolLogin），携带参数：schoolType（固定\&\#34;1\&\#34;）、username（kuaijie）、password（kj@82828282）、validateCode（算术结果，数字）、rememberMe（固定\&\#34;false\&\#34;）。

    - 登录成功判断：响应JSON中code == 0；登录失败时，自动重试20次，重试失败提示“驾培系统登录失败，请检查账号密码或稍后重试”。

    - Cookie缓存：登录成功后，缓存Session Cookie至Redis，有效期7200秒，过期自动重新登录。

- 自动查询逻辑（按身份证号查询）：
        

    - 登录成功后，自动发送POST请求（接口：/business/student/list），携带入参：pageNum（固定1）、pageSize（固定10）、identity（学员身份证号）。

    - 提取学员ID：从响应JSON的rows数组中提取id（学员ID）、name（学员姓名）、mobile（手机号）、licenseType（车型）、createTime（报名时间），用于后续合同查询。

    - 检查电子合同是否存在：发送POST请求（接口：/business/student/checkContract），携带参数id（学员ID），响应\{\&\#34;code\&\#34;: 0\}表示有合同，其他响应表示无合同。

    - 获取电子合同PDF：有合同则发送GET请求（接口：/business/student/viewContract/\{student\_id\}，student\_id为学员ID），解析HTML页面，通过正则提取PDF链接，拼接完整URL后下载PDF，存储至服务器本地目录，数据库记录PDF路径；无合同则提示用户手动上传合同（支持PDF、图片格式），上传后存储至服务器并关联工单。

#### 3\.2\.4 三系统查询异常处理

- 单个系统查询失败：提示“XX系统查询失败（原因：XXX），已跳过该系统查询，可手动重新触发查询”，不影响其他系统查询流程。

- 所有系统查询失败：提示“所有系统查询失败，请检查网络连接、账号密码或稍后重试”，允许用户手动重新触发全部查询。

- 查询结果缺失：部分字段未查询到时，标记为“未查询到”，不影响后续流程，用户可手动补充缺失信息。

- 网络超时：设置请求超时时间为30秒，超时后自动重试2次（指数退避：1s/2s），重试失败提示“网络超时，查询失败”。

### 3\.3 模块三：LLM合同分析与扣费计算

#### 3\.3\.1 LLM配置（可自由切换）

- 系统设置页面提供LLM配置表单，管理员可填写以下信息：
        

    - LLM API地址（必填，如https://api\.openai\.com/v1/chat/completions）

    - LLM API Key（必填，用于身份验证）

    - 模型名称（必填，如gpt\-3\.5\-turbo、deepseek\-chat）

- 支持自由切换LLM厂商，无需修改代码，保存配置后立即生效，系统不限制具体厂商。

- LLM调用异常处理：调用失败时提示“LLM调用失败，请检查API地址、API Key或模型名称，稍后重试”，允许用户重新触发分析。

#### 3\.3\.2 LLM智能合同分析（核心逻辑）

- 合同来源：系统自动从驾培系统查询到的PDF合同，或用户手动上传的合同（PDF、图片，图片需先通过OCR转换为文本）。

- 分析触发：三系统查询完成（或用户上传合同后），系统自动触发LLM合同分析，无需用户手动操作。

- 分析要求（明确提示LLM）：
        

    - 严格从合同原文中提取信息，不添加任何主观判断，完全以合同条款为准。

    - 提取核心信息：报名总费用、实际交费金额（区分全款/分期付款，分期付款需记录每期交费金额和时间）、各阶段扣费标准明细（如“已考科目一扣X元，已考科目二扣Y元”）、合同有效期、退费相关约定。

    - 输出格式：JSON格式，字段清晰，便于系统解析，示例如下：
                `\{
      \&\#34;报名总费用\&\#34;: \&\#34;4380元\&\#34;,
      \&\#34;实际交费金额\&\#34;: \&\#34;1500元\&\#34;,
      \&\#34;交费方式\&\#34;: \&\#34;分期付款\&\#34;,
      \&\#34;分期付款记录\&\#34;: \[\{\&\#34;金额\&\#34;: \&\#34;1500元\&\#34;, \&\#34;时间\&\#34;: \&\#34;2024\-07\-18\&\#34;\}\],
      \&\#34;扣费标准\&\#34;: \[
        \{\&\#34;阶段\&\#34;: \&\#34;未参加任何考试\&\#34;, \&\#34;扣费金额\&\#34;: \&\#34;综合服务费1100元\+理论培训费600元\+违约金876元\&\#34;\},
        \{\&\#34;阶段\&\#34;: \&\#34;已考科目一\&\#34;, \&\#34;扣费金额\&\#34;: \&\#34;XXX元\&\#34;\}
      \],
      \&\#34;合同有效期\&\#34;: \&\#34;3年\&\#34;,
      \&\#34;退费约定\&\#34;: \&\#34;合同期满，合同终止，无须退费；转校需缴纳转校费500元\&\#34;
    \}`

- 分析结果存储：LLM分析结果自动关联至对应工单，存储至数据库，支持用户查看原始分析结果和合同原文。

#### 3\.3\.3 自动扣费核算（严格按合同条款）

- 核算触发：LLM分析完成后，系统自动结合学员实际学习进度（从内部系统、第三系统查询到的考试进度、培训时长），触发扣费核算。

- 核算逻辑：
        

    - 根据学员当前考试进度（如“待考科目一”“已考科目一未通过”“科目二待考”等），匹配LLM提取的对应阶段扣费标准。

    - 计算应扣总金额：汇总对应阶段的所有扣费项目（如综合服务费、理论培训费、违约金等）。

    - 计算应退费金额：应退费金额 = 实际交费金额 \- 应扣总金额（若应扣总金额＞实际交费金额，则应退费金额为0）。

    - 生成扣费明细：明确列出扣费项目、每项金额、总扣费金额、实际交费金额、应退费金额，格式清晰，可直接用于文档生成。

- 核算结果展示：扣费明细自动展示在工单详情页，支持用户查看，不允许手动修改（若发现异常，可重新触发LLM分析和扣费核算）。

#### 3\.3\.4 特殊情况自动检测与提醒（核心约束）

系统自动检测以下4种特殊情况，检测到后立即在工单详情页标红提示用户，且在生成投诉回复函时自动说明相关理由，明确“此种情况下没有费用退还”：

|触发条件（严格按此判断）|系统提示内容（固定不变）|回复函说明内容（固定不变）|
|---|---|---|
|合同报名时间超过3年（当前日期 \- 报名日期 ＞ 3年）|⚠️ 警告：该学员合同报名时间已超过3年，此种情况下没有费用退还。|该学员合同报名时间已超过3年，根据合同约定，此种情况下没有费用退还。|
|学员技能证时间超过3年（当前日期 \- 技能证时间 ＞ 3年）|⚠️ 警告：该学员技能证时间已超过3年，此种情况下没有费用退还。|该学员技能证时间已超过3年，根据合同约定，此种情况下没有费用退还。|
|科目二考试次数超过5次不及格（从内部系统时间轴提取，科二不及格次数＞5）|⚠️ 警告：该学员科目二考试次数已超过5次不及格，此种情况下没有费用退还。|该学员科目二考试次数已超过5次不及格，根据合同约定，此种情况下没有费用退还。|
|科目三考试次数超过5次不及格（从内部系统时间轴提取，科三不及格次数＞5）|⚠️ 警告：该学员科目三考试次数已超过5次不及格，此种情况下没有费用退还。|该学员科目三考试次数已超过5次不及格，根据合同约定，此种情况下没有费用退还。|

说明：若同时触发多种特殊情况，系统需全部提示，回复函中需逐一说明所有情况。

### 3\.4 模块四：回访管理与文档生成

#### 3\.4\.1 回访状态管理

- 回访触发：扣费明细生成后，用户线下与学员协商完成，在工单详情页选择回访状态（4选1，单选，不可多选）。

- 回访状态选项（固定配置，顺序不变）：
        

    - a、拒绝协商

    - b、无法联系

    - c、同意协商

    - d、其他情况

- 状态补充：选择任意回访状态后，需填写回访备注（必填，长度1\-200字符），记录协商过程、结果等信息，提交后不可修改。

- 回访记录存储：回访状态、回访备注、回访时间（提交时间）自动关联至工单，存储至数据库，可在工单详情页查看。

#### 3\.4\.2 文档自动生成规则（严格按模板格式）

|回访状态|自动生成文档（固定规则，无例外）|文档生成触发|
|---|---|---|
|拒绝协商|1\. 学员投诉登记表（附件2模板）；2\. 给交通部门的学员投诉回复函（附件3模板）|选择状态并提交备注后，自动生成两份文档|
|无法联系|仅生成：学员投诉登记表（附件2模板）|选择状态并提交备注后，自动生成|
|同意协商|仅生成：学员投诉登记表（附件2模板）|选择状态并提交备注后，自动生成|
|其他情况|用户自主选择生成文档：勾选“学员投诉登记表”“投诉回复函”，可多选或全选|选择状态、提交备注、勾选需生成的文档后，点击“生成文档”触发|

#### 3\.4\.3 文档内容要求（严格匹配模板，无格式偏差）

- 学员投诉登记表（附件2模板）：
        

    - 必填字段自动填充：编号（工单编号）、日期（回访日期）、学员姓名、学员电话、学习进度（从内部系统查询的考试进度）、投诉对象（学员所属分校全称）、受理人（当前登录管理员账号）、投诉内容（工单提取的投诉内容原文）、投诉处理（结合工单信息、扣费明细、合同约定填写，格式与模板一致）、处理人签名（自动填充当前登录管理员姓名）、处理日期（回访日期）。

    - 可选字段：上级领导意见、回访记录（后续手动填写，系统预留填写位置）。

    - 格式要求：表格布局、字体（宋体、小四）、行距（单倍行距）、页边距（上下2\.5cm，左右2cm），与附件2模板完全一致。

- 给交通部门的投诉回复函（附件3模板）：
        

    - 必填字段自动填充：接收单位（固定为“东莞市东坑交通局”）、投诉人信息（学员姓名、身份证号）、投诉日期（工单投诉日期）、学员报名信息（报名时间、报名分校、学习进度）、投诉问题（投诉内容原文）、调查核实情况（结合三系统查询信息、LLM分析结果填写）、处理依据（合同条款、扣费明细）、处理意见（如“不予退费”“需缴纳转校费”等）、驾校名称（固定为“东莞市快捷汽车驾驶员培训有限公司”）、盖章位置（预留）、日期（回访日期）。

    - 特殊要求：若检测到3\.3\.4中的特殊情况，需在处理意见中自动添加对应说明，明确“此种情况下没有费用退还”。

    - 格式要求：字体（宋体、小四）、行距（单倍行距）、页边距（上下2\.5cm，左右2cm）、段落缩进（首行缩进2字符），与附件3模板完全一致。

#### 3\.4\.4 文档功能要求（用户操作体验）

- 在线预览与编辑：文档生成后，支持在线预览（分页展示，与Word/PDF格式一致），预览时可直接编辑修改文字内容（如补充细节、调整措辞、修改签名等），编辑后实时保存，不影响原始数据。

- 打印功能：预览/编辑页面提供“打印”按钮，点击后调用浏览器打印功能，支持设置打印参数（纸张大小、方向、页码范围），打印效果与模板一致。

- 导出功能：支持下载为Word格式（\.docx）和PDF格式（\.pdf），导出的文档保留编辑后的内容和格式，可直接用于线下提交或存档。

- 文档存储：生成的文档自动关联至对应工单，存储至服务器本地目录，数据库记录文档路径，可在工单详情页随时查看、编辑、打印、导出。

### 3\.5 模块五：投诉状态流转管理

#### 3\.5\.1 状态定义与流转规则（固定不可修改）

- 状态定义：
        

    - 未处理：工单已接收、信息已提取，尚未触发三系统查询或三系统查询未完成。

    - 处理中：三系统查询完成、LLM分析完成、扣费明细已生成，用户已进行线下协商或已选择回访状态，尚未完结。

    - 已完结：文档已生成（按规则），工单处理全部完成。

- 流转规则：
        

    - 默认流转：未处理 → 处理中 → 已完结（不可逆，除非手动回退）。

    - 状态回退：支持从“已完结”回退至“处理中”（需填写回退备注，说明回退原因，必填，长度1\-200字符），不支持从“处理中”回退至“未处理”。

    - 状态自动变更：
                

        - 工单接收并完成信息提取后，自动变更为“未处理”。

        - 三系统查询完成、LLM分析完成、扣费明细生成后，自动变更为“处理中”。

        - 文档生成完成后，自动变更为“已完结”。

    - 手动变更：用户可手动变更状态（如未处理→处理中、处理中→已完结），变更时需填写备注（必填）。

#### 3\.5\.2 操作记录要求（无遗漏）

- 每次状态变更（自动或手动），必须记录以下信息，存储至数据库，可在工单详情页查看完整操作记录：
        

    - 操作时间（精确到秒，格式YYYY\-MM\-DD HH:MM:SS）

    - 操作备注（必填，手动变更需用户填写，自动变更由系统自动生成，如“三系统查询完成，自动变更为处理中”）

    - 无需记录操作人（按用户需求）

- 操作记录展示：按操作时间倒序排列，清晰展示每次状态变更的时间和备注，不可修改、不可删除。

#### 3\.5\.2 操作记录要求（无遗漏）

- 每次状态变更（自动或手动），必须记录以下信息，存储至数据库，可在工单详情页查看完整操作记录：
        

    - 操作时间（精确到秒，格式YYYY\-MM\-DD HH:MM:SS）

    - 操作备注（必填，手动变更需用户填写，自动变更由系统自动生成，如“三系统查询完成，自动变更为处理中”）

    - 无需记录操作人（按用户需求）

- 操作记录展示：按操作时间倒序排列，清晰展示每次状态变更的时间和备注，不可修改、不可删除，支持复制备注内容，便于后续追溯和核对。

- 操作记录关联：操作记录与对应工单一一关联，删除工单时同步删除对应的操作记录；查询工单时，可同步查看该工单的所有操作记录，形成完整的处理轨迹。

- 异常操作记录：若状态变更过程中出现异常（如系统崩溃、网络中断导致状态变更失败），系统自动记录异常信息，备注为“状态变更异常，需手动核对状态”，同时提示用户检查工单状态，确保状态与实际处理进度一致。

### 3\.6 模块六：统计分析与数据看板

#### 3\.6\.1 数据看板展示（首页/历史处理页面）

- 看板布局：采用分栏布局，清晰展示核心统计数据和图表，支持页面刷新实时更新数据，默认展示近30天数据，可手动切换时间范围（近7天、近30天、近90天、自定义时间范围）。

- 核心统计指标（固定展示，不可修改）：
        

    - 工单总数：展示所选时间范围内接收的投诉工单总数量，区分“未处理”“处理中”“已完结”三类状态的数量，用数字\+进度条展示占比。

    - 处理效率：展示平均处理时长（所有已完结工单的处理时长平均值）、超时工单数量及占比（超时工单/总工单数），超时工单标红突出显示。

    - 投诉类型分布：展示所选时间范围内各类投诉类型（退费纠纷、教学服务等）的数量及占比，便于了解投诉重点。

    - 分校投诉分布：展示各分校（如“明仔”“麻”“沥”）的投诉工单数量，按数量从多到少排序，支持点击分校查看该分校的详细工单列表。

    - 回访状态分布：展示各类回访状态（拒绝协商、无法联系等）的数量及占比，辅助分析学员协商意愿。

- 图表展示（固定类型，不可新增）：
        

    - 折线图：展示工单接收趋势（按日期统计每日接收工单数量），直观呈现投诉量变化规律。

    - 饼图：展示投诉类型占比、回访状态占比，支持鼠标悬浮查看具体数量和占比数值。

    - 柱状图：展示各分校投诉数量对比、各时间段（如每日、每周）工单处理数量，便于对比分析。

#### 3\.6\.2 统计分析功能

- 多维度筛选：支持按投诉类型、投诉渠道、分校、工单状态、时间范围等维度筛选统计数据，筛选后图表和指标同步更新，便于精准分析。

- 数据导出：支持将统计数据（含图表数据、明细数据）导出为Excel格式，导出文件名格式为“统计数据\_时间范围\_导出日期”，便于离线分析和存档。

- 明细查询：点击统计指标或图表中的数据，可跳转至对应明细工单列表，查看具体工单信息，实现“统计\-明细”联动查询。

- 数据刷新：支持手动点击“刷新数据”按钮，实时更新统计数据和图表，默认每30分钟自动刷新一次。

#### 3\.6\.3 数据存储与追溯

- 统计数据存储：所有统计数据（含原始数据、图表数据、筛选结果）均存储至MySQL数据库，保留至少1年历史数据，便于后续追溯和历史对比分析。

- 数据准确性：系统自动核对统计数据与工单原始数据，若出现数据不一致（如统计的工单数量与实际工单数量不符），提示“数据核对异常，请刷新页面重试”，并记录异常日志，便于排查问题。

### 3\.7 模块七：系统配置与管理

#### 3\.7\.1 LLM模型配置

- 配置入口：系统顶部导航栏“系统设置”→“LLM配置”，仅管理员可访问（无需角色区分，登录即可操作）。

- 配置表单：包含LLM API地址、API Key、模型名称三个必填字段，其中API Key输入时隐藏显示（输入内容替换为“\*”），提交表单时校验字段完整性，未填写则提示“请完善所有必填配置项”。

- 配置生效：表单提交后立即生效，无需重启系统，系统后续调用LLM时自动使用最新配置；支持随时修改配置，修改后立即覆盖原有配置，同时记录配置修改记录（修改时间、修改前后的配置内容）。

- 配置校验：提交配置后，系统自动发送测试请求（调用LLM API，发送简单测试指令），校验配置是否有效，若校验失败（如API地址错误、API Key无效），提示“LLM配置校验失败，请检查配置信息后重试”，并显示具体错误原因。

#### 3\.7\.2 业务参数配置

- 可配置参数（固定可配置项，不可新增其他参数）：
        

    - 超时天数：默认3个工作日，可修改为1\-7个工作日，修改后立即生效，用于工单超时提醒的判断。

    - 投诉类型/渠道：仅允许修改“其他”选项的显示文本，不可新增、删除或调整其他选项的顺序和内容。

    - 文档存储路径：可修改服务器本地文档存储目录，修改后新生成的文档将存储至新路径，原有文档仍保留在原路径，不自动迁移。

    - OCR识别重试次数：可修改三系统验证码识别的最大重试次数（默认按各系统规则设置，可调整为1\-20次），修改后立即生效。

- 参数保存：修改参数后点击“保存配置”，系统自动存储至数据库，同时记录参数修改记录（修改时间、修改前后的参数值），支持查看历史修改记录，便于追溯。

#### 3\.7\.3 操作日志管理

- 日志记录范围：系统自动记录所有关键操作，包括工单创建、状态变更、三系统查询、LLM分析、文档生成、系统配置修改、参数调整等，确保操作可追溯。

- 日志内容：每条日志包含操作时间（精确到秒）、操作类型（如“工单创建”“LLM配置修改”）、操作内容（如“创建工单编号202605001”“修改LLM API地址”）、操作结果（成功/失败），若操作失败，需记录失败原因。

- 日志操作：支持按操作时间范围、操作类型、操作结果筛选日志，支持查看日志详情，不允许修改、删除日志，日志保留至少1年。

#### 3\.7\.4 账号管理（简单管理，无需角色权限）

- 账号配置：系统默认提供1个管理员账号（账号：admin，密码：123456），支持修改管理员密码（需输入原密码、新密码、确认密码，新密码长度不少于8位，包含字母和数字），不支持新增、删除账号。

- 密码重置：若忘记管理员密码，可通过服务器后台操作

