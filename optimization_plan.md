# 优化需求：自动识别身份证号/手机号

## 当前问题
用户需要手动切换单选按钮选择"身份证号"或"手机号"查询方式，操作繁琐。

## 优化方案
1. **同时显示两个输入框**：身份证号和手机号输入框都显示在表单中，不需要单选切换
2. **自动识别逻辑**：
   - 优先使用非空输入框
   - 如果两个都有值，优先使用身份证号（更准确）
   - 如果都空，提示用户至少输入一个
3. **后端适配**：`/api/query` 接口根据传入的参数自动判断查询类型

## 改动文件
- `templates/index.html`: 移除单选按钮，改为双输入框同时显示
- `static/js/composables/useComplaint.js`: 修改 `queryAll` 逻辑，自动判断
- `app.py`: 确保后端能正确处理（已有 `phone` 和 `id_card` 参数，只需确认逻辑）
